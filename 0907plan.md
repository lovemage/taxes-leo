# 翻後依牌力分組策略修改計劃

**日期**：2026-09-07  
**基準版本**：v0.1.4  
**狀態**：規劃中，尚未開始修改程式  
**修訂**：2026-09-08 補明公共牌比較、分類定義、恢復語意與完整度統計
**目標**：讓翻牌、轉牌、河牌不只顯示動作「可用／不成立」，而是能依牌面與玩家牌力顯示、編輯並實際執行動作頻率。

**架構決議**：沿用翻前的無狀態覆寫模式。後端不保存可變策略；UI／呼叫端傳入完整的稀疏覆寫清單，run 開始時以值快照凍結。

---

## 一、目前狀況

v0.1.4 已完成：

- 翻牌、轉牌、河牌三個策略階段。
- 「無人下注」與「面對下注」兩種狀態。
- 過牌、跟注、1/3 底池、2/3 底池、1 個底池、蓋牌六種動作欄位。
- 六種牌面外觀，以及乾燥／濕潤結構分類。
- Bot 已會依牌面、勝率、底池賠率與對手數動態決策。
- 引擎已有 `BoardTextures { surface, connectivity }` 與 `classify_board()`；這是現況，不是本期新增項目。
- `RuleSet::resolve()`、`RuleSet::analyse()`、`CoverageStats` 已存在，但尚未接入產品執行與 UI 路徑。
- `equityTexture/v2-unapproved` 與 `checkFold/v0` fallback 已存在，可作為漸進接線的安全底線。

目前策略頁只回傳各動作是否合法，因此畫面顯示「可用／不成立」，沒有：

- 牌力分組。
- 各動作百分比。
- 翻後規則的編輯與儲存。
- 使用者設定的規則與 Bot 實際決策連動。

---

## 二、本次核心決議

### 2.1 牌力採八組

現有 `HandStrength` 的 `Value／Bluff／BluffCatcher／Draw／Air` 不足以支援策略編輯，其中 `Bluff` 是動作意圖，不是手牌本身的強度，應改為以下八組：

| Key | 顯示名稱 | 判定概念 | 常見例子 |
|---|---|---|---|
| `nuts` | 堅果／超強成牌 | 接近目前牌面可形成的最強範圍 | 堅果同花、葫蘆、四條、安全牌面的頂三條 |
| `madePlusDraw` | 成牌＋強聽牌 | 已有攤牌價值，同時有大量有效補牌 | 頂對＋同花聽牌、兩對＋順子聽牌 |
| `strongMade` | 強成牌 | 可持續價值下注或加注 | 高兩對、三條、超對、頂對頂踢腳 |
| `mediumMade` | 中等成牌 | 有攤牌價值，但通常不適合打大底池 | 頂對弱踢腳、第二對、部分口袋對 |
| `bluffCatcher` | 弱成牌／抓詐 | 主要用於控池或抓詐唬 | 底對、弱口袋對、部分 A 高 |
| `strongDraw` | 強聽牌 | 約八張以上有效補牌，或複合聽牌 | 同花聽牌、兩頭順、同花＋順子複合聽牌 |
| `weakDraw` | 弱聽牌 | 補牌較少、非堅果或只有後門潛力 | 卡順、弱後門同花、雙高張 |
| `air` | 空氣牌 | 沒有足夠攤牌價值，也沒有有效聽牌 | 未成對且無明顯順花結構 |

### 2.2 分組必須相對於牌面

不能只用「一對、兩對、三條」直接決定牌力組。同樣是兩對：

- 在乾燥彩虹面可能屬於強成牌。
- 在四順、三花或公對牌面可能只屬於中等成牌。

分類器必須綜合：

1. 目前成牌類別與 kicker。
2. 玩家底牌是否真的改善公共牌。
3. 相對於所有合法對手組合的目前牌力百分位。
4. 有效補牌數與是否為堅果聽牌。
5. 牌面外觀與乾燥／濕潤結構。
6. 翻牌、轉牌或河牌。

### 2.3 分類判定樹

分類結果必須唯一，且判定流程是**明確的判定樹**，不是純優先序。`bluffCatcher`
必須向後檢查該手是否已符合強聽牌，這種例外無法用「依序取第一個命中」表達；
若照純優先序實作，`priority()` 的順序測試會鎖住一個判定流程實際上並不遵守的順序。

判定依下列順序，前一條未命中才進入下一條：

1. `nuts`：百分位 `>= nuts_floor_myriad`。
2. `madePlusDraw`：`is_made_hand` 且（`effective_outs_centi >= strong_draw_floor_centi` 或 `combo_draw`）。不設百分位下限。
3. `strongMade`：`is_made_hand` 且百分位 `>= strong_made_floor_myriad`。
4. `mediumMade`：`is_made_hand` 且百分位 `>= medium_made_floor_myriad`。
5. `bluffCatcher`：百分位 `>= showdown_value_floor_myriad`，**且不符合第 6 條的強聽牌條件**（明示的向後檢查，避免 A 高堅果同花聽牌被提前截走）。
6. `strongDraw`：`effective_outs_centi >= strong_draw_floor_centi` 或 `combo_draw`。
7. `weakDraw`：`0 < effective_outs_centi < strong_draw_floor_centi`，或 `backdoor_draw`。
8. `air`：以上皆未命中。

`HandStrength::priority()` 只供規則條件的顯示與排序使用，**不是分類判定順序**；
doc comment 必須寫明這件事，測試也只鎖顯示順序，不得被當成分類流程的規格。

**「成牌」的唯一定義**：`is_made_hand = made_category >= Category::Pair && improves_board`。
只靠公共牌成立的牌型不算成牌；否則公對面上握 `A2` 的手也有「一對」，會被判進
`mediumMade`，整個牌力組被公共牌灌水。`improves_board` 的定義見 §3.2。

河牌已沒有未來補牌，因此河牌只會出現：

- `nuts`
- `strongMade`
- `mediumMade`
- `bluffCatcher`
- `air`

河牌的不可達組別必須在**程式層**封死，不是只靠 §4.2 的列舉器過濾：`classify()`
在 `Street::River` 走獨立分支，並以測試鎖定「河牌永不回傳 `strongDraw`／`weakDraw`／`madePlusDraw`」。

分類器只在英雄有實際決策時呼叫；已全下或沒有合法選項的節點不分類。
這一句同時是 §5.3 完整度分母的前提。

### 2.4 牌面採兩個維度

牌面不是八個互斥類別，應拆為：

**外觀（六選一）**：

1. 花面
2. 聽花面
3. 彩虹面
4. 彩虹公對面
5. 聽花公對面
6. 三條面

**結構（二選一）**：

1. 乾燥面
2. 濕潤面

例如同一個牌面可以同時是「彩虹面＋濕潤面」。策略節點應以兩個條件共同匹配，而不是把乾燥／濕潤當成獨立牌面列。

現有 `PostflopCondition.board_texture` 單欄無法表示複合條件，Stage 0 必須改為：

```rust
pub board_surface: Option<BoardSurface>,
pub board_connectivity: Option<BoardConnectivity>,
```

並同步改寫 `matches()`、`contains()`、`intersects()`；單欄設計使用的 `texture_intersects()` 特例移除。規則可以只指定其中一軸作為廣域條件，也可以同時指定兩軸精確匹配。

IPC DTO 同步遷移，不保留把兩軸混裝回單一清單的相容層：

- `PostflopStrategyView.textures` 改為 `surfaces` 與 `connectivities`。
- `PostflopTextureView.dimension` 隨單一 `BoardTexture` DTO 一併移除，分別產生 surface／connectivity option view。
- 同步修改 `agent.rs` 的 `textures.contains(...)`、`ipc/strategy.rs` 的 `BoardTexture::ALL`、前端 API 與 generated TypeScript 型別。
- 舊版 DTO 若需匯入，只能在版本化邊界明確轉換，不讓舊 schema 滲入新規則引擎。

---

## 三、牌力分類器設計

### 3.1 新增資料結構

預計在 `apps/engine/src/strategy/postflop.rs` 新增或調整：

```rust
pub enum HandStrength {
    Nuts,
    MadePlusDraw,
    StrongMade,
    MediumMade,
    BluffCatcher,
    StrongDraw,
    WeakDraw,
    Air,
}

pub struct HandStrengthSnapshot {
    pub group: HandStrength,
    pub made_category: Category,
    pub improves_board: bool,
    /// 公共牌自身的最佳部分牌型類別，供 UI 解釋「為什麼不算改善」
    pub board_partial_category: Category,
    /// 對「一個均勻隨機的合法對手底牌」的目前牌力百分位
    pub random_opponent_percentile_myriad: Myriad,
    pub has_showdown_value: bool,
    pub raw_outs: u8,
    pub effective_outs_centi: u16,
    pub nut_draw: bool,
    pub combo_draw: bool,
    pub backdoor_draw: bool,
}
```

enum 宣告順序與 §2.3 判定樹一致，但 `priority()` 僅供顯示排序；規則業務順序不得暗中依賴 derived `Ord`。

`HandStrengthSnapshot` 用於：

- 將 `group` 用於規則比對。
- UI 說明「為什麼被分到這一組」。
- 決策 trace 與逐手重播。
- 測試分類結果是否穩定。

`random_opponent_percentile_myriad`、`has_showdown_value`、`raw_outs` 與 `effective_outs_centi` 是指定底牌的分類預覽／trace 資料，不是牌力組規則節點的可編輯欄位。

三個聽牌旗標必須有確定性定義，不能只宣告欄位：

| 欄位 | 定義 |
|---|---|
| `combo_draw` | 同時具備至少一種同花聽牌與至少一種順子聽牌（含卡順） |
| `nut_draw` | 補牌完成後，該牌型為當下公共牌所能形成的最強同花或最強順子；是否因公對而可能被葫蘆壓過**不納入判定**，此限制寫入 doc comment |
| `backdoor_draw` | 僅翻牌成立。後門同花＝英雄兩張底牌同花色且公共牌恰有兩張同色；後門順子＝存在一個五點數視窗，英雄底牌與公共牌在其中合計有三種點數且至少一張來自底牌，仍缺兩張 |

### 3.2 工程預設門檻與公共牌比較

以下先作為未簽核工程值，之後由牌手顧問校準。門檻不得散落成 magic number，全部集中在可版本化的 `HandStrengthConfig`，並標示 `consultantApproved: false`：

| 常數 | 工程預設 | 用途 |
|---|---:|---|
| `nuts_floor_myriad` | `9_500` | `nuts` 下限；不再使用未定義的「反向壓制」條件 |
| `strong_made_floor_myriad` | `8_000` | `strongMade` 下限 |
| `medium_made_floor_myriad` | `5_500` | `mediumMade` 下限 |
| `showdown_value_floor_myriad` | `3_500` | 「有攤牌價值」與 `bluffCatcher` 的**同一個**下限 |
| `strong_draw_floor_centi` | `800` | 強聽牌所需有效補牌（8.00 張） |
| `overcard_out_weight_centi` | `50` | 高張成對補牌的折算係數（0.50 張） |

「有攤牌價值」唯一定義為 `random_opponent_percentile_myriad >= showdown_value_floor_myriad`。
`bluffCatcher` 直接引用同一個常數，不再另寫 35%——同一門檻寫兩次必然漂移。
各組的完整判定條件見 §2.3 判定樹，本節只定義常數與其語意。

**`improves_board` 的定義（Stage 1 必須先做這一項）**

原本的「英雄七／六／五張牌的最佳牌型是否嚴格優於只用公共牌的牌型」在翻牌與轉牌**不可計算**：`eval::evaluate` 至少需要 5 張牌（`apps/engine/src/eval.rs:79`，少於 5 張直接 panic），翻牌公共牌只有 3 張、轉牌只有 4 張。而且翻牌時英雄只有 5 張可用，最佳五張必然含兩張底牌，直接比較會恆為 `true`，欄位失去資訊。改為：

- 新增 `board_partial_rank(board) -> PartialRank`，對 3～5 張公共牌計算**公共牌自身**的最佳部分牌型。3／4 張時只可能是 `HighCard`／`Pair`／`TwoPair`（4 張）／`Trips`／`Quads`（4 張）；順子與同花由公共牌獨立成立只可能發生在河牌。河牌直接以 `evaluate(board)` 計算。
- `improves_board` 為真的條件：英雄成牌 category **嚴格大於**公共牌部分 category；或 category 相同但英雄成牌的關鍵牌值（**不含踢腳**）嚴格較高。踢腳一律不算改善。
- `board_partial_category` 一併寫入 `HandStrengthSnapshot`，供 UI 解釋與 trace。

Golden 向量（Stage 1 必備）：

| 底牌 | 公共牌 | `improves_board` |
|---|---|---:|
| `AK` | `222` | false（只有踢腳） |
| `KK` | `222` | true（葫蘆） |
| `A2` | `KK7` | false |
| `77` | `KK7` | true |
| 任意兩張無關牌 | `AKQJT` 彩虹（河牌） | false |

95%／80%／55%／35% 是校準起點，不直接凍結。Stage 1 第一個產物是 §2.1 全部範例牌型（特別包含 A 高、K 高與各類強聽牌）對隨機合法對手組合的實測百分位對照表；依對照表修正門檻並留下 golden vectors，再交由牌手顧問確認。對照表標題必須明寫「對手模型：均勻隨機合法組合」，理由見 §3.3。

### 3.3 有效補牌與目前牌力百分位

**有效補牌**

有效補牌不能只計算「能讓牌型升級的牌」，還要排除會讓對手形成更強牌的污染補牌。第一版至少完成：

- 同花聽牌與堅果同花聽牌。
- 兩頭順、卡順、雙卡順。
- 同花＋順子的複合聽牌。
- 一對改善為兩對／三條的補牌。
- 未成對高張改善為一對的補牌；每張高張補牌先以 `overcard_out_weight_centi`（0.50 張）計算，因此兩張活高牌最多由 6 張 raw outs 折算為 3.00 張。
- 翻牌後門同花與後門順子標記（定義見 §3.1）。

補牌先按實際牌張去重，再扣除污染補牌；一般乾淨順／花補牌以 1.00 計，高張成對補牌以 0.50 計，使用 `effective_outs_centi` 保存，避免浮點運算。折算係數集中在 `HandStrengthConfig`，Stage 1 對照表必須另列 raw outs 與折算後 outs。

**污染補牌的判準（第一版採白名單式）**

「明顯會讓對手形成更強牌」是機率敘述、不是確定性條件，與本節的確定性要求衝突，因此必須落成可人工驗算的規則。第一版採 (a)；(b) 列為備案，改採前必須先過 benchmark：

- (a) **白名單式**：只在該補牌使公共牌成三條、使公共牌成四張同花色、或完成一個純由公共牌構成的順子時，將該補牌折算為 `polluted_out_weight_centi`（工程預設 `0`）。
- (b) **窮舉式**：假設該補牌落下後重算英雄百分位，低於 `outs_pollution_floor_myriad` 的補牌折 0。較嚴謹，但成本約為「補牌數 × 一次完整窮舉」，必須與分類 benchmark 一併量測後才可切換。

兩者的係數與門檻都寫進 `HandStrengthConfig`，並在 trace 標明本次採用哪一種。

**目前牌力百分位**

分類必須是確定性的；相同底牌、公共牌與公開狀態不得因隨機取樣得到不同牌力組。`random_opponent_percentile_myriad` 不沿用 Bot 目前的 Monte Carlo equity，改為列舉所有合法對手兩張底牌並用 `eval::evaluate` 比較：

| 街別 | 對手底牌組合數 |
|---|---:|
| 翻牌 | `C(47, 2) = 1,081` |
| 轉牌 | `C(46, 2) = 1,035` |
| 河牌 | `C(45, 2) = 990` |

百分位固定使用 `floor((2 × wins + ties) × 10_000 / (2 × total))`，避免平台或執行次序造成捨入差異。

**對手模型與死牌集合必須明寫**，否則上表三個數字沒有唯一解，Stage 1 對照表也會被誤讀：

- 對手模型是**均勻隨機的合法組合**，不是「會延續到本街的範圍」。同一手頂對在 c-bet 池與 3-bet 池對抗的範圍差很多，但分類器看不到。`HandStrengthConfig` 明列 `opponentModel: uniformRandom`，欄位名稱亦改為 `random_opponent_percentile_myriad`，避免顧問以「對手續打範圍」的直覺去校準門檻。
- 百分位固定是**對一個對手**的牌力序位。多人底池由規則維度（`active_players`／`opponents_behind`）處理，分類器不因人數調整，並以測試鎖定。
- 死牌只扣英雄兩張與已公開的公共牌；棄牌者的手牌不扣、burn card 不扣、多人時對手之間不做互斥。

**公共牌本身即成手的退化案例**

河牌 `AKQJT` 彩虹這類牌面，幾乎所有底牌都與公共牌同分，上述公式會讓所有人落在約 50%，於是沒有任何一手是 `nuts`，打公共牌的手還會被分到 `mediumMade`。因此明訂：**河牌 `improves_board == false` 時，牌力組強制不高於 `bluffCatcher`**，並以測試鎖定（`AKQJT` 彩虹、四張同花公共牌、公共牌葫蘆三種案例）。

列舉計算先做獨立 benchmark，再接進每次決策；驗證既有延遲預算（p50 ≤ 1 ms、p99 ≤ 5 ms）。這是核心規格 7.3 的明示局部例外，僅適用於必須確定性的牌力分類器：處置順序為精確列舉 → 節點級確定性 cache／查表 → 誤差已量化揭露的確定性近似 → 經業主同意後調整門檻；不得退回隨機抽樣。既有 `equityTexture` baseline 的 Monte Carlo 不受此例外影響。

---

## 四、翻後策略節點

### 4.1 節點鍵與牌局線路

每條翻後策略至少包含：

```text
街別
× 下注狀態
× 牌面外觀
× 乾燥／濕潤
× 牌力組
× 當前／前街公開 action history
× 有效籌碼
× 動作頻率
```

面對下注時加入 `facingSize`；其他必要條件為：

- 單次加注池／3-bet pot／4-bet pot。
- 單挑／多人底池。
- 身後仍會行動的對手數。
- SPR 範圍。
- 玩家位置／本街行動順序。
- 有效籌碼分檔沿用既有 `StackBucket` 與規則細則 8.5，不新增第二套邊界；逐對手數值由 `OpponentPublic.stack` 推導。

第一版 action history 至少具備以下明確軸：

```rust
pub enum AggressorRole { Hero, Opponent, None }
pub enum RelativeAggressorOrder { HeroBefore, HeroAfter, NotApplicable }

pub struct PostflopLine {
    pub previous_street_aggressor: AggressorRole,
    pub last_aggressor_before_current_street: AggressorRole,
    pub relative_aggressor_order: RelativeAggressorOrder,
    pub hero_checked_this_street: bool,
    pub current_street_bet_count: u8,
    pub current_street_raise_count: u8,
}

pub struct PostflopLineCondition {
    pub previous_street_aggressor: Option<AggressorRole>,
    pub last_aggressor_before_current_street: Option<AggressorRole>,
    pub relative_aggressor_order: Option<RelativeAggressorOrder>,
    pub hero_checked_this_street: Option<bool>,
    pub current_street_bet_count: Option<RangeInclusive<u8>>,
    pub current_street_raise_count: Option<RangeInclusive<u8>>,
}
```

`PostflopLine` 是 context 的具體值，`PostflopLineCondition` 是規則側的可選／範圍條件；`matches()` 比較 condition 與 context，`contains()`／`intersects()` 只比較 condition 集合，不得混用相等語意。bet／raise 使用範圍，才能表示「至少加注一次」。

`last_aggressor_before_current_street` 是跨街回溯、但不包含本街目前下注的最近主動方；即使上一街 check-through，仍保留更早街主動方，才能區分 delayed c-bet 與 probe。`relative_aggressor_order` 只在該主動方是對手時使用 `HeroBefore`／`HeroAfter`；主動方為 Hero 或 None 時強制定為 `NotApplicable`。列舉器不得為不適用情況產生兩個布林分支。

所有線路值由既有完整 `PublicAction` history、`current_street_history()` 與公開座位順序推導，不新增 `DecisionView` 欄位。

有效籌碼不屬於 `PostflopLine`：在 `PostflopContext` 增加具體值 `effective_stack_bucket: StackBucket`，在 `PostflopCondition` 增加 `effective_stack_bucket: Option<StackBucket>`，與既有 `spr_centi` 同級。

線路名稱與最小條件固定如下；列舉器與 UI 共用同一個分類函式，不各自複製判斷：

| 下注狀態 | 線路 | 最小條件 |
|---|---|---|
| 無人下注 | c-bet 機會 | `previousStreetAggressor=Hero` |
| 無人下注 | delayed c-bet 機會 | `previousStreetAggressor=None`、`lastAggressorBeforeCurrentStreet=Hero` |
| 無人下注 | donk bet 機會 | `previousStreetAggressor=Opponent`、`relativeOrder=HeroBefore` |
| 無人下注 | stab／float bet 機會 | `previousStreetAggressor=Opponent`、`relativeOrder=HeroAfter` |
| 無人下注 | probe bet 機會 | `previousStreetAggressor=None`、`lastAggressorBeforeCurrentStreet=Opponent`、`relativeOrder=HeroBefore` |
| 無人下注 | delayed stab 機會 | `previousStreetAggressor=None`、`lastAggressorBeforeCurrentStreet=Opponent`、`relativeOrder=HeroAfter` |
| 無人下注 | 無主動方底池下注機會 | `lastAggressorBeforeCurrentStreet=None` |
| 面對下注 | 面對 c-bet | `previousStreetAggressor=Opponent` |
| 面對下注 | 面對 delayed c-bet | `previousStreetAggressor=None`、`lastAggressorBeforeCurrentStreet=Opponent` |
| 面對下注 | 面對 donk bet | `previousStreetAggressor=Hero`、`heroCheckedThisStreet=false` |
| 面對下注 | 過牌後面對 stab／float bet | `previousStreetAggressor=Hero`、`heroCheckedThisStreet=true` |
| 面對下注 | 面對 probe bet | `previousStreetAggressor=None`、`lastAggressorBeforeCurrentStreet=Hero` |
| 面對下注 | 面對無主動方底池下注 | `lastAggressorBeforeCurrentStreet=None` |

「面對下注」名稱描述的是決策情境，不使用「直接加注／check-raise」等英雄尚未選擇的動作名稱；英雄是否已過牌仍是可見的次級條件。不得建立可編輯的共用「其他」桶；若出現未覆蓋但可達的 canonical 線路簽章，列舉測試直接失敗，要求先命名／映射，避免一條規則吞掉多個殘餘情境。

同一狀態內若多條命名條件可同時命中，依表格順序取第一個；測試需涵蓋多人池、前街 check-through、stab／float、防守 c-bet 與面對 donk。

### 4.2 節點列舉、規模與可達性

Stage 0 先實作 `enumerate_postflop_nodes()`，用程式列出真正可達的節點並建立 `content_size` 類型的數量／序列化體積閘門，不能先用笛卡兒積寫死內容。

列舉器必須排除不可能組合，例如：

- 翻牌三條面必然沒有跨 rank 順子結構，不列「三條面＋濕潤面」。
- 河牌不列 `strongDraw`、`weakDraw`、`madePlusDraw`。
- 與下注狀態矛盾的動作或 `facingSize` 不列節點。
- 線路必須命中 §4.1 的唯一命名映射；不列共用「其他」桶，也不列 `lastAggressor != Opponent` 卻具有 `HeroBefore／HeroAfter` 的退化組合。

列舉報表至少分別輸出核心節點、加入 facing size 後、加入 pot type／SPR／位置／action history／有效籌碼後的數量，作為 DTO 與官方內容凍結前的審核依據。

### 4.3 動作意圖與實際 Action 換算

不新增六欄 `u16` 的 `PostflopActionWeights`，避免和既有 `ActionDistribution<(Action, Myriad)>` 形成兩份真理。規則保存尺寸意圖與既有 `Myriad`（`u32`，`FULL = 10_000`）：

```rust
pub struct PostflopIntentDistribution {
    pub weights: Vec<(PostflopActionKind, Myriad)>,
}
```

決策時的完整換算／Bot 管線：

```text
PostflopActionKind 頻率
→ 依當下底池／有效籌碼用 postflop_raise_to() 轉成 Action
→ fit_raise_sizes() 合併撞在相同合法尺寸的意圖
→ drop_free_fold() 保護工程 fallback
→ mask_and_renormalise() 移除非法動作並正規化
→ 依規則 source 產生有效參數，完整跑完既有七階段 pipeline
→ ActionDistribution<(Action, Myriad)>
```

`source=UserOverride` 代表使用者輸入的絕對頻率，沿用翻前覆寫語意：

- 將同一份合法化後分布同時作為 `reference` 與 `baseline`，不得被 `exploitAdjustmentCapPp` 拉回工程基準。
- 不跳過 pipeline；把 persona 倍率中和為 `FULL`、`allowedBetSizes` 中和為涵蓋覆寫中全部合法尺度、noise 中和為 0，並以相同 reference／baseline 讓 cap 成為恆等變換。
- 七個 trace 階段全部保留，並標記 `neutralizedByAbsoluteOverride`、原始參數與有效中性參數，讓結構與一般翻後決策一致。
- 只有最小加注、有效籌碼、全下、重合尺寸與非法動作遮罩可以改變實際頻率；改變前後都寫入 trace。

`Official`／`Generic`／`EngineeringFallback` 是 Bot 可調整 baseline：使用來源分布作為原始 `reference`，再照現有 pipeline 套用 persona、behavior、cap 與 noise。`equityTexture` fallback 仍保留既有 `drop_free_fold()`，不得因規則來源本身已把無人下注 fold 設為 0 而刪除。

合法性規則：

- 無人下注：可過牌或下注；跟注與蓋牌固定為 0。
- 面對下注：不可過牌；可跟注、加注或蓋牌。
- 實際執行仍套用最小加注、有效籌碼與全下限制。
- 遮罩／尺寸合併後，剩餘頻率以既有整數規則精確正規化為 `10_000`。
- UI 顯示的使用者頻率與 trace 中「合法化前覆寫」必須完全相同；若合法性使最終頻率不同，trace 與 UI 重播需明確列出原因。

### 4.4 規則解析與 fallback

避免要求顧問逐格填寫數百個節點，採「一般規則＋稀疏特例覆寫」：

```text
使用者指定特例
→ 顧問／官方翻後規則
→ 同牌力組的一般規則
→ 現有 equityTexture/v2-unapproved 工程基準
→ checkFold/v0 最終合法 fallback
```

每條規則保存 `id`、`priority`、`condition`、意圖頻率、`source`、`version`、`consultantApproved`。`RuleSet::resolve()` 接入正式 Bot 決策，`RuleSet::analyse()` 用於規則診斷。

---

## 五、策略頁調整

### 5.1 導覽順序

```text
翻牌／轉牌／河牌
→ 無人下注／面對下注
→ 牌局線路
   - 無人下注：c-bet／delayed c-bet／donk／stab-float／probe／delayed stab／無主動方底池
   - 面對下注：面對 c-bet／delayed c-bet／donk／probe／過牌後面對 stab-float／無主動方底池下注
→ 牌面外觀
→ 乾燥／濕潤
→ 牌力組
→ 面對下注尺度（需要時）
```

牌局線路是第一級可見導覽，不藏在「更多條件」，且選項依下注狀態及 §4.1 映射過濾；進階的 bet／raise 次數範圍、位置、玩家數、SPR 與 `StackBucket` 才放在更多條件。線路名稱只描述英雄當前面對的情境，不預設英雄將跟注、加注或蓋牌。

### 5.2 畫面內容

把目前的「可用」改為實際頻率，例如：

```text
翻牌 → 無人下注 → c-bet → 彩虹面＋濕潤面 → 強聽牌

過牌 30%｜1/3 底池 45%｜2/3 底池 20%｜滿池 5%
```

畫面需要：

- 六個動作的百分比輸入欄。
- 不成立動作維持顯示，但停用且固定為 0%。
- 即時顯示合計，未滿或超過 100% 時禁止儲存。
- 顯示規則來源與**來源層級**：你的節點覆寫／你的規則 `R#`／官方 v#／同組通則／工程 fallback。
- 顯示是否已經顧問簽核。
- 恢復操作依下段定義。
- 節點編輯器只顯示牌力組與條件，不把單手的百分位／補牌數誤當節點欄位。

另設「指定底牌分類預覽」，才顯示該手牌的分類說明、目前百分位與有效補牌數。

**恢復操作的語意**

原本只寫「提供恢復繼承值與恢復官方預設」，兩顆按鈕在現有模型下都沒有定義。問題有兩層。

第一層是**覆寫粒度**。§4.4 是「規則＋條件」模型，一條使用者覆寫可以只指定 surface 不指定 connectivity，因此涵蓋一整片節點；但驗收條件 10 要求「修改一個節點只影響該條稀疏覆寫」。在被廣域使用者規則涵蓋的節點上按恢復，只有兩條路，兩條都壞：刪掉整條規則會連帶影響其他節點；把規則切成補集條件會讓規則數暴增，還會自己觸發 §5.3 的 `Overlap` 警告。因此覆寫 DTO 拆成兩種（見 §6.1），恢復語意分別定義：

| 值的來源 | 「恢復繼承值」的行為 |
|---|---|
| 你的節點覆寫 | 刪除該筆 `PostflopNodeOverrideView`，值落回下一層；只影響這一個節點 |
| 你的規則覆寫 | **不就地清除**。按鈕改為「前往規則 `R#`」＋「以節點覆寫蓋過」，並顯示該規則涵蓋的節點數 |
| 官方／通則／fallback | 按鈕停用，說明「此節點沒有你的覆寫」 |

第二層是**「恢復官方預設」在現有四層鏈上與「恢復繼承值」等價**。鏈是 `UserOverride → Official → Generic → EngineeringFallback`，刪掉使用者覆寫後落到的就是官方值（官方排在通則之前）；而官方層沒有規則時（第一版很可能整批沒有，見 §十待確認 6），「恢復官方預設」沒有定義。二選一，Stage 0 鎖定：

- **方案 A（預設）**：只留一顆「恢復繼承值」，移除「恢復官方預設」。
- **方案 B**：層次改為 `UserNodeOverride → UserRuleOverride → Official → Generic → EngineeringFallback`。此時「恢復繼承值」＝刪節點覆寫、落回使用者自己的規則；「恢復官方預設」＝同時略過使用者兩層，且必須明確定義它是刪除涵蓋此節點的使用者規則，還是寫入一筆 official-pinned 標記。

無論哪個方案都禁止「把工程 fallback 或通則的值寫成一筆使用者覆寫」：那會把未簽核值凍進 §6.2 的 `ContentSnapshot`，並讓 §5.3 的完整度虛報。官方層無值時按鈕停用並顯示原因。

其餘必須定義的行為：

- **批次恢復**：可對整條線路／整個牌面／整個牌力組／整街執行，執行前顯示受影響節點數與二次確認。
- 恢復後重跑 §5.3 驗證，並進入未儲存草稿狀態。
- 恢復可 undo，與一般編輯共用同一個 undo stack。
- 進行中的 run 已依 §6.2 凍結快照，恢復不影響它；UI 必須顯示這一點。
- 恢復操作本身不寫入 trace 或 manifest，只有最終覆寫清單會進快照。
- 恢復的對照基準是本機覆寫清單，其保存方式見 §6.1。

### 5.3 規則診斷與完整度

先同步 `RuleIssue` 與 UI 規格 D.8 的定義及嚴重度：

| Issue | 判定 | 嚴重度 |
|---|---|---|
| `Overlap` | 同一 `source` 層內兩條規則部分相交 | warning |
| `Shadowed` | 同一 `source` 層內較早規則完整包含較晚規則 | warning；調整現有 `is_error()` |
| `Unreachable`（新增） | 條件無法命中 `enumerate_postflop_nodes()` 的任何可達 context | warning |
| `Impossible` | 靜態條件自相矛盾／範圍為空、權重合計不等於 `10_000`，或正權重全部填在該下注狀態固定不成立的動作 | error |
| `LayerOrderViolation`（新增） | 規則來源排序違反使用者覆寫優先不變量 | error |

- 使用者覆寫壓過官方通則是設計本意，跨 `source` 的一般相交不產生 overlap／shadowed 警告。
- 規則建構與解析強制 `UserOverride → Official → Generic → EngineeringFallback` 的來源順序，另以排序不變量測試防止官方通則排到使用者覆寫之前。
- 執行期因籌碼／最小加注造成 `DistributionError::AllWeightsMasked` 仍走正常 `FallbackReason::AllWeightsMasked`，不回推成編輯期 `Impossible`。
- Stage 0 明文把 UI 規格 D.8 的「完全不可達不得存在」調整為 warning 語意；只有上述靜態 `Impossible` 與來源排序錯誤阻擋保存。
- `Unreachable` 的 O(規則數 × 可達節點數) 成本與 `analyse()` 的 O(n²) 一起量測；若超過互動預算，改以條件反向列舉／索引候選節點，不在每次保存逐條掃描全部節點。
- 診斷結果按規則／條件聚合，避免逐對規則產生重複噪音。
- `error` 必須修正後才能保存；`warning` 可保存，但保留在驗證摘要。
- 完整度依下段定義；`CoverageStats` 不直接接上策略頁。

**完整度統計拆成兩個指標**

策略頁在儲存前沒有 run，只能算靜態節點覆蓋；報表算的是執行期決策次數比例。現有 `CoverageStats`（`apps/engine/src/strategy/postflop.rs:642-671`）是後者——`record()` 只在決策發生時累加——把它接到策略頁等於把執行期計數器接到沒有執行期資料的畫面。因此定義兩個型別：

| 型別 | 分母 | 使用處 |
|---|---|---|
| `PostflopStaticCoverage { node_set_version, total_nodes, by_source: [user, official, generic, fallback] }` | `enumerate_postflop_nodes()` 的可達節點數，四項合計＝`total_nodes` | 策略頁、儲存前驗證 |
| `PostflopRuntimeCoverage { node_set_version, by_source, fallback_no_rule, fallback_masked }` | 英雄實際發生的翻後決策次數 | 報表、manifest |

- **現有 `CoverageStats` 一接四層規則就會虛報。** 四層規則塞進同一個 `RuleSet` 後，`Matched::Rule(i)` 不分層，`completeness_myriad()` 算出來的是「命中任何規則的比例」——工程 fallback 規則本身也是規則，比例會接近 100%，UI 規格 D.7「命中**玩家**規則的節點數」的定義直接被破壞。因此 `resolve()` 必須回傳命中規則的 `source`，`record()` 收 `source` 後分層累加。這是可以在 Stage 0 就修的既有缺陷。
- **分母要版本化。** 節點集合會隨階段擴充（facing size、SPR、位置、action history、`StackBucket` 逐項加入），同一份覆寫的完整度會在不同版本間暴跌。UI、報表與 manifest 都要帶 `node_set_version`，否則跨 run 比較沒有意義。
- **分母界定寫死**：只算英雄座位；只算英雄有實際選擇的節點（已全下、無合法選項不算）；棄牌後不再產生節點；翻牌前就結束的手不進翻後分母。
- **fallback 使用率的分子不合併**：`NoRuleMatched` 是策略缺口，`AllWeightsMasked` 是籌碼造成的合法性事件，混在一起會讓使用者以為策略沒寫完。現有型別已分開，DTO 不得合併。
- **逐桌型維度**：UI 規格 D.3（`UI面板詳細規格.md` 518–523 行）要求完整度逐桌型（9／8／7／6 人）顯示，並在 run 前警告「預計有 N% 手牌落在未校準桌型」；D.7 要求各切片分開統計。本期若只做翻前／翻後，必須在 Stage 0 的規格同步時明文記下這個差異與延後理由，不得默默省略。

作用中狀態使用整格背景色、文字加深或底線，不使用左側強調邊框。

---

## 六、IPC、型別與儲存

### 6.1 IPC

採無狀態純函式介面，預計新增／調整：

- `postflop_strategy_nodes(overrides)`：取得可達節點、條件選項、摘要與 `PostflopStaticCoverage`。
- `postflop_strategy_rule(query, overrides)`：純函式查詢單一節點的解析後頻率、繼承來源與來源層級。
- `classify_postflop_hand`：供 UI 預覽指定底牌與牌面的牌力組。
- `RunRequest.hero_postflop_overrides`：run 時隨請求傳入完整的稀疏覆寫清單。

覆寫 DTO 依 §5.2 拆成兩種，兩者一起放進 `hero_postflop_overrides`：

| DTO | 鍵 | 產生者 | 恢復語意 |
|---|---|---|---|
| `PostflopNodeOverrideView` | 完整 canonical node key | UI 主編輯器只產生這種 | 刪除該筆，只影響該節點，驗收條件 10 因此成立 |
| `PostflopRuleOverrideView` | `PostflopCondition` ＋ `PostflopLineCondition` | 進階規則編輯器 | 刪整條，刪除前顯示受影響節點數 |

解析時節點覆寫排在規則覆寫之前；兩者同屬 `UserOverride` 層，其內部順序一併納入 §5.3 的來源排序不變量測試。

UI 在本地新增、更新、刪除這兩份清單，查詢時整份送入；後端不新增 `set_*`／`clear_*` 可變狀態 command。所有 DTO 由 Rust 產生 TypeScript 型別，不在前端手工複製 schema。

**覆寫清單的本地保存**：§6.2 的 run 快照只解決「run 開始後不被改動」，沒有解決「App 重開後覆寫從哪裡來」，而 §5.2 的恢復操作正是以這份清單為基準。D.9 策略庫本期 out of scope，因此本期只做**單一 working set** 的最小持久化（不命名、不多份、不匯出），並在 UI 明示它不是策略庫存檔；若連這個都不做，必須在 UI 明白告知「重開後覆寫清空」，不得留白。

### 6.2 儲存與可重現性

- 每次 run 開始時，`RunRequest.hero_postflop_overrides` 以值傳入並凍結；UI 後續修改不影響進行中的 run。
- 預設嚴格遵守核心規格 3.3 與既有翻前語意：`RunManifest` 的 `ContentSnapshot.content` 保存官方 baseline 全部規則列與使用者覆寫列的 canonical 內容及 content hash，必須能在沒有外部版本表的情況下獨立重建當次策略；禁止只存 hash。
- 目前 `serde_json` 未啟用 `preserve_order`，`Value` object 使用 `BTreeMap`，key 順序天然穩定；在依賴／測試中鎖定此假設，若日後啟用 `preserve_order`，必須先提供等價 canonical 排序與 hash 相容測試。
- Stage 0 精算完整快照的節點數、序列化體積與讀寫成本，再決定自包含的 canonical／緊湊編碼；新增體積與 round-trip 測試，並與既有翻前 `< 80 KB` 閘門一併評估。
- 若完整內容確實無法落在核准預算，才能提出「baseline 版本指標＋稀疏覆寫」替代案；啟用前必須取得業主書面同意，定義舊版本支援期限，並為每個官方規則版本加入 content-hash pin 測試，任何內容漂移立即失敗。
- 同 seed、同版本、同策略快照必須產生相同結果。
- 只套用到使用者座位；Bot 對手仍使用各自的基準與人格參數。

完整的策略庫（命名存檔、複製、刪除、匯入／匯出）屬於獨立功能，本期明列為 **out of scope**；本期只定義 run 所需的稀疏覆寫 DTO 與可重現快照格式。

### 6.3 舊參數相容性

固定八組後，`postflopBucketCount`（舊範圍 2–24、目前 `implemented: false`）不再是有效的可調策略參數：

- 新 schema 從可編輯參數表移除。
- 舊存檔匯入時接受該欄但標記 deprecated 並忽略，避免破壞相容性。
- schema／UI 測試確認不再出現無作用滑桿。

---

## 七、實作階段

### 階段 0：地基與規模閘門

- 先將目前未完成的 report／generated type 工作提交完成或與本功能隔離；特別確認所有 generated view 都已加入 `packages/poker-types/src/index.ts`，避免 Stage 3 的型別 CI 混入既有差異。
- 更新 `UI面板詳細規格.md` D.5／D.7／D.8、F.4 與 `9max平台核心規格.md` 4.2，使規格和八組牌力、雙軸牌面、action history、有效籌碼、`RuleIssue` 嚴重度一致。
- 將 `PostflopCondition.board_texture` 拆成 surface／connectivity，完成引擎與 IPC DTO 遷移。
- 加入 context／condition 非對稱的 action history、跨街 `last_aggressor_before_current_street`、`RelativeAggressorOrder::NotApplicable`、既有 `StackBucket` 與無狀態 override DTO，重寫並測試條件集合運算及攻守線路映射。
- 實作 `enumerate_postflop_nodes()`、reachability 過濾、節點數、完整快照體積、`RuleSet::analyse()` O(n²) 與 `Unreachable` O(規則數 × 節點數) 成本報表；若成本過高，採同層索引、條件反向列舉或分組分析。
- 以完整自包含 `ContentSnapshot` 為預設輸出快照格式；版本指標替代案依 §6.2 的核准條件處理。
- 凍結 `PostflopActionKind → Action` 的完整管線、來源排序，以及使用者絕對覆寫不受 persona／behavior／cap／noise 修改的不變量。
- 依 §5.2 鎖定恢復按鈕方案（A 或 B），並確定覆寫 DTO 拆成節點覆寫與規則覆寫。
- 讓 `resolve()` 回傳命中規則的 `source`、`CoverageStats` 依來源分層，並定義 `node_set_version` 的產生與比較規則。

### 階段 1：獨立牌力分類器與效能

- 先實作 `board_partial_rank()` 與 §3.2 的 `improves_board` golden 向量；門檻校準建立在它之上，順序不可對調。
- 替換 `HandStrength` 八組定義、§2.3 判定樹與只供顯示排序的 `priority()`。
- 先產生 §2.1 所有例子的「隨機合法對手百分位、raw outs、effective outs、預期牌力組」對照表（標題註明對手模型），再校準門檻並建立 golden vectors。
- 實作成牌強度、合法對手底牌窮舉百分位、有效補牌、污染補牌白名單與複合聽牌判定。
- 補齊翻牌、轉牌、河牌測試向量、河牌不可達組別、公共牌即成手的退化案例與邊界案例。
- 建立分類 benchmark；在符合延遲預算後才允許接入產品決策。

### 階段 2：規則與 Bot 執行整合

- 建立尺寸意圖分布與未簽核工程預設規則。
- 將 `RuleSet::resolve()` 接入 Bot 決策，完成 intent 換算、尺寸合併、合法遮罩及正規化。
- 依規則來源接線：使用者覆寫仍跑完整七階段 pipeline，但 persona／behavior／cap／noise 全部中和為恆等變換；官方／通則／fallback 使用實際參數。
- 保留 `equityTexture/v2-unapproved` 與 `checkFold/v0` fallback。
- 增加 `exploitAdjustmentCapPp=1_000`、非中性 persona、非零 noise 與縮減 `allowedBetSizes` 的測試，證明使用者覆寫不被默默改寫。
- 驗證相同公開狀態與策略輸入的決策分布可重現。

### 階段 3：IPC、型別與策略 UI

- 建立無狀態查詢、分類預覽與 `RunRequest.hero_postflop_overrides`；覆寫 DTO 依 §6.1 拆成節點覆寫與規則覆寫。
- 產生並匯出 TypeScript bindings；增加 schema／輸入驗證。
- 新增牌力組、雙軸牌面及依下注狀態過濾的線路導覽，將「可用」換成百分比編輯器。
- 加入合計驗證、來源層級顯示、未簽核提示與單手分類預覽。
- 實作 §5.2 的恢復語意（含按鈕停用條件、批次恢復、undo）與覆寫 working set 的最小持久化或「重開清空」提示。

### 階段 4：規則診斷與完整度

- 依 §5.3 新增／調整 `RuleIssue`、嚴重度、同層 overlap／shadowed 與節點列舉式 unreachable 分析，接到 UI 保存驗證。
- 加入來源排序不變量與跨層覆寫不產生噪音警告的測試。
- 依 §5.3 實作 `PostflopStaticCoverage`（策略頁）與 `PostflopRuntimeCoverage`（報表），兩者依來源分層並帶 `node_set_version`；`resolve()` 回傳來源、`CoverageStats` 分層累加。
- 加入不可達節點不顯示、error 阻擋保存、warning 留在摘要，以及「fallback 規則不得計入玩家完整度」的測試。

### 階段 5：無狀態快照與 trace

- run 以值凍結稀疏覆寫，不建立後端可變策略狀態或鎖。
- manifest 預設保存完整、可獨立重建的翻後 `ContentSnapshot.content` 及 hash。
- 決策 trace 記錄牌力分組、命中規則、fallback、尺寸換算、遮罩及最終頻率。
- 加入快照還原與體積上限測試。

### 階段 6：驗證與發布

- 引擎單元測試、整合測試、不變量測試與效能 benchmark。
- IPC 序列化及 generated-types 一致性。
- UI production build。
- Windows Tauri compile／installer CI。
- 顧問門檻尚未簽核前，所有畫面與 manifest 保持 `consultantApproved: false`。

---

## 八、主要修改檔案

| 範圍 | 預計檔案 |
|---|---|
| 核心／UI 規格同步 | `9max平台核心規格.md`、`UI面板詳細規格.md` |
| 牌力分類與規則 | `apps/engine/src/strategy/postflop.rs`、`apps/engine/src/eval.rs`（`board_partial_rank()`） |
| Bot 決策接線 | `apps/engine/src/bot/agent.rs`、`apps/engine/src/bot/pipeline.rs` |
| 規則與分類測試 | `apps/engine/tests/postflop_rules.rs`、`apps/engine/tests/bot_agent.rs` |
| 節點／體積計算 | `apps/engine/examples/content_size.rs` 或新增同級 `postflop_content_size.rs` |
| 舊參數相容 | `apps/engine/src/bot/params.rs`、對應 IPC schema、`apps/ui/src/panels/BotParams.tsx` |
| IPC DTO／command | `apps/ipc/src/strategy.rs`、`apps/desktop/src/main.rs`、`apps/devserver/src/main.rs` |
| 前端 API／策略頁 | `apps/ui/src/api.ts`、`apps/ui/src/panels/Strategy.tsx`、`apps/ui/src/panels/StrategyNav.tsx` |
| 產生型別 | `packages/poker-types/src/generated/`、`packages/poker-types/src/index.ts` |
| 快照與執行 | `apps/ipc/src/run.rs`、`apps/ipc/src/snapshot.rs` |
| 完整度報表 | 現有 report DTO／IPC／UI 報表路徑（先與目前未完成工作協調）；新增 `PostflopStaticCoverage`／`PostflopRuntimeCoverage` 兩個型別 |

---

## 九、驗收條件

1. 翻牌與轉牌可選八個牌力組；河牌只顯示五個仍成立的牌力組。
2. 每個節點顯示實際動作百分比，不再只顯示「可用」。
3. 無人下注的跟注／蓋牌，以及面對下注的過牌，固定顯示不成立且不可編輯。
4. 每個節點的合法動作頻率精確合計 100%；尺寸意圖合併後不會產生重複或非法 `Action`。
5. 同一底牌、牌面與公開狀態的牌力分類完全確定；百分位以合法對手組合窮舉、對手模型為均勻隨機且對單一對手計算，`improves_board` 以 `board_partial_rank()` 比較且踢腳不算改善，高張補牌按 0.50 折算，不使用 Monte Carlo。
6. 彩虹乾燥與彩虹濕潤可設定不同策略，且條件集合運算正確。
7. 列舉器不輸出不可能到達的牌面、街別、牌力、下注狀態與線路組合。
8. c-bet、delayed c-bet、donk、stab／float、probe 及其面對下注的防守情境可分開設定；沒有共用「其他」桶，probe 與 delayed c-bet 不會撞在同一節點。
9. 既有 `StackBucket` 與 SPR 會參與規則匹配及實際尺寸合法化，且 context／condition 模型位置正確。
10. 節點覆寫只影響該一個節點；規則覆寫在刪除前顯示受影響節點數。兩者都不滲透到其他街別、牌面、牌力組或牌局線路。
11. 使用者翻後覆寫是絕對頻率，不受 persona、`allowedBetSizes`、exploit cap 或 noise 默默改寫；完整七階段 trace 仍存在，只有可追蹤的牌局合法化可以使最終頻率不同。
12. 規則未命中或合法動作被遮罩時可安全 fallback，不會產生非法動作。
13. UI 依 §5.3 顯示同層 overlap／shadowed、unreachable、靜態 impossible 與 layer-order violation；Shadowed／Unreachable 是 warning，只有明定 error 阻擋保存，執行期全遮罩仍正常 fallback。
14. 策略頁顯示 `PostflopStaticCoverage`（分母為可達節點數、依來源分層），報表顯示 `PostflopRuntimeCoverage`（分母為英雄決策次數、依來源分層並分開兩種 fallback 原因）；兩者都帶 `node_set_version`，且翻前／翻後分開。工程 fallback 規則的命中不得計入玩家完整度。
15. 每次 run 預設可只靠 manifest 內的完整翻後內容與 hash 還原策略；進行中不受 UI 修改影響。
16. 翻後完整快照通過 Stage 0 凍結的序列化體積與 round-trip 上限；若改採版本指標，已有業主書面同意及逐版本 hash pin 測試。
17. `postflopBucketCount` 不再以無作用滑桿出現在新 schema，舊存檔仍可相容匯入。
18. 未簽核工程內容不得顯示為 GTO 或正式顧問策略。
19. Linux 全套測試、Clippy、benchmark、前端 build 與 Windows Tauri CI 全部通過。
20. 「成牌」一律為 `made_category >= Pair` 且 `improves_board`；河牌 `improves_board == false` 的手牌不得高於 `bluffCatcher`，公共牌即成手的退化牌面有測試鎖定。
21. 「恢復繼承值」只在值來自使用者節點覆寫時就地清除；來自規則覆寫時導向規則編輯，來自官方／通則／fallback 時停用。任何恢復操作都不得把 fallback 或通則的值寫成使用者覆寫。

---

## 十、已鎖定工程決議與待確認事項

以下工程決議在 Stage 0 鎖定，不再留到實作中途：

1. 使用無狀態稀疏覆寫，不建立後端 `set_*`／`clear_*` 狀態。
2. 節點鍵納入前街主動方、跨街最近主動方（不含本街）、三態相對行動順序、英雄本街是否已過牌、當前 bet／raise 次數與既有 `StackBucket`。
3. 牌面條件拆成 surface／connectivity；UI 只列可達組合。
4. 規則保存尺寸意圖與 `Myriad`，決策當下才換算成具體 `Action`；使用者覆寫完整保留七階段 trace，但所有非合法性調整均中和為恆等變換。
5. 牌力百分位採合法對手底牌窮舉，分類器不得使用 Monte Carlo。
6. 策略庫另案處理；本期只完成稀疏覆寫、run 快照及還原。
7. manifest 預設保存完整、自包含的翻後內容快照與 hash；版本指標不是預設方案。
8. `madePlusDraw` 不設百分位下限；底對＋強聽牌優先於 `bluffCatcher`。
9. 規則診斷採明確 `RuleIssue`／嚴重度，Shadowed 依 UI 規格列為 warning，來源排序違反才是 error。
10. 分類採 §2.3 判定樹，`priority()` 只供顯示排序；「成牌」定義為 `made_category >= Pair` 且 `improves_board`。
11. `improves_board` 以 `board_partial_rank()` 計算，踢腳不算改善；河牌 `improves_board == false` 強制不高於 `bluffCatcher`。
12. 百分位的對手模型固定為均勻隨機合法組合，且為對單一對手的序位；多人由規則維度處理，死牌只扣英雄底牌與已公開公共牌。
13. 覆寫 DTO 拆成節點覆寫與規則覆寫，恢復語意依來源層級決定。
14. 完整度拆成靜態節點覆蓋與執行期決策覆蓋，兩者依來源分層並帶 `node_set_version`；`resolve()` 回傳命中規則的 `source`。

以下項目不阻擋工程骨架，但正式策略內容上線前需由業主／牌手顧問確認：

1. 八個牌力組名稱是否符合業主用語。
2. 依 Stage 1 範例牌型實測對照表，確認堅果、強成牌與中等成牌的最終百分位門檻。
3. `madePlusDraw` 與一般強聽牌是否固定採至少 8.00 張有效補牌或複合聽牌，以及高張補牌 0.50 的折算係數。
4. 依 `showdown_value_floor_myriad`（工程起點 `3_500`）與高張補牌折算結果，確認 A 高／K 高分入抓詐、弱聽牌或空氣牌的三分規則。
5. 面對下注第一版是否只提供 1/3、2/3、1 個底池三檔；完整 `FacingSize` 仍保留在引擎條件層。
6. 第一批正式頻率由牌手顧問提供，或先沿用清楚標記為未簽核的工程基準。
7. Stage 0 節點精算完成後，確認 UI 可接受的節點規模、完整快照編碼與體積上限；若要改採版本指標，另取得書面同意。
8. 恢復按鈕採方案 A（只留「恢復繼承值」）或方案 B（分成使用者節點層與使用者規則層），見 §5.2。
9. 污染補牌採白名單式或窮舉式，以及 `polluted_out_weight_centi`／`outs_pollution_floor_myriad` 的值。
10. 逐桌型（9／8／7／6 人）完整度是否納入本期，或延後並在規格明文記下差異。
11. 覆寫 working set 是否做本地最小持久化，或在 UI 明示「重開後清空」。
