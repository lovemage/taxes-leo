// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * 策略內容的來源與現況（UI 規格 D.2、D.7、D.8）。
 */
export type StrategyMetaView = { baselineName: string, baselineVersion: string, 
/**
 * 是否已由牌手顧問簽核。false 代表這是工程佔位內容，
 * 不得當成校準過的結果解讀
 */
consultantApproved: boolean, 
/**
 * 低於此 bucket 一律推入或棄牌
 */
pushFoldBelow: string, 
/**
 * 各情境的加注尺度，以 BB 的百分之一表示（250 = 2.5BB）
 */
openSizeCentiBb: number, threeBetSizeCentiBb: number, fourBetSizeCentiBb: number, 
/**
 * 翻後 fallback 的版本字串（核心規格 4.2 要求明示）
 */
postflopFallback: string, 
/**
 * 翻前節點總數（桌型 × 位置 × bucket × 情境）
 */
preflopNodeCount: number, 
/**
 * 翻前總格數 = 節點數 × 169
 */
preflopCellCount: number, 
/**
 * equity 排序的取樣數。面板與執行層共用同一份，兩邊看到的必須一致
 */
rankingSamples: number, 
/**
 * 排序來源：`asset/v1`（離線資產）／`debugFallback`／`unavailable`
 */
rankingSource: string, 
/**
 * 排序的取樣數是否足以產製正式內容。
 *
 * false 代表面板上畫的範圍**不是正式內容**（debug 低樣本替代品，
 * 或內容根本沒載進來）。UI 必須明講——低樣本排序看起來與正式的
 * 一模一樣，使用者沒有任何辦法自己分辨
 */
rankingContentGrade: boolean, 
/**
 * 給使用者看的一句話說明排序來源
 */
rankingNote: string, 
/**
 * 預設組合表的來源檔名（顧問給的那份 Excel）
 */
chartSource: string, 
/**
 * 預設組合表的內容版本
 */
chartVersion: string, 
/**
 * 表是否成功載入。false 時 `chart_note` 帶著失敗原因
 */
chartLoaded: boolean, 
/**
 * 表上的格數（深度 × 位置 × 情境）
 */
chartCellCount: number, 
/**
 * 引擎的翻前節點之中，由表提供內容的比例（萬分比）
 */
chartCoverageMyriad: number, 
/**
 * 給使用者看的一句話說明：表涵蓋什麼、沒涵蓋什麼
 */
chartNote: string, };
