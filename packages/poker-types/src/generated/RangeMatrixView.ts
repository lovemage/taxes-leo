// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ChartRowView } from "./ChartRowView";
import type { MatrixCellView } from "./MatrixCellView";

/**
 * 一個節點的完整範圍矩陣。
 */
export type RangeMatrixView = { 
/**
 * 節點的唯一鍵（`9max/BTN/160-240/unopened`）
 */
nodeKey: string, seated: number, hero: string, bucket: string, scenario: string, scenarioLabel: string, 
/**
 * 169 格，依 `HandClass::index()` 遞增（列由 A 到 2）
 */
cells: Array<MatrixCellView>, 
/**
 * 範圍寬度：以 combo 加權的主動頻率（萬分比）
 */
widthMyriad: number, 
/**
 * 混合格數（同一手牌有兩種以上行動）
 */
mixedCount: number, 
/**
 * 本節點被覆寫的格數
 */
overrideCount: number, 
/**
 * 本節點的主動行動描述（推入或加注到多少 BB）
 */
aggressiveAction: string, 
/**
 * 產生排序時假設的對手數（見 `baseline::expected_opponents`）
 */
expectedOpponents: number, 
/**
 * 這個節點的內容來源：`chart`（顧問的預設組合表）或 `baseline`（參數產生器）
 */
source: string, 
/**
 * 本節點用的是表上哪一檔深度。引擎有九檔 bucket、表只有四檔，
 * 對應關係必須說出來，否則使用者不知道 40–70BB 看的是 35–50BB 那一欄
 */
chartDepth: string | null, 
/**
 * 本節點用的是表上哪一欄情境
 */
chartScenario: string | null, 
/**
 * 表上這一格的五列原文。`source` 為 `baseline` 時是空的
 */
chartRows: Array<ChartRowView>, };
