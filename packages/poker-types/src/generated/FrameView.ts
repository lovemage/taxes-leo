// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { StreetView } from "./StreetView";

/**
 * 重播動畫的一幀（UI 規格 G.4）。
 *
 * 每一幀是「牌桌在某個時間點的完整狀態」，不是「相對前一幀的變化」。
 * 前端因此可以直接跳到任意幀，不必從頭累加——逐步、拖曳與倒帶都成立。
 *
 * 金額全部由引擎算好。G.4 明訂底池與各座籌碼不得由 UI 重算，
 * 因為那等於把下注規則複製一份到前端。
 */
export type FrameView = { 
/**
 * ante／smallBlind／bigBlind／straddle／deal／fold／check／call／raiseTo／allIn／settle
 */
kind: string, street: StreetView, 
/**
 * 這一幀由誰行動。發牌與收池為 null
 */
seat: number | null, 
/**
 * 金額。`raiseTo` 為加注到的本街累計額，`call`／`allIn` 為跟到的額度，
 * 強制下注為該筆金額，其餘為 null
 */
to: number | null, 
/**
 * **這一幀已發出**的公共牌。未發的牌不在此，UI 拿不到就畫不出來
 */
board: Array<string>, pot: number, 
/**
 * 各座本手累計投入
 */
committed: number[], 
/**
 * 各座剩餘籌碼
 */
stacks: number[], folded: Array<boolean>, };
