# 德州撲克策略模擬器 — UI/UX 設計規格與線框說明

> 版本 v0.1（草案） · 目標：極簡控制台外觀、但承載大量資料的策略模擬工具
> 遊戲：德州撲克 Texas Hold'em（6-max 起，可擴充 2/9 人桌）

---

## 1. 產品定位與核心體驗

一句話：**在一個「極簡控制台」的殼裡，跑一台能即時視覺化的德州撲克策略沙盒。**

- 使用者是「策略研究者 / 玩家」：他要快速組桌、放 BOT、設定策略、跑模擬、看結果。
- 介面要「安靜但資訊密集」：預設乾淨，資料以表格、數字、熱力圖形式密集呈現，而不是華麗裝飾。
- 牌桌是舞台中心：打牌過程要有清楚的動畫與節奏，速度可調（0.8x–3x）。

三個關鍵動線：
1. **組桌**：＋新增 BOT → BOT 進左側導航 → 點 BOT 設定策略。
2. **跑模擬**：Header 按 Play → 打牌動畫開始 → 左側位置隨每局結束自動輪轉。
3. **看數據**：牌桌畫面即時更新；BOT 列表顯示當前位置、籌碼、勝率等 KPI。

---

## 2. 設計原則與視覺語言

### 2.1 設計原則
| 原則 | 說明 |
| --- | --- |
| 極簡控制台 | 深色底、低飽和、細邊框、克制的圓角（2–6px），像終端機/監控儀表板 |
| 資料優先 | 數字用等寬字型對齊；決策與狀態用顏色編碼而非只靠文字 |
| 一張主舞台 | 只有一個中心焦點（牌桌），其餘為側欄與工具列 |
| 即時回饋 | 下注、發牌、輪轉位置都有輕量動畫，但不過度（可關閉） |
| 密度可調 | 提供「精簡 / 標準 / 完整」三種資訊密度，預設「標準」 |

### 2.2 色彩系統（深色）
| 用途 | 色值 | 說明 |
| --- | --- | --- |
| 背景 base | `#0e1116` | 全站背景 |
| 面板 surface | `#151a21` | 導航、卡片、面板 |
| 邊框 border | `#2a3138` | 分隔線、欄位框 |
| 牌桌絨布 felt | `#1d4a3c` → `#16392f`（漸層） | 牌桌本體 |
| 主互動 accent | `#3b82f6`（藍）或 `#22c55e`（綠） | 按鈕、選中、焦點 |
| 強調/籌碼 | `#f5c542`（金） | 底池、按鈕位 BTN |
| 危險/加注 | `#ef4444`（紅） | raise、錯誤、警示 |
| 成功/跟注 | `#22c55e`（綠） | call、check |
| 中性/棄牌 | `#6b7280`（灰） | fold |
| 文字 primary | `#e6edf3` | 主文字 |
| 文字 secondary | `#8b949e` | 次要文字、標籤 |

### 2.3 字型
- **資料 / 數字 / 籌碼**：等寬 `JetBrains Mono` / `Consolas`（確保表格對齊）。
- **標籤 / 標題**：`Inter` / 系統 sans-serif。
- 牌面花色：`♥ ♦` 紅 `#ef4444`，`♠ ♣` 白/綠。

### 2.4 版式
- 基準字級 13px；大數字（底池、籌碼）18–24px；面板標題 14px 全大寫 + 字距。
- 間距 8px 網格；面板內距 12–16px。

---

## 3. 整體版面（App Shell）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ HEADER  (48px 高)                                                            │
│  ▶ Play │ ──速度拉桿 0.8x ──3x── │ 手數 #128 │ 底池 $240 │ 速率 42h/s │ ⋯    │
├──────────┬───────────────────────────────────────────────────────────────────┤
│          │                                                                   │
│  左側     │                                                                   │
│  導航     │                        MAIN CONTENT (主內容區)                    │
│  (240px) │          ┌─────────────────────────────────────────────┐          │
│          │          │                                              │          │
│  ① 領域視覺│          │          領域視覺牌桌 / 本機策略 / BOT 策略   │          │
│  牌桌     │          │          （三者共用同一主內容區，切換顯示）     │          │
│  ② 本機策略│          │                                              │          │
│          │          └─────────────────────────────────────────────┘          │
│  ──────  │                                                                   │
│  BOT 列表 │                                                                   │
│  [BTN] BOT-2 │                                                                │
│  [SB]  BOT-3 │                                                                │
│  [BB]  BOT-4 │                                                                │
│  ...       │                                                                  │
│  ＋新增 BOT │                                                                  │
│          │                                                                   │
├──────────┴───────────────────────────────────────────────────────────────────┤
│ STATUS BAR (24px)  · 連線/引擎狀態 · 模擬進行中/暫停 · 記憶體/手數 ·           │
└──────────────────────────────────────────────────────────────────────────────┘
```

- **Header**：全域控制（Play、速度、目前手數/底池/速率快覽）。
- **左側導航（固定，可收合到 48px 只留圖示）**：導航區 + BOT 列表區 + 新增按鈕。
- **主內容區**：牌桌、本機策略、BOT 策略三個視圖共用，靠左側導航切換。
- **狀態列**：模擬引擎狀態、手數、速率等極簡狀態。

---

## 4. 左側導航欄（Left Navigation）

### 4.1 結構（由上而下）
```
┌────────────────┐
│ ① 領域視覺牌桌  │   ← 導航項（圖示 + 文字），預設選中
│ ② 本機策略      │   ← 導航項
│ ────────────── │   ← 分隔線（BOT 區）
│ BOT 列表（標題） │
│ [BTN] BOT-2   ●│   ← 每個 BOT 一列：位置徽章 + 名稱 + 籌碼 + 勝率
│       $980     │
│ [SB]  BOT-3    │
│       $1,200   │
│ [BB]  BOT-4    │
│       $760     │
│ [UTG] BOT-5    │
│       $1,450   │
│ ────────────── │
│  ＋ 新增 BOT    │   ← 常駐底部（或緊接最後一個 BOT）
└────────────────┘
```

### 4.2 導航項
- ① **領域視覺牌桌**：主視圖，呈現整張牌桌與打牌動畫（詳見 §5）。
- ② **本機策略**：設定「你自己」的策略（詳見 §6）。
- 兩者為固定項；選中態：左側 2px accent 高亮 + 底色微亮。

### 4.3 BOT 列表（關鍵：位置徽章會自動輪轉）
每個 BOT 條目顯示四樣資訊（等寬對齊）：

| 欄位 | 內容 | 說明 |
| --- | --- | --- |
| 位置徽章 | `BTN / SB / BB / UTG / HJ / CO` | **每局結束後自動更換**（順時針輪轉），即「左側顯示目前位置」 |
| 名稱 | `BOT-2` | 可自訂名稱 |
| 籌碼 | `$980` | 即時籌碼堆 |
| 勝率/EV | `+12.4 bb/100`（綠/紅） | 大量數據之一，可選顯示 |

- **點擊 BOT 條目** → 主內容區切換到「BOT 策略編輯器」，可設該 BOT 的詳細策略（§7）。
- BOT 條目右側可放「狀態點」：在牌局中、已棄牌、All-in、已淘汰。
- **位置徽章是重要互動點**：當局結束時，徽章會做一次「輪轉」動畫（淡出→換字→淡入），讓使用者直覺看到位置移動。

### 4.4 ＋新增 BOT
- 位置：BOT 列表底部（常駐）。
- 點擊後彈出「新增 BOT」對話框：名稱、初始籌碼、策略預設（TAG/LAG/NIT/Loose-Passive/自訂）、座位。
- 確認後：BOT 出現在左側列表，並在牌桌上佔一個座位。

---

## 5. 主視圖 A — 領域視覺牌桌（Table View）

### 5.1 畫面組成
中心是橢圓牌桌，四周環繞座位，中央是公共牌與底池，下方是行動紀錄。

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 牌桌工具列： [桌型 6-max ▾] [盲注 1/2 ▾] [重置籌碼] [資訊密度 ▾]               │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                        [UTG]                ← 座位：位置標 + 名稱 + 籌碼 + 下注 │
│                       (BOT-5)                                                │
│                       $1,450                                                 │
│               [HJ]                    [CO]                                   │
│              (BOT-2)                 (BOT-4)                                 │
│               $980                   $760                                   │
│                                                                              │
│              ╔═════════════════════════════════╗                              │
│              ║      🂠 🂠 🂠   🂠   🂠            ║   ← 公共牌（翻牌3/轉牌1/河牌1）│
│              ║       底池 Pot: $240             ║                              │
│              ║     [ 下注進度/當前行動 ]          ║                              │
│              ╚═════════════════════════════════╝                              │
│                                                                              │
│               [BTN]                    [SB]                                   │
│              (Hero/你)                (BOT-1)                                 │
│              $2,000  🔵 下注 $60      $1,100                                  │
│                        [BB]                                                   │
│                       (BOT-3)                                                │
│                        $1,200                                                 │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 行動紀錄（Action Log）： UTG fold · HJ raise $6 · CO call · BTN 3bet $24 · …  │
│ 底池/勝率快覽： Hero equity 62% │ BOT-1 21% │ BOT-3 17%                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 座位（Seat）
每個座位顯示：
- **位置標籤**（`BTN/SB/BB/UTG/HJ/CO`），BTN 用金色高亮。
- **玩家名稱**（Hero 標記為「你」）。
- **籌碼堆**（數字 + 可視化的籌碼堆小圖）。
- **本局下注**（當前已投入，綠色）與 **動作狀態**（fold 灰、check 綠、bet/raise 紅、all-in 金）。
- **手牌**：Hero 亮牌顯示；BOT 通常蓋牌（可開啟「上帝視角」看全部手牌，供策略分析）。
- **思考中指示**：BOT 行動前有短暫「…」或進度條（受速度拉桿影響）。

### 5.3 公共牌與底池
- 公共牌分階段翻開：翻牌（3 張）→ 轉牌（1 張）→ 河牌（1 張），每階段有發牌動畫。
- 底池顯示總額；多人時可 hover 顯示主池/邊池。

### 5.4 打牌動畫規格（配合速度拉桿）
| 事件 | 動畫 | 時長（1x 基準） |
| --- | --- | --- |
| 發手牌 | 牌從中央扇形飛至各座位 | 0.3s |
| 翻牌/轉牌/河牌 | 牌背翻開、依次亮相 | 0.25s/張 |
| 下注 | 籌碼從座位滑入底池，數字跳動 | 0.2s |
| 棄牌 | 手牌飛入棄牌堆並半透明 | 0.15s |
| All-in | 整疊籌碼滑入 + 金框閃一下 | 0.4s |
| 攤牌 | 手牌翻開、勝方高亮 + 籌碼歸位 | 0.6s |
| 位置輪轉 | BTN 徽章順時針移到下一座位 | 0.3s |

- 速度拉桿 **0.8x–3x** 為全域倍率，直接乘上以上基準時長。
- 提供「瞬間完成（跳到結果）」快轉按鈕（跑大量手數時用）。

### 5.5 大量資料呈現（此視圖可疊加的面板）
牌桌預設乾淨；需要資料時從工具列開啟疊加層：
- **即時勝率（Equity）條**：每位玩家手牌範圍 vs 公共牌，以百分比條顯示。
- **籌碼走勢圖**：下方時間軸，每局每位玩家的籌碼曲線。
- **行動統計**：本桌各 BOT 的 VPIP / PFR / 3-bet / c-bet 迷你表格。

---

## 6. 主視圖 B — 本機策略（Local Strategy）

「你」的策略設定面板，採「參數化 + 範圍表」雙模式，符合「大量數據的工具」定位。

### 6.1 版面
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 本機策略（Hero）                                      [載入預設 ▾] [儲存]     │
├──────────────────────────────────────────────────────────────────────────────┤
│  模式： (●) 參數化   ( ) 範圍表（Range Grid）  ( ) 規則引擎                     │
│                                                                              │
│  ┌─ 核心參數 ──────────────────────────────┐  ┌─ 位置別調整 ─────────────────┐ │
│  │ VPIP       ████████░░ 24%              │  │ 位置   開牌範圍   3bet%       │ │
│  │ PFR        ███████░░░ 18%              │  │ UTG   ████ 10%  ███ 6%      │ │
│  │ 3-bet      ███░░░░░░░  8%              │  │ HJ    █████ 14% ████ 8%     │ │
│  │ 侵略性(AF) ██████░░░░ 2.5              │  │ CO    ██████ 18% ████ 10%   │ │
│  │ C-bet 頻率 ███████░░░ 65%              │  │ BTN   ████████ 26% █████ 12%│ │
│  │ 加注尺寸   [2.5bb ▾]  [3bb ▾]           │  │ SB    ...     ...           │ │
│  └────────────────────────────────────────┘  │ BB    ...     ...           │ │
│                                              └─────────────────────────────┘ │
│  ┌─ 翻後傾向 ────────────────────────────────┐  ┌─ 攤牌傾向 ────────────────┐ │
│  │ 過牌率 / 領先下注率 / 加注率（各街）        │  │ 攤牌率 WTSD / 勝率 W$SD    │ │
│  └──────────────────────────────────────────┘  └───────────────────────────┘ │
│                                                                              │
│  預覽：此策略預估 VPIP 24 / PFR 18 / AF 2.5，定位為「LAG 略鬆侵略」             │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 模式說明
- **參數化（預設）**：滑桿 + 數字輸入，適合快速調整。
- **範圍表 Range Grid**：13×13 起手牌矩陣，點格子切換「開牌/跟注/棄牌/混合」顏色（見 §7.3）。
- **規則引擎**：給進階使用者的 if-then 規則（如「BB vs BTN open → 3bet 範圍 X」）。

---

## 7. BOT 策略編輯器（點左側 BOT 進入）

與本機策略共用同一編輯器元件，但標題與對象是該 BOT，並多一欄「座位/初始籌碼/淘汰處理」。

### 7.1 版面（右側抽屜 vs 主內容區）
- 建議 **主內容區切換**（保持左側 BOT 列表可同時看到多個 BOT）。
- 頂部：BOT 名稱（可改）、策略預設下拉、狀態（在桌/已淘汰）。
- 主體：與 §6 相同的三模式編輯器。
- 底部：`套用` / `複製到其他 BOT` / `重設`。

### 7.2 策略預設（快速起步）
| 預設 | VPIP | PFR | 3-bet | 風格 |
| --- | --- | --- | --- | --- |
| NIT（岩石） | 12 | 8 | 4 | 極緊 |
| TAG（緊兇） | 18 | 14 | 7 | 標準 |
| LAG（鬆兇） | 26 | 21 | 10 | 侵略 |
| Loose-Passive（鬆被動） | 34 | 12 | 3 | 跟注站 |
| Maniac（狂人） | 45 | 38 | 20 | 極激進 |

### 7.3 範圍表（Range Grid）規格
```
13×13 矩陣，X/Y 軸為 A..2，對角線為對子、右上為同花、左下為非同花。

        A   K   Q   J   T   9   8   7   6   5   4   3   2
    A  [AA][AKs][AQs]...[A2s]
    K  [AKo][KK][KQs]...
    Q  [AQo][KQo][QQ]...
    ...
    2
```

- 格色編碼：`綠=加注 raise`、`黃=混合 mixed`、`藍=跟注 call`、`灰=棄牌 fold`、`空白=未設定`。
- 點擊循環切換、右鍵選混合比例；可拖曳框選區塊。
- 每格 hover 顯示該起手牌的勝率（對隨機牌）。

---

## 8. Header 控制區

```
┌─────────────────────────────────────────────────────────────────────┐
│  ▶ Play   │  ⏸   │  速度 ═══●══════ 1.4x   │ #128 │ Pot $240 │ 42 h/s │
└─────────────────────────────────────────────────────────────────────┘
```

| 元件 | 行為 |
| --- | --- |
| **Play 按鈕** | 主 CTA。點擊 → 開始模擬；進行中變 `⏸ Pause`；再點暫停。長按或右鍵 → `Stop` 結束本局 |
| **速度拉桿** | 0.8x–3x，連續拖曳；數值即時顯示；鍵盤 `[` `]` 可微調 |
| **手數 #** | 目前局數 |
| **底池 Pot** | 目前底池 |
| **速率 h/s** | 手/秒（跑大量模擬時顯示，受速度拉桿影響） |
| **⋯ 選單** | 大量模擬（如「跑 10,000 手」）、上帝視角開關、動畫開關、設定 |

**Play 後的流程**：
1. Play 按下 → 狀態列顯示「模擬進行中」，按鈕變 Pause。
2. 牌桌開始發牌、下注、逐街推進（受速度拉桿控制）。
3. 當局結束 → 攤牌/結算 → **左側 BOT 位置徽章輪轉** + BTN 移到下一位 → 自動開下一局。
4. 直到暫停/停止/某 BOT 籌碼歸零。

---

## 9. 互動流程（User Flows）

### Flow 1 — 首次組桌
```
啟動 → 空桌(或預設 6-max) → 點「＋新增 BOT」→ 命名+選預設+初始籌碼
     → BOT 進左側列表並入座 → (重複) → 點本機策略設定自己 → 點 Play 開始
```

### Flow 2 — 調整 BOT 策略
```
點左側 BOT 條目 → 主內容切到 BOT 策略編輯器 → 調參數/範圍表 → 套用
→ 回到牌桌視圖（或 Play 直接跑新策略）
```

### Flow 3 — 跑模擬與觀察位置輪轉
```
Play → 看打牌動畫 → 局結束 → 左側位置徽章輪轉 + 底池/籌碼更新
     → 自動下一局 → 調整速度拉桿觀察快慢 → Pause/Stop
```

---

## 10. 狀態與資料模型（供後續實作參考）

### 10.1 全域狀態
```
Session {
  tableSize, blinds, players[], buttonSeat, handCount,
  isRunning, speed(0.8..3), godView, animationEnabled
}
Player {
  id, name, isHero, seatIndex, stack, hand[], status,
  stats{ vpip, pfr, threeBet, af, cBet, wtsd, winRate, evBB100 },
  strategy{ ... }
}
Hand {
  handId, board[], pot, sidePots[], actionLog[],
  bets{ playerId, amount }, winnerId, amountWon
}
```

### 10.2 大量資料出口
- **BOT KPI 表**（可匯出 CSV）：VPIP/PFR/3-bet/C-bet/WTSD/W$SD/勝率/EV。
- **範圍熱力圖**：每位 BOT 實際行動範圍 vs 設定範圍的偏差。
- **籌碼曲線 / EV 曲線**：跨手數的走勢圖。
- **對抗矩陣**：每對 BOT 的單挑勝率（n×n）。

---

## 11. 響應式與密度

- 桌面優先；窄螢幕（<1024px）時左側導航自動收合成 48px 圖示列（hover 展開）。
- 資訊密度三檔：
  - **精簡**：只顯示牌桌 + 底池 + 位置徽章。
  - **標準**：加籌碼、下注、行動紀錄、BOT KPI。
  - **完整**：加勝率條、範圍熱力圖、籌碼曲線、對抗矩陣。

---

## 12. 後續待確認（Open Questions）

1. 座位數預設 6-max 是否 OK？要不要支援 2 人（單挑 HU）與 9 人滿桌？
2. 策略模型複雜度：先做「參數化 + 範圍表」是否足夠，還是初期就要「規則引擎」？
3. 「大量模擬」是否為核心需求（例如一次跑 1 萬手看統計），還是先聚焦單桌可視化？
4. 上帝視角（可看 BOT 手牌）預設開或關？
5. 是否需要「錄製/重播」某手牌，方便回放檢討？
6. BOT 名稱是否要支援中文/自訂頭像？

---

*本文件為 v0.1 草案，供 UI/UX 對齊用；確認後可再細化為元件規格與實作計畫。*
