# UI 參數與統計呈現詳細規格

> 版本：v1.8　｜　日期：2026-08-19
> 上層文件：[`9max模擬平台實做計劃.md`](9max模擬平台實做計劃.md) 第五章
> 權威規格：[`9max平台核心規格.md`](9max平台核心規格.md)

本文件涵蓋本產品的 **視覺設計方向（第 V 章）** 與最重要的兩個 UI 面向：**參數如何呈現**與**統計數據如何呈現**。牌桌動畫保留，但屬輔助理解，不得壓縮本文件範圍或阻塞操作。現行文件清單見核心規格開頭；未列入者不具規範力。

**面板清單（合併後為七個）**：A 牌桌設定／B 座位設定／C Bot 參數／D 自身策略編輯器／**F 報表與數據儀表板**／E 執行／G Log 檢視器與重播。原面板 F（報表）與原面板 H（儀表板）已合併為單一面板 F，**面板 H 不再存在**。

**涵蓋範圍**：本文件目前有**面板 B**、**面板 D** 與**面板 F** 的欄位級規格。面板 **A／C／E／G** 的欄位級規格**尚未撰寫**，屬 M0 必須補齊的凍結項（見實做計劃 M0 與第十一章風險表）。M3 不得在缺欄位級規格的情況下開工。

**修訂紀錄**：

| 版本 | 日期 | 內容 |
|---|---|---|
| v1.1 | 2026-08-15 | 面板 D／H 欄位級規格 |
| v1.2 | 2026-08-16 | 修正 D.6 overbet 尺度重疊；Win Rate 更名為「獲勝手數比例」；D.3 補策略檔多桌型涵蓋要求；D.4 補位置標籤來源；補 dead button 手的切片處理；補手數效力預覽 |
| v1.3 | 2026-08-16 | **面板 F 與 H 合併為面板 F**（業主決議），H.x 重編為 F.x；全域原則 G.x 改為 UX.x 以免與面板 G 撞號；**位置命名定案：最早位一律為 `UTG`**，6 人桌改為 UTG／HJ／CO |
| v1.4 | 2026-08-16 | 新增**面板 B（座位與 Bot 設定）**欄位級規格：座位列表的位置 label 來源與更新時機、新增／複製／刪除／交換 Bot、重設為官方預設 9-max 組合、run 進行中的變更鎖定；面板 D 新增 **D.9 策略庫**（儲存、命名、切換、與 run 快照的關係） |
| v1.5 | 2026-08-16 | **移除 Bot 等級（L1–L5）**，座位列表與面板 C 不再有等級欄位，原等級層 10 欄改為逐座可調的行為參數；**起始深度預設改為 200BB**（仍可調），`stackBySeatBb` 型別為整數 BB |
| v1.6 | 2026-08-16 | 新增**第 V 章「視覺設計方向」**：App shell 三欄結構、色彩 token、字型與數值、行動字母編碼、牌桌視覺（細線橢圓＋圓形位置節點）、四色牌組、牌桌 HUD、行動按鈕與尺度列、資料表格密度、設定面板元件。**Codex 首版視覺草稿（`01-main-workspace.png`）經業主否決，列入 V.14 落差對照，不作為重做基礎** |
| v1.8 | 2026-08-19 | **補齊面板 A／C／E／G 的欄位級規格**，七個面板至此全部具備欄位級定義，M3 的開工前提解除。面板 A（牌桌設定，含 A.3 手數效力預覽連動、A.5 run 進行中鎖定）；面板 C（Bot 兩層參數，21 欄由 Rust schema 產生對照，含 C.5 決策管線七步呈現）；面板 E（執行，含 E.4 暫停不改變結果、E.5 執行期間可瀏覽既有報表）；面板 G（Log 與重播，含 G.5 底牌可見範圍、G.6 逐行動原因）。三處待業主確認以引言標示 |
| v1.7 | 2026-08-16 | 第 V 章補三節基礎 token：**間距與圓角（V.4）**、**圖示（V.5）**、**資訊密度（V.6）**；原 V.4–V.11 順移為 V.7–V.14。明確界定「元件圓角 vs 選取狀態必須直角」的界線，以及「密度 ≠ 漸進揭露」 |

---

## 全域 UX 原則

> 本節編號為 `UX.x`（原 `G.x`）。面板 G（Log 檢視器）補寫欄位級規格時會使用 `G.x`，改名以免撞號。

### UX.1 資訊架構

- 主導覽順序：牌桌設定 → 座位與 Bot → 自身策略 → 執行 → 報表／儀表板 → Log／重播。
- 參數與統計頁可以直接互相跳轉；從報表點擊情境時，策略編輯器應開啟相同桌型、位置與節點。
- 桌面寬度不足時優先保留參數值、單位、樣本數與 CI；裝飾圖、動畫與次要敘述後收合。

### UX.2 參數呈現

- 每個參數顯示名稱、目前生效值、單位、合法範圍、簡短說明及來源層級。
- Bot 參數顯示「官方預設 → 人格修正 → 行為參數 → 逐座覆寫 → 最終生效值」。
- 支援參數搜尋、分類、只看已覆寫、重設單欄、重設群組、複製座位、變更前後比較及 undo／redo。
- 未儲存修改固定顯示；切換頁面、策略或桌型前必須讓使用者儲存、放棄或返回。
- 即時驗證使用 Rust schema 產生的相同規則；前端通過不代表引擎可省略驗證。

### UX.3 動畫層級

- 牌桌動畫只有 `off`／`fast`／`normal` 三種模式。
- 動畫只呈現發牌、下注、收池、showdown 與重播時間序；不得自己改變底池、籌碼或行動狀態。
- 批次模擬不播放動畫；參數編輯及報表頁不載入非必要動畫資源。

---

## 視覺設計方向（V）

> 本節編號為 `V.x`，是**跨面板的視覺語彙與設計 token**，不是某個面板的欄位規格。所有面板的切版一律以本節為準。

### V.0 參考來源與定位

**參考素材**（`mockups/v1.4-current/`）：

| 檔案 | 內容 | 取用重點 |
|---|---|---|
| `maxresdefault.jpg` | Table Wizard — Overlay 設定畫面 | 極簡橢圓桌、圓形位置節點、行動字母 chip、下注尺度列、折疊式設定區塊 |
| `692db06a6ef2bcf59251fe21_Use this.avif` | 同產品完整版面 | icon rail ＋ 參數欄 ＋ 主內容三欄結構、浮動 HUD 數值卡、綠色強調 |
| `image.png` | GTO Wizard — Analyzed hands 列表 | 密集資料表格、篩選 chip 列、四色牌方塊、行動字母序列、分頁 |
| `01-main-workspace.png` | **本專案 v1.4 草稿（Codex 首版，業主已否決）** | **不是方向**。僅作為「不要長成這樣」的對照，見 V.14 |

**定位**：取用的是**設計語彙**（資訊密度、元件形態、色彩層級、編碼方式），不是像素級複製。品牌識別、logo、專有配色與版面比例須自行設計，不得直接沿用參考產品的識別元素。

### V.1 App shell 結構（三欄）

```
┌──────────────────────────────────────────────────────────────┐
│ Header 48px：產品名 │ 模式切換 │ 執行控制 │ 手數/底池 │ 引擎狀態 │
├────┬──────────────┬──────────────────────────────────────────┤
│icon│  面板參數欄    │                                          │
│rail│  280–320px    │            主內容區                       │
│56px│  可收合        │      （牌桌／矩陣／表格／圖表）            │
│    │              │                                          │
├────┴──────────────┴──────────────────────────────────────────┤
│ Status bar 24px：引擎 · SQLite · 動畫模式 · 批次 UI 更新頻率     │
└──────────────────────────────────────────────────────────────┘
```

- **icon rail**：面板 A–G 的切換，僅圖示 ＋ hover tooltip。
- **選中態**：整格**直角**背景色塊填滿 ＋ 圖示轉為強調色。**不得使用左側強調邊框或側邊指示條**，不使用圓角膠囊。圓角與選取態的界線見 V.4。
- **面板參數欄**：該面板的參數與設定，可收合以擴大主內容區。
- 桌面寬度不足時，依 UX.1 優先保留參數值、單位、樣本數與 CI。

### V.2 色彩 token

| Token | 值 | 用途 |
|---|---|---|
| `--bg-base` | `#0b0c0e` | 全站背景 |
| `--bg-surface` | `#16181c` | 面板、卡片、表格容器 |
| `--bg-raised` | `#1e2126` | 浮動 HUD 卡、下拉、tooltip |
| `--bg-hover` | `#22262c` | 列 hover、按鈕 hover |
| `--border` | `#2a2e34` | 分隔線、欄位框、牌桌輪廓 |
| `--text-primary` | `#e8eaed` | 主文字、數值 |
| `--text-secondary` | `#9aa0a6` | 標籤、單位、次要敘述 |
| `--text-tertiary` | `#6b7280` | 停用、佔位、樣本不足灰階 |
| `--accent` | `#22c55e` | 唯一強調色：選中、主要按鈕、焦點 |
| `--accent-hover` | `#16a34a` | 強調色 hover |
| `--positive` | `#22c55e` | 正盈虧、達標 |
| `--negative` | `#ef4444` | 負盈虧、error |
| `--warning` | `#f59e0b` | warning、探索性標示 |

- **單一強調色（綠）**，克制使用——只用於選中態、主要按鈕與焦點，不做大面積填充。藍色**不作為主色**，僅保留給 Fold 的行動編碼（V.7）。
- 不使用賭場符碼：金籌碼堆、賭場紅黑金、拉霸特效一律不採用（沿用實做計劃第六章第 3 點）。

### V.3 字型與數值

- **所有數字使用等寬字型**（`JetBrains Mono`／`Consolas`／`ui-monospace`），確保表格與矩陣縱向對齊。
- 標籤與敘述使用系統 sans-serif（`Inter`／`Noto Sans TC`）。
- 基準字級 13px；表格內 12–13px；大數值（bb/100、底池）20–28px；面板標題 14px。
- 數值一律**右對齊**；正值前置 `+`，負值 `−`，並套用 `--positive`／`--negative`。
- 對外顯示最多一位小數（F.5「不假精確」）。

### V.4 間距與圓角

**間距：8px 基準網格**，所有內距與間隔取 4 的倍數。

| 用途 | 值 |
|---|---|
| 元件內部（chip、tag、小按鈕） | 水平 6–8 px／垂直 2–4 px |
| 表單列、清單列的垂直內距 | 8 px |
| 面板、卡片內距 | 12–16 px |
| 同群組區塊之間 | 12 px |
| 跨群組之間 | 24 px |
| 三欄之間（rail／參數欄／主內容） | 16 px |
| 表格 cell 水平內距 | 12 px |

**圓角階梯**：

| 元件 | radius |
|---|---|
| 牌方塊、行動 chip、狀態 tag | 3–4 px |
| 按鈕、輸入框、下拉、篩選 chip | 6 px |
| 面板、卡片、表格容器、HUD 卡 | 8 px |
| 大容器（牌桌卡、對話框） | 12 px |
| toggle switch、圓形位置節點、狀態點 | 全圓 |

> **圓角與「選取狀態必須直角」的界線**（兩者不衝突，但實作極易混用）：
>
> - **圓角**用於**容器與元件本身的外形**——卡片、按鈕、chip、輸入框都有圓角，這是參考圖的語彙。
> - **選取／作用中狀態的標示一律直角**：icon rail 的選中格、清單選中列、tab 選中態，一律用**整格直角背景色塊填滿**＋文字加深。**不得使用圓角膠囊，不得使用左側強調邊框或側邊指示條。**
> - 換句話說：一個有 8px 圓角的卡片，它內部被選中的那一列是直角滿版色塊。

### V.5 圖示

- **線性 outline 圖示**，非填充；線寬 1.5–2 px；端點與轉角圓角。不使用彩色、擬真或多色圖示。
- **採用單一開源圖示庫，不混用**。建議 **Lucide**（ISC 授權）或 Heroicons（MIT）；納入技術選型的授權審查一併確認。
- 尺寸：

| 位置 | 圖示尺寸 | 點擊區 |
|---|---|---|
| icon rail | 20 px | ≥ 40×40 px |
| 面板內、按鈕內 | 16 px | ≥ 32×32 px |
| 表格列內、chip 內 | 14 px | ≥ 24×24 px |

- **只有圖示沒有文字時必須有 tooltip**（icon rail 全部項目適用）。
- 語意圖示（完成 ✓、警告 ⚠、鎖定、樣本不足）須與語意色搭配，但**狀態不得只靠圖示顏色傳達**——需同時具備形狀差異或文字（延續 V.7 的色盲原則）。

### V.6 資訊密度

本產品定位是**高密度分析工具**，預設即為高密度；留白用於分組，不用於裝飾。

提供兩段密度切換（**不是三段**，避免中間值難以校準）：

| 項目 | `compact` | `standard`（預設） |
|---|---|---|
| 表格列高 | 24 px | 30 px |
| 清單列高（座位列表） | 28 px | 36 px |
| 面板內距 | 8 px | 12–16 px |
| 表格 cell 水平內距 | 8 px | 12 px |
| 基準字級 | 12 px | 13 px |

> **密度 ≠ 漸進揭露，兩者是不同機制，不得互相取代：**
>
> - **密度**只改變**空間**（列高、內距、字級）。切換密度**不得隱藏任何資訊**。
> - **漸進揭露**（V.13、核心規格 6.1）改變的是**顯示哪些內容**——常用值固定可見，進階項折疊。
> - 需要少顯示東西時用漸進揭露，不要靠調密度；需要一頁塞更多時用密度，不要靠折疊常用值。
>
> 密度不影響字型、色彩、圓角與圖示尺寸；縮排與對齊網格在兩種密度下維持一致。

### V.7 行動編碼（跨牌桌、log、重播共用）

以**單字母 chip** 表示行動，緊接位置節點顯示：

| 行動 | 字母 | 顏色 |
|---|---|---|
| Fold | `F` | 藍 `#3b82f6` |
| Check | `X` | 綠 `#22c55e` |
| Call | `C` | 綠 `#22c55e` |
| Bet | `B` | 紅 `#ef4444` |
| Raise | `R` | 紅 `#ef4444` |
| All-in | `AI` | 深紅 `#991b1b` |

- 有尺度時附在字母後：`R 2.3`（BB）或 `B 75%`（%Pot），單位依 D.6 的顯示切換。
- **字母是主要載體，顏色只是加速辨識。** 移除顏色後資訊必須完整——這是色盲可用性的實作方式，也讓 log 的純文字匯出（`FFRRFFRRC`）保持可讀。
- 顏色可由使用者自訂並「重設為預設」（參考圖的 Colors 圖例作法）。

### V.8 牌桌視覺

- **橫向橢圓，細線輪廓，無絨布材質**。以 `--border` 畫 1–2px 橢圓，桌面同 `--bg-base`——牌桌與背景幾乎同色，靠輪廓線界定，**不做綠色絨布、不做厚邊框、不做材質貼圖**。
- **座位為圓形節點**（直徑約 48–56px），節點內顯示**位置 label**（`UTG`／`HJ`／`CO`／`BTN`／`SB`／`BB`…），不是玩家名稱。
- 名稱、籌碼、人格顯示在節點外的小字，或收在左側座位列表（B.2）。
- **使用者座位**以強調色描邊 ＋ 底色區隔。
- **按鈕位**以獨立的小 `D` 標記掛在該節點外緣。
- **dead button／dead SB**：該手無 `BTN`／`SB` 標籤時，`D` 標記顯示在**空的按鈕位置**並以 `--text-tertiary` 呈現，同時在牌桌角落標示「dead button」。不得把標記硬塞給鄰座（B.3）。
- 公共牌與底池置中；每座的當手投入以小額籌碼標示於節點與底池之間。

### V.9 牌面（SVG，四色牌組）

- 52 張牌面、牌背、花色符號全為 SVG（沿用實做計劃第六章第 1 點）。
- **預設四色牌組**：♠ `#d1d5db`（深底上不用純黑）、♥ `#ef4444`、♦ `#3b82f6`、♣ `#22c55e`。可切換為傳統兩色。
- **花色符號必須始終繪出**，顏色只是加速辨識；紅（♥）與綠（♣）對紅綠色盲難分，形狀是唯一可靠的區分依據。
- 表格與 log 內使用**緊湊牌方塊**（點數 ＋ 花色的小色塊，如參考圖 `image.png`），牌桌上使用完整牌面。

### V.10 牌桌 HUD 數值卡

浮動於牌桌左上，`--bg-raised`：

| 欄位 | 說明 |
|---|---|
| 手數 | 目前手序（大字） |
| `SPR` | Stack-to-Pot Ratio |
| `PO` | 底池賠率（Pot Odds） |
| `EFF` | 當前有效籌碼（BB），並標示所屬 bucket（規則細則 8.5） |

- 互動模式即時更新；**批次模式依 B.3 的低頻取樣更新**，不得逐手重繪。

### V.11 行動按鈕與下注尺度列

**兩者並存，不可互相取代**：

- **行動按鈕**（互動對打必需）：棄牌／過牌／跟注 N BB／加注至 N BB。只顯示引擎回傳的合法行動（核心規格 3.1），主要行動套用強調色。
- **下注尺度快選列**：依 D.6 的 `betSizeTree` 渲染（25%／33%／50%／66%／75%／100%／125%／150%／200%／All-in），加一個自訂輸入框。可切換 %Pot 與 BB 顯示；當前選定值以強調色標示。
- UI **不得自行計算**可下注金額、最小加注額或底池（核心規格 2.2）；所有金額由引擎回傳。

### V.12 資料表格（面板 F／G 共用）

依 `image.png` 的密度取向：

- **篩選器為 chip 式下拉**，橫排於表格上方（日期／位置／街別／輸贏／牌型／EV 區間…）。
- 列高緊湊（28–32px），交替列不使用斑馬紋，改以 hover 底色與細分隔線。
- 數值欄等寬右對齊；正負套語意色；狀態用圖示 ＋ tooltip。
- **行動欄以字母序列呈現整手牌**，依街分組並加 `Preflop`／`Flop`／`Turn`／`River` 小標（例：`FFRRFFRRC`）。這是在單列內表達整手牌的關鍵，對百萬手 log 的瀏覽效率決定性。
- 底部固定分頁列 ＋ 每頁筆數；100 萬手 DB 的載入須符合核心規格 7.2 的 p95 ≤200 ms。
- 寬表格允許水平捲動，但**位置／日期等識別欄固定**（sticky column）。

### V.13 設定面板元件

- **折疊區塊 ＋ toggle switch**：每個群組一列，左側 toggle 控制啟用、右側箭頭展開細項。這是核心規格 6.1「漸進揭露」的實作形態。
- 展開後才顯示 slider、數值輸入、選項按鈕組、色彩圖例。
- 每個群組提供「重設本群組」；每欄提供「重設本欄」（UX.2）。
- **已覆寫的欄位加標記**，並支援「只看已覆寫」篩選。
- run 進行中鎖定的面板（B.6）整欄降為 `--text-tertiary` 並顯示鎖定原因。

### V.14 v1.4 草稿（已否決）— 落差對照

> `01-main-workspace.png` 是 Codex 產出的首版視覺草稿，**業主明確表示非常不滿意**。它**不是**重做的基礎——**不要在它上面修改**，依 V.1–V.13 重新設計。本節只列出它與參考方向的落差，避免重蹈。

**視覺落差（這些是被否決的主因方向）**：

| 項目 | v1.4 草稿 | 應為 |
|---|---|---|
| 牌桌 | **綠色絨布 ＋ 厚邊框**，像線上撲克室 | 細線橢圓輪廓、無絨布、與背景同色的冷靜分析介面（V.8） |
| 座位 | 矩形卡片，笨重、佔空間、密度低 | 圓形位置節點，資訊掛在節點外（V.8） |
| 主色 | **大面積亮藍填充**，選中態過度搶眼 | 單一綠色強調、克制使用；藍降為 Fold 專用（V.2／V.7） |
| 整體調性 | 偏「遊戲畫面」 | 偏「專業分析工具」——數據優先、留白克制、密度高 |
| 資訊密度 | 牌桌佔據過大面積，數據區被擠壓 | 牌桌是功能畫面而非主視覺；數據與參數是主體 |
| 行動呈現 | 只有底部文字紀錄列 | 位置節點旁的字母 chip，一眼看完整條行動線（V.7） |
| HUD | 只有頂部手數／底池 | 牌桌浮動卡 SPR／PO／EFF（V.10） |
| 下注尺度 | 只有四個大行動按鈕，吃掉垂直空間 | 行動按鈕 ＋ 緊湊尺度快選列並存（V.11） |
| 左側導覽 | 文字選單，佔寬 | icon rail ＋ 可收合參數欄（V.1） |

**規格層面已過時（無論視覺怎麼重做都必須修正）**：

| 項目 | v1.4 草稿 | 現行規格 |
|---|---|---|
| Bot「等級 L4」下拉 | 有 | **移除**——等級已於 2026-08-16 取消 |
| 座位列表「P3 · L4」 | 有 | 僅顯示人格 `P3`（B.2） |
| 「Persona 修正 → Level 修正」對照欄 | 有 | 改為「人格修正 → 行為參數」（UX.2） |
| 起始深度 `100 BB` | 100 | **預設 `200 BB`**（可調、可逐座覆寫） |

**但下列是規格要求的功能，重做時不得一併丟掉**（它們的問題是表現形式，不是存在本身）：

- 左側**座位列表含位置 label**（B.2，業主明確要求）
- Bot 參數的**「官方預設 → 修正 → 最終值」逐層對照**（UX.2）
- **「run 開始後設定鎖定」**的狀態提示（B.6）
- **樣本狀態卡**：先講可判定／樣本不足，再給 bb/100、95% CI、N（F.4）
- 互動對打的**合法行動按鈕**（V.11）

---

## 面板 A — 牌桌設定

> 欄位定義的權威來源是 [`9max平台核心規格.md`](9max平台核心規格.md) 2.1。本節只規範**呈現方式與編輯 UX**，不重複定義欄位語意；兩者衝突時以核心規格為準。

### A.1 版面

- 單欄表單，依「桌型 → 籌碼 → 強制下注 → 抽水 → 執行」分群，群間距 24px（V.4）。
- 每個欄位依 UX.2 顯示名稱、目前生效值、單位、合法範圍與簡短說明。
- 修改後即時以 Rust 產生的 schema 驗證；非法即標紅並給修復提示，提交後引擎再驗一次。

### A.2 欄位

| 群組 | 欄位 | 型別／範圍 | 預設 | 呈現要點 |
|---|---|---|---|---|
| 桌型 | `players` | 6–9 | **9** | 分段控制而非下拉；改動會重算下方全部效力預覽 |
| 桌型 | `autoRefill.enabled` | 布林 | 開 | 關閉時 A.4 的桌次結束條件說明必須同步改變 |
| 桌型 | `autoRefill.targetPlayers` | 6–9，且 ≤ `players` | 同 `players` | `autoRefill.enabled` 為關時停用並灰階 |
| 籌碼 | `stackBySeat` | 整數 BB | **200** | 全桌統一輸入 ＋「逐座覆寫」展開區；覆寫後顯示「統一值 → 覆寫值」 |
| 籌碼 | `stackPolicy` | 固定 `bustOut` | — | **唯讀顯示**，非下拉。v1 只有一種政策，做成可選會誤導 |
| 強制下注 | `smallBlind` / `bigBlind` | 最小籌碼單位正整數，`SB < BB` | 1／2 | 兩欄相鄰；`SB ≥ BB` 即時標紅 |
| 強制下注 | `ante.mode` | `none`／`perPlayer`／`bbAnte`／`btnAnte` | `none` | 選 `bbAnte`／`btnAnte` 時，旁註說明代付者付「金額 × 在座人數」 |
| 強制下注 | `ante.amount` | 非負整數 | 0 | `mode` 為 `none` 時強制 0 並停用 |
| 強制下注 | `straddle.mode` | `none`／`single`／`double` | `none` | — |
| 強制下注 | `straddle.seats` / `amounts` | 自 UTG 起連續；首段 2×BB，後段為前段 2 倍 | — | 金額欄**唯讀自動計算**。規則細則 2.3 的倍數是硬規則，開放輸入只會製造非法值 |
| 抽水 | `rake.pct` / `rake.cap` / `rake.noFlopNoDrop` | 0–100%，小數點後兩位／整數／布林 | 0／0／關 | 欄位以 % 顯示，**跨 IPC 傳萬分比整數**（5% = 500）。實務常見的 4.5% 用整數百分比表達不出來，而規則層不收浮點。cap 可切換以 BB 或最小籌碼單位顯示 |
| 執行 | `handLimit` | 1K–100K，**以 1K 為單位** | 10K | 滑桿＋數字輸入並存；旁邊即時顯示 A.3 的效力預覽 |
| 執行 | `masterSeed` | u64 | 隨機 | 提供「固定／每次隨機」切換與「複製 seed」按鈕；重現他人結果的唯一入口 |

### A.3 手數與統計效力預覽

`handLimit` 旁必須即時顯示 F.5.1 定義的四項預覽。**這是面板 A 的必要組成，不是附加資訊**——核心規格 5.3.1 要求效力在設定階段就看得見。

### A.4 執行前的設定摘要

按下執行前顯示一段純文字摘要，讓使用者確認實際要跑的是什麼：

- 桌型與補位：`9-max，自動補位至 9 人`
- 籌碼與政策：`起始 200BB，破產離桌`
- 桌次結束條件：`使用者破產／達手數上限`（補位關閉時加上`／在桌人數不足 6`）
- 強制下注與抽水的實際值
- 手數與 seed

### A.5 鎖定與重現

- **run 進行中，本面板全部欄位鎖定**（與 B.6 同一約束）。中途改設定會讓 `RunManifest` 與實際執行不符，破壞可重現性。
- 提供「載入既有 run 的設定」：由 `RunManifest` 還原全部欄位，供重現與對照比較。

> **待業主確認**：`masterSeed` 預設要「每次隨機」還是「固定」。每次隨機較貼近實際使用，固定則利於重複比較。目前草案採每次隨機。

---

## 面板 B — 座位與 Bot 設定

### B.1 版面

- **左側座位列表**（常駐）：由上而下列出全部在桌座位，使用者座位固定置頂並以醒目底色標示。
- **右側編輯區**：點選任一 Bot 座位後編輯其人格、行為參數與逐座覆寫（欄位定義見面板 C）。
- 座位列表在牌桌視圖與報表視圖之間切換時保持可見，作為全域的座位脈絡。

### B.2 座位列表的每列欄位

| 欄位 | 內容 | 說明 |
|---|---|---|
| 位置 label | `UTG`／`UTG+1`／`UTG+2`／`LJ`／`HJ`／`CO`／`BTN`／`SB`／`BB` | **當前手**的位置標籤，見 B.3 |
| 座位號 | `#1`–`#9` | 固定不變的實體座位識別，位置 label 每手輪轉時座位號不動 |
| 名稱 | 使用者座位為「你」；Bot 為可自訂名稱，預設 `BOT-<座位號>` | — |
| 人格 | 例 `P3 · 緊兇` | **無等級欄位**（L1–L5 已於 2026-08-16 移除）。點擊直接跳到面板 C 的對應欄位 |
| 籌碼 | 目前籌碼（bb 與最小籌碼單位可切換） | 未開始時顯示該座的起始深度（預設 200BB） |
| 狀態點 | 在牌局中／已棄牌／All-in／**已離桌** | 破產離桌的座位保留列位並標示，直到補位或桌次結束 |

### B.3 位置 label 的來源與更新時機

- **來源**：位置 label 一律取自引擎依 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4.1 寫入每手 log 的欄位。**UI 不得自行從座位索引推導**，否則 dead button／dead SB 手會標錯。
- **dead 情況**：該手為 dead button 時無任何座位顯示 `BTN`，dead small blind 時無任何座位顯示 `SB`。列表須能呈現「本手無此位置」，不得回退成把標籤硬塞給鄰座。同時在列表頂端標示該手為 dead button／dead SB。
- **更新時機（效能約束）**：

| 模式 | 行為 |
|---|---|
| 互動對打 | 每手結算後即時更新，可加輪轉動畫（淡出→換字→淡入），依 UX.3 可關閉 |
| **批次模擬** | **不逐手更新**。批次每秒可跑數千手，逐手重繪會直接違反核心規格 7.2 的 UI 門檻。改為固定低頻取樣更新（建議 ≥500 ms 一次）或顯示「批次進行中」而不顯示位置 |
| 重播 | 依重播進度更新，與重播速度（1x–10x）同步 |

- **run 未開始時**：尚未決定按鈕位置，顯示座位號與「開桌後決定」，或依初始按鈕位置顯示**預覽**標籤並明確標示為預覽值。

### B.4 Bot 操作

| 操作 | 行為 | 限制 |
|---|---|---|
| **新增 Bot** | 於空位加入 Bot，需指定人格與起始深度（預設 200BB） | 上限 9 人（含使用者） |
| **複製 Bot** | 複製來源座位的**完整設定**（人格、行為參數、逐座覆寫、起始深度）到目標座位；不複製名稱與當前籌碼 | 需有空位；滿桌時須先選定要覆蓋的目標座位並二次確認 |
| **刪除 Bot** | 移除該 Bot，座位變空 | **下限 6 人（含使用者）**，刪到 6 人後禁用並提示；**使用者座位不可刪除** |
| **交換座位** | 兩座設定互換 | — |
| **重設為預設** | 見 B.5 | — |
| **匯入／匯出** | 座位配置整組 JSON 匯入匯出 | 匯入時驗證人數在 6–9 且含且僅含一個使用者座位 |

### B.5 重設為官方預設 Bot 組合

- 提供一鍵「**重設為官方預設 9-max 組合**」，把全桌 Bot 回復到出廠樣板。
- 樣板同時提供既有的三組情境預設：**魚桌／Reg 桌／混合桌**；「官方預設 9-max」為其中的預設選項。
- **重設範圍二選一，UI 必須讓使用者明確選擇**：

| 範圍 | 行為 |
|---|---|
| 單座重設 | 只把該座位回復為樣板中對應座位的人格與行為參數，清除逐座覆寫；不改變座位數 |
| 全桌重設 | 回復全部 Bot 座位；**座位數一併回到樣板的 9 人**。目前若為 6／7／8 人桌，重設後會變成 9 人桌，執行前必須明確告知 |

- 樣板內容（各座位的人格分配與行為參數）由牌手顧問於 M2 定案並隨內容包版本化；樣板版本寫入 `RunManifest`。
- **術語澄清**：此處的「預設組合／樣板」與 RNG 的 `seed`（面板 A 的亂數種子）**是兩件不同的事**，文件與介面都不得混用「seed」一詞指稱 Bot 樣板。

### B.6 變更時機（可重現性約束，不可放寬）

依核心規格 3.3 與 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.1.1，`RunManifest` 保存的是 run 開始時的 Bot 快照，且「Bot 組合在整個 run 中維持不變」。因此：

- **run 進行中（含暫停）一律不得新增、刪除、複製、交換 Bot 或修改任何 Bot 參數。** UI 在 run 開始後即鎖定面板 B 與面板 C 為唯讀，並顯示鎖定原因。
- 要改設定必須先停止或完成該 run；變更後的設定屬於**新的 run**，與既有 run 的統計不可合併比較。
- 自動補位進場的新 Bot 沿用該座位原設定，不視為「變更」，因此不破壞此約束。
- 面板 B 的唯讀鎖定與面板 A 的參數鎖定採同一機制與同一提示文案。

---

## 面板 C — Bot 參數（兩層 schema）

> **欄位定義由 Rust structs 產生**（`apps/engine/src/bot/params.rs`），本節的表格是該 schema 的呈現對照，不是另一份定義。兩者不一致時以 schema 為準，並視為本文件待修。

### C.1 版面與逐層對照

- 由面板 B 點選某座 Bot 後進入。頂端顯示座位號、名稱與人格。
- 每個欄位依 UX.2 呈現 **「官方預設 → 人格值 → 逐座覆寫 → 最終生效值」**，只顯示最後數字不合格。
- 已覆寫的欄位以強調色標示，並提供「重設此欄」與「重設本群組」。
- 提供「只看已覆寫」篩選，避免 21 個欄位淹沒實際改動。

### C.2 人格層（`persona`，11 欄）

| 欄位鍵 | 顯示名稱 | 型別 | 預設 | 合法範圍 | 說明 |
|---|---|---|---|---|---|
| `rangeWidth` | 範圍寬度 | 萬分比 | 100% | 50–160% | 相對基準策略的進池範圍縮放。100% 為不變，越高越鬆 |
| `preflopAggression` | 翻前侵略性 | 萬分比 | 100% | 50–180% | 翻前把跟注權重移往加注的傾向。100% 為不變 |
| `postflopAggression` | 翻後侵略性 | 萬分比 | 100% | 50–180% | 翻後把過牌與跟注權重移往下注與加注的傾向 |
| `callPersistence` | 跟注黏著度 | 萬分比 | 100% | 50–180% | 面對下注時保留跟注權重的傾向，越高越像跟注站 |
| `foldDiscipline` | 棄牌紀律 | 萬分比 | 100% | 50–180% | 在弱勢節點確實棄牌的傾向，與跟注黏著度反向 |
| `bluffFrequency` | 詐唬頻率 | 萬分比 | 100% | 0–200% | 以弱牌採取主動行動的頻率縮放 |
| `valueThreshold` | 價值門檻 | 萬分比 | 100% | 50–160% | 採取價值下注所需的牌力門檻。越高越保守 |
| `betSizePreference` | 尺度偏好 | 萬分比 | 100% | 50–160% | 在可用尺度中偏好較大或較小的一端。100% 為置中 |
| `trapFrequency` | 設陷頻率 | 萬分比 | 100% | 0–200% | 以強牌選擇過牌或跟注以設陷的頻率 |
| `tiltResistance` | 抗傾斜 | 萬分比 | 100% | 0–100% | 近期大額損失後維持原策略的程度。越低越容易偏離 |
| `adaptationStyle` | 調整風格 | 列舉 | 0 | 0–2（列舉） | 對手模型生效時的調整方向：0 不調整／1 針對緊手／2 針對鬆手 |

### C.3 行為層（`botBehavior`，10 欄）

原「等級層」。2026-08-16 移除 L1–L5 分級後，這 10 欄改為**逐座直接可調**，因為它們是引擎管線的必需輸入而非分級的附屬品。

| 欄位鍵 | 顯示名稱 | 型別 | 預設 | 合法範圍 | 說明 |
|---|---|---|---|---|---|
| `decisionNoisePp` | 決策噪音 | 萬分比 | 0% | 0–30% | 以具名公式混入均勻分佈的比例。0 為完全依策略 |
| `preflopCoverage` | 翻前覆蓋率 | 萬分比 | 100% | 0–100% | 使用完整翻前策略表的比例，其餘走 fallback |
| `postflopBucketCount` | 翻後分桶數 | 整數 | 8 | 2–24 | 翻後牌力分桶的粒度。越少越粗糙 |
| `allowedBetSizes` | 可用尺度數 | 整數 | 4 | 1–10 | 從尺度樹取用的尺度數量上限 |
| `bluffComplexity` | 詐唬複雜度 | 整數 | 1 | 0–3 | 詐唬選牌的依據層數。0 只看牌力，越高越考慮阻斷與線路 |
| `multiStreetPlanningDepth` | 多街規劃深度 | 整數 | 1 | 0–3 | 決策時往後推演的街數。0 為只看當街 |
| `opponentModelEnabled` | 啟用對手模型 | 布林 | 否 | 布林 | 是否依觀察到的公開行動調整對手範圍估計 |
| `opponentModelUpdateHands` | 對手模型更新手數 | 整數 | 100 | 10–10000 | 累積多少手後更新一次對手模型 |
| `exploitAdjustmentCapPp` | 剝削調整上限 | 萬分比 | 10% | 0–30% | 對手模型可改變任一行動頻率的上限。引擎強制套用 |
| `explanationDepth` | 解釋深度 | 整數 | 2 | 0–3 | 決策 trace 保留的層數。影響 log 大小與面板 G 的可解釋性 |

### C.4 逐座覆寫

- 兩層的任一欄皆可逐座覆寫，覆寫優先於人格層與行為層的設定值。
- **只接受已登錄的欄位鍵**。核心規格 4.3 第 4 點：「不得直接注入未登錄參數」——UI 不提供自由文字的鍵值輸入。
- 覆寫值同樣受 schema 的型別與範圍約束，越界即標紅。

### C.5 決策管線的呈現

面板 G 的逐行動原因直接由引擎的決策 trace 渲染，階段順序固定為核心規格 4.3 的七步：

`基準策略 → 人格修正 → 行為參數 → 逐座覆寫 → 合法遮蔽與剝削上限 → 決策噪音 → 最終選擇`

- 每階段顯示該階段結束後的行動分佈。
- 只列出**實際生效**的具名偏移；有設值但未改變分佈的參數不列，否則會用一堆無作用的「修正」掩蓋真正起作用的那些。
- `explanationDepth` 控制 trace 保留的層數，影響 log 大小與本區的可解釋性。

### C.6 變更時機

與 B.6 同一約束：**run 進行中不得修改任何 Bot 參數**。Bot 組合是 `RunManifest` 快照的一部分，中途變更會讓該 run 無法重現，也無法與其他 run 比較。

---

## 面板 D — 自身策略編輯器

### D.1 總覽與版面

- 定位：本產品**資訊密度最高**的面板，編輯「自身座位」的策略。
- 版面：左側「情境導航樹」（位置 → 情境 → 街別），右側「編輯區」（範圍矩陣／規則列表／尺度樹）。
- 預設收合（依核心規格第六章漸進揭露）；常用值固定可見，進階使用者再展開規則細節。
- 所有修改即時驗證，非法標紅並給修復提示；儲存前顯示「策略完整度」。

### D.2 策略 Meta（`Strategy.meta`）

| 欄位 | 型別 | 說明 |
|---|---|---|
| id | string | 唯一識別 |
| name | string | 顯示名稱 |
| author | string | 作者 |
| version | string | 版本（每次儲存自動遞增） |
| source | enum | official（唯讀）／ custom（複製後可編輯） |
| baselineVersion | string | 所基於的官方基準版本（如「基準 v1.2」） |
| createdAt / updatedAt | datetime | 建立／更新時間 |

### D.3 桌型適用範圍（`Strategy.table`）

| 欄位 | 型別 | 說明 |
|---|---|---|
| players | 6–9 的**集合**（預設涵蓋 6／7／8／9） | 本策略校準涵蓋的座位數。**不是單一值**：破產離桌會使在桌人數自開桌人數持續下降，一張 9-max 桌在同一個 run 內會經歷 9／8／7／6 人四種桌型 |
| blind | struct | SB／BB；顯示可用 bb 正規化，保存為最小籌碼整數 |
| stackBySeatBb | map[seat, integer BB] | 每座**桌次開局**的起始深度，**預設 200BB**（仍可調，可逐座覆寫）。破產離桌後籌碼跨手漂移，策略 applicability 依英雄與相關對手的**有效籌碼 bucket**（規則細則 8.5，共 9 檔）判定，不依起始深度 |
| ante | enum | none／perPlayer（逐人）／bbAnte（BB ante）／btnAnte（BTN ante）＋ 金額 |
| straddle | struct | none／single／double ＋座位與金額 |
| rake | struct | pct ＋每手 cap ＋ `noFlopNoDrop: boolean` |

> **策略檔的桌型涵蓋是完整度問題，不是綁定問題。** 因為在桌人數會在 run 內下降，只為 9-max 校準的策略檔在桌上剩 8 人之後就會整段落入 fallback，使用者卻仍以為自己在測試自己的策略。因此：
>
> - 策略檔顯示**逐桌型的完整度**（9 人 X%／8 人 Y%／7 人 Z%／6 人 W%），不只顯示單一總完整度。
> - 開始執行前，若任一將會出現的桌型完整度低於門檻，UI 必須明確警告「此 run 預計有 N% 的手牌落在未校準桌型」，並顯示預估比例。
> - 該預估比例由開桌人數、`autoRefill` 設定與歷史桌次長度推算；無歷史資料時以 M0 實測的衰減曲線估算。
> - 桌型完整度不足**不阻擋執行**（fallback 本來就是合法路徑），但必須在報表的切片脈絡中持續顯示。

### D.4 Preflop 範圍矩陣

**位置集合（依座位數動態映射）**：

| 座位數 | 位置集合 |
|---|---|
| 9（預設） | UTG／UTG+1／UTG+2／LJ／HJ／CO／BTN／SB／BB |
| 8 | UTG／UTG+1／LJ／HJ／CO／BTN／SB／BB |
| 7 | UTG／LJ／HJ／CO／BTN／SB／BB |
| 6 | UTG／HJ／CO／BTN／SB／BB |

> 6-max 以下不在產品範圍內。破產離桌後在桌人數可能在**手與手之間**短暫低於 6，該情形下桌次即結束（自動補位關閉時）或於下一手開始前補回 `targetPlayers`（自動補位開啟時）。依核心規格 2.1 的座位數不變量，**任何一手開始時在桌人數必須 ≥6**，引擎不會發出 5 人以下的手牌。

**位置標籤來源**：上表是無 dead blind 情況下的位置集合。實際每手的位置標籤由引擎依 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4.1 產生並寫入 log，**UI 與統計層一律讀該欄位，不得自行從座位索引推導**。dead button／dead small blind 手可能缺少 `BTN` 或 `SB` 標籤，UI 需能呈現該情形。

> **位置命名已定案（2026-08-16）**：**最早位一律命名 `UTG`**，不使用 `LJ` 作為最早位。因此 6 人桌為 `UTG／HJ／CO`（`LJ` 在 6 人桌不出現）。此命名即 preflop baseline 內容表的欄位鍵，引擎、策略內容與 UI 三方共用同一組字串，不另設別名。產生規則見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4.1。

**節點情境清單**：

| 情境 | 節點可用行動範例 |
|---|---|
| Unopened | Fold／Limp／Raise(size)／All-in |
| vs Limp | Fold／Overlimp／Isolation Raise(size)／All-in |
| vs Open | Fold／Call／3-bet(size)／All-in |
| vs 3-bet | Fold／Call／4-bet(size)／All-in |
| vs 4-bet | Fold／Call／5-bet(size)／All-in |
| vs Squeeze | Fold／Call／Raise(size)／All-in |
| Short Stack | Fold／Limp／Call／Raise(size)／All-in，依合法行動顯示 |

每個節點由桌型人數、位置、有效籌碼 bucket、ante、rake、straddle 與完整公開翻前 action history 識別；不得只用「vs Open」文字合併不同加注尺度與前序行動。

**169 格編碼**：13×13 網格；對角線 = 對子（AA…22，13 格），上三角 = 同花（AKs…32s，78 格），下三角 = 非同花（AKo…32o，78 格）；合計 169。每格保存的是**各合法行動的頻率分佈**，不是單一「進池頻率」。

**頻率編輯**：
- Action tabs 顯示 Fold／Call／Raise 尺度／All-in；矩陣以分色堆疊顯示同一手牌的混合頻率。
- 點格可直接輸入各 action 的 0–100%；同一手牌在 legal-action mask 後合計必須為 100%。
- 支援選區批次填入、複製 action layer、按比例縮放與鎖定某 action 後重新分配其餘頻率。
- 色階不得只靠紅綠；格內可切換顯示主要 action、完整分佈或數值。
- **v1 範圍**：UI 僅提供 169 格 × action 編輯；combo 權重由引擎與 schema 支援，UI 下鑽列後續。

### D.5 Postflop 規則清單

- 依優先序排列，**第一條命中即生效**。
- 每條規則欄位：

| 欄位 | 型別 | 說明 |
|---|---|---|
| street | enum | flop／turn／river |
| boardTexture | enum | 乾／濕／成對／單色／三同花／順子面／高張… |
| handStrength | enum | value／bluff／bluffcatcher／draw／其他分桶 |
| activePlayers | integer／range | 本街仍在牌局的人數 |
| actionOrder | struct | 英雄行動順位及各存活對手的相對位置；多人底池不得只用 IP／OOP |
| potType | enum | SRP／3-bet Pot／4-bet Pot |
| currentStreetAction | struct | 當前街截至此節點的公開行動序列 |
| prevAction | struct | 前街公開行動序列 |
| facingSize | enum | 面對下注尺度級距（1/4、1/3、1/2、2/3、3/4、1x、overbet…） |
| spr | range | SPR 區間 |
| effectiveStacks | struct | 英雄與相關存活對手的有效籌碼 bucket |
| actions | freq[] | Fold／Check／Call／Bet(sz)／Raise(sz)／All-in 各頻率 |

- 行動頻率合計 = 100%；不合法行動先 mask 再正規化。若合法行動剩餘權重為 0，直接進入 fallback。
- 規則編輯器即時標示條件重疊、被更高優先規則完全遮蔽及不可達規則；使用者必須處理 error，warning 可保留但寫入驗證摘要。

### D.6 下注尺度樹（`betSizeTree`）

**本節是 `betSizeTree` 預設尺度的唯一定義來源**；實做計劃第五章面板 D 的敘述以本節為準。

| 分類 | 預設尺度 |
|---|---|
| Pot 內（≤100%） | 25%／33%／50%／66%／75%／100% |
| Overbet（>100%） | 125%／150%／200% |
| 全下 | All-in |

- **「Overbet」是分類名稱，不是獨立尺度檔位。** 任何 >100% Pot 的尺度即屬 overbet；先前「Overbet（150–300%）」與獨立的 150% 檔位重疊，已移除該寫法。
- 自訂尺度上限 300% Pot；超過 300% 時 UI 建議改用 All-in（實務上多數情境下等價，且避免尺度樹爆炸）。
- 每街可增刪尺度、設定可用的情境。
- 尺度以 %Pot 或 bb 表示，可切換顯示。
- 尺度集合是策略內容的一部分，**變更後既有策略的對應節點需重新對應**；UI 必須在刪除已使用的尺度前警告受影響的節點數。

### D.7 Fallback

- 未命中任何規則，或合法行動 mask 後剩餘權重為 0 的節點，由具版本的 fallback 基準策略補足。
- fallback 命中次數列入報告；策略完整度 = 命中玩家規則的節點數 ÷ 總決策節點數，翻前／翻後及各切片分開統計。

### D.8 驗證與策略完整度

| 檢查 | 規則 |
|---|---|
| 頻率合計 | 每節點行動頻率 = 100% |
| 合法行動 | 不合法行動移除後正規化 |
| 參數區間 | 所有參數在合法範圍 |
| 完整度 | 儲存前顯示「你的策略 翻前 X%／翻後 Y% ＋ 基準 vZ 補足」 |
| Preflop action 維度 | 每個 hand class 的合法 action 頻率合計 100% |
| 規則可達性 | 不得存在完全不可達或資料不完整的規則；遮蔽規則需明確 warning |

### D.9 策略庫：儲存、命名與切換

使用者必須能把自己的策略存下來、命名、之後叫回來用，並在多套策略之間比較。

**策略庫列表**

- 以列表呈現本機全部策略，每列顯示：名稱、`source`（official／custom）、版本、桌型涵蓋、逐桌型完整度（見 D.3）、`baselineVersion`、更新時間。
- 支援搜尋、依名稱／更新時間排序、以及「只看 custom」。
- 列表操作：**開啟**、**重新命名**、**複製為新策略**、**刪除**、**匯出 JSON**、**設為執行用策略**。

**命名規則**

| 規則 | 內容 |
|---|---|
| 必填 | 名稱不可為空，長度 1–60 字 |
| 唯一性 | 名稱在本機策略庫內唯一；重名時即時標紅並提示，不得靜默覆蓋既有策略 |
| 預設名稱 | 由官方基準複製時預設為「`<基準名稱> 的副本`」，游標自動選取以便直接改寫 |
| 可隨時重新命名 | 重新命名不改變 `id`，因此不影響既有 run 對該策略的引用 |

**儲存行為**

- **儲存**：寫回同一 `id`，`version` 自動遞增，`updatedAt` 更新。
- **另存新檔**：產生新 `id` 與新名稱，`baselineVersion` 沿用來源，`version` 重新起算。
- **official 策略唯讀**：直接編輯官方策略時，UI 自動轉為「複製為 custom 後編輯」，並提示已建立副本。
- 儲存前顯示驗證結果與**逐桌型策略完整度**（D.8）；有 error 不得儲存，warning 可儲存但寫入驗證摘要。
- 未儲存的修改固定顯示；切換策略、桌型或離開面板前，必須讓使用者選擇儲存／放棄／返回（UX.2）。
- 自動保存草稿以防當機遺失，但草稿**不算已儲存版本**，不得被 run 引用。

**與 run 的關係（重要）**

- run 開始時，引擎依核心規格 3.3 把該策略的**完整內容快照**寫入 `RunManifest`，不是只存 `id` 或 hash。
- 因此**日後修改或刪除策略，都不影響既有 run 的重播與統計**——已完成的 run 永遠可以重播。UI 應在刪除策略時明確告知這一點，避免使用者誤以為會連帶損毀歷史資料。
- 反之，同一策略的不同版本產生的 run **不得合併統計**；報表的範圍選擇器（F.4）須顯示策略名稱與版本，跨版本選取時標示為不可比。

**匯入匯出**

- 單一策略或整個策略庫皆可匯出為 JSON（M4 交付項）。
- 匯入時驗證 schema 版本與桌型涵蓋；名稱衝突時要求改名，不自動覆蓋。

---

## 面板 E — 執行

### E.1 兩種模式

| 模式 | 使用者座位 | 動畫 | 用途 |
|---|---|---|---|
| **互動對打** | 手動選擇合法行動 | 播放，可 `off`／`fast`／`normal` | 自己下場打 |
| **批次模擬** | 由所選策略自動決策 | **不播放** | 累積統計樣本 |

- 兩種模式共用同一組牌桌與 Bot 設定，切換模式不改變設定。
- UI 只呈現引擎回傳的合法行動與金額區間（核心規格 2.2），**不得自行推導**可下注金額、最小加注額、底池或 side pot。

### E.2 互動對打的行動區

- 行動按鈕依 V.11 呈現，按鈕集合完全來自引擎的合法行動。
- 尺度快選列依 D.6 的 `betSizeTree` 渲染，另附自訂輸入；輸入值超出引擎給的區間即停用送出。
- Bot 回應期間 UI 必須保持可操作；**超過 250 ms 顯示思考狀態**（核心規格 7.2）。

### E.3 批次模擬的進度區

| 顯示項 | 內容 |
|---|---|
| 進度 | 已完成手數／目標手數，含百分比 |
| 速率 | 手／秒，取近期移動平均而非全程平均 |
| 預估剩餘時間 | 依近期速率推算，標示為估計 |
| 桌次 | 目前第幾個桌次、已結束桌次數 |
| 目前 bb/100 | 附 CI 與可判定狀態（F.4）。**不得只顯示點估計** |

- 進度更新頻率上限為每秒數次。逐手更新會讓 UI 執行緒被寫入與重繪吃滿。

### E.4 暫停、續跑與取消

- **暫停確認 ≤ 1 秒；取消確認 ≤ 2 秒**（核心規格 7.2）。
- 暫停／續跑**不得改變最終結果**（核心規格 3.2）。續跑後的逐事件序列必須與不暫停時一致。
- 取消時明確告知已完成的手數會保留、可供檢視與匯出，避免使用者誤以為取消等於全部作廢。
- 批次執行期間，面板 A／B／C 全部鎖定（A.5、B.6、C.6）。

### E.5 執行期間的可用性

核心規格 3.2 要求「使用者可在運算中瀏覽既有報表與 log」。因此批次執行時：

- 面板 F 與 G 保持可用，顯示的是**已完成手數**的資料。
- 面板 F 必須標示「本 run 仍在進行中」，避免使用者把中途數字當成最終結果。
- 寫入採背壓或批次交易，不得讓 log 寫入餓死 UI。

### E.6 完成與失敗

- 完成時顯示總手數、桌次數、總時長與最終 bb/100（附 CI 與可判定狀態）。
- **invariant 未通過的模擬一律不出報表**（實做計劃強制規則 5）。此時顯示失敗原因與該手序，並提供匯出該手 log 供除錯。

> **待業主確認**：批次模擬是否需要「跑到指定條件即停」（例如使用者破產 N 次後停）。目前草案只支援固定手數。

---

## 面板 F — 報表與數據儀表板

> **本面板由原面板 F（報表）與原面板 H（本機方數據儀表板）合併而成**（2026-08-16 業主決議）。自統計主體限縮為「只有使用者座位」後，兩者報告的是同一個母體，欄位重疊約八成，分立已無意義。面板總數自八個減為七個（A／B／C／D／E／F／G），**面板 H 不再存在**，其他文件對「面板 H」的引用一律改指本面板。

### F.1 定位

- 呈現「自身座位」（本機方）的表現：EV／bb100、獲勝手數比例、行為頻率，供策略診斷。
- **統計主體只有使用者座位**（核心規格 5.0）。Bot 的結果不計入策略統計，只在「對照」區塊作為基準顯示，不進入顯著性判定與漏洞旗標。
- 資料來源：本機引擎的逐手輸出聚合 ＋ Equity engine；小型狀態可精確枚舉，大型多人狀態採 deterministic sampling。**不拿逐手盈虧直接下結論，一律揭露樣本數、計算模式與不確定性**。
- **合併後的完整職責**（原面板 F 的內容全部併入本面板，不另設報表頁）：
  - 單次 run 的摘要與跨 run 的深度診斷同頁呈現，以「範圍選擇器」切換資料範圍（本次 run／指定 run／全部 run），不以兩個面板區分。
  - 涵蓋原面板 F 獨有的：總盈虧、顯著性閘門、切片有效樣本、最虧三類情境、策略完整度、桌次卡。
  - 涵蓋原面板 H 獨有的：13×13 EV 熱力圖、逐街分解、對照區塊、逐位置表。

### F.2 指標定義（含公式）

| 指標 | 定義 | 公式 | 單位 |
|---|---|---|---|
| Equity（多人攤牌勝率） | 對該 pot 有資格對手的聯合範圍，套用 reach weight、card removal 與多人 runout；小狀態 exact、大狀態 sampling | Σ P(聯合組合, runout)·share(h)；多人平手 share=1/k | % |
| EV（期望值） | 每手平均淨贏 | EV = (1/N)·Σ Δstack | bb／手 |
| bb/100 | 每百手期望值 | bb/100 = EV × 100 | bb |
| 95% CI | 信賴區間 | [EV − 1.96·SE, EV + 1.96·SE]（naive SE = σ̂/√N；正式依 estimator 選定，見 F.3） | bb |
| All-in EV | 以最後一次 all-in 節點之後的 runout 改用 equity 結算，依 main／side pot 分段計算 | `Σ段(自己在該段的 equity × 該段扣 rake 後底池) − 全部街的自身總投入`（權威定義見核心規格 5.2） | bb |
| 獲勝手數比例（Hands Won %） | 獨贏手數 ÷ 總手數；tie 另列，不混入 wins | wins ÷ N，並顯示 wins／ties／N | % |
| 攤牌獲勝比例（Showdown Won %） | 攤牌獨贏手數 ÷ 進入攤牌手數；tie 另列 | showdownWins ÷ showdowns | % |
| σ（標準差） | 每手結果樣本標準差 | √(Σ(Δ−EV)² ÷ (N−1)) | bb |
| σ₁₀₀ | 每百手標準差 | σ × 10 | bb |
| Max Drawdown | 累計盈虧曲線最大峰谷差 | max(peak − trough) | bb |
| VPIP | 自願投入籌碼進池手數 ÷ 總手數 | — | % |
| PFR | 翻前加注手數 ÷ 總手數 | — | % |
| 3-bet | 翻前再加注次數 ÷ 面對加注機會次數 | — | % |
| C-bet | 翻牌圈下注次數 ÷ 翻牌圈作為翻前攻擊者可下注次數 | — | % |
| Fold to C-bet | 面對 C-bet 棄牌次數 ÷ 面對 C-bet 次數 | — | % |
| WTSD | 進入攤牌手數 ÷ 看到翻牌手數 | — | % |
| W$SD | 攤牌贏手數 ÷ 進入攤牌手數 | — | % |

> **多人正確性**：Equity 與 All-in EV 必須涵蓋聯合範圍、reach weight、card removal、多人 runout、1/k 平分、main／side pot eligibility、rake 與總投入。Complete-hand evaluator 精確；Range Equity 是否 exact 依凍結門檻決定，不得要求 9-max 全節點無條件枚舉。

> **術語鐵則：介面與文件一律不使用「Win Rate」一詞。** 撲克圈慣用的 win rate 指的是 **bb/100**，與本表的「獲勝手數比例」是完全不同的東西，並列出現必然造成業主誤讀。獲勝手數的指標一律稱「獲勝手數比例（Hands Won %）」與「攤牌獲勝比例（Showdown Won %）」；bb/100 一律直接寫 **bb/100**，不加別名。
>
> 另注意「獲勝手數比例」的分母是**總手數**（含棄牌手），9-max 下典型值僅約 10–15%，本身診斷力有限，定位為輔助指標，不列入整體卡的首要結論。

> **All-in EV 的權威公式在 [`9max平台核心規格.md`](9max平台核心規格.md) 5.2**。本表的敘述是同一公式的口語說明；`totalContribution` 已包含全部街的自身投入，實作時不得另外加上「非 all-in 街實際收付」而重複計算。

### F.3 算法對照（指標 → 引擎輸出）

| 指標 | 引擎需輸出的原始資料 | 計算層 |
|---|---|---|
| Equity | 聯合範圍、計算模式（exact/sampling）、reach/card removal、有效樣本、誤差與多人 share | 引擎（Equity engine） |
| EV／bb100／σ／Max Drawdown | 每手 Δstack 序列 | 統計層聚合 |
| All-in EV | all-in 節點：**分段（main／side pot）** equity、各段底池、已投入籌碼、rake | 引擎記錄 ＋ 統計層加總 |
| 獲勝手數比例／攤牌獲勝比例 | 每手 win/lose/tie 與是否攤牌 | 統計層計數 |
| 95% CI | estimator id、cluster／pair id、樣本均值與所需統計量 | 統計層 |
| 行為頻率（VPIP…） | 每個決策節點的 action ＋ context 計數 | 統計層計數 |

> **工程驗收依據**：引擎穩定輸出 RunManifest、每手 Δstack、每節點多人 range/equity 計算描述、每決策 action＋context、all-in 分段結算及 pair/cluster id。UI 只呈現引擎與統計層結果，不重算牌局邏輯。

### F.3.1 Estimator 決策表

| 資料／比較 | 顯示方法 |
|---|---|
| 一般 EV／bb100 | **moving block bootstrap**（block 長度 M0 凍結，桌次邊界為強制斷點），顯示 estimator 名稱 |
| CRN／duplicate A/B | 對配對差值做 CI，不顯示兩次獨立區間相減 |
| 獲勝手數比例／VPIP／PFR 等比例 | 分子／分母＋Wilson interval；有 cluster 時改用 cluster bootstrap |
| Exact Equity | 標示「模型內精確」，不製造 sampling CI |
| Sampled Equity | 顯示 Monte Carlo N 與 CI／誤差上限 |
| 169 格／大量切片 | 預設「探索性」；漏洞旗標使用 BH-FDR 或預先登錄控制 |

### F.4 版面與視覺（GTO Wizard 式）

**版面順序**：範圍選擇器 → 整體卡 → 顯著性摘要 → 逐位置表 → 行為頻率 → 桌次卡 → 13×13 熱力圖 → 逐街分解 → 對照。

- **範圍選擇器**（合併後新增）：切換資料範圍為「本次 run／指定 run／全部 run」，並顯示目前範圍的桌型、策略版本、Bot 組合、engine 版本與時間區間。跨 engine 版本的 run 不得合併統計，選取時直接標示為不可比（核心規格 3.3）。
- **整體卡**：先顯示「可判定／無法判定／樣本不足」，再顯示 EV／bb100＋CI、estimator、**總盈虧**、獲勝手數比例的分子／分母與區間、All-in EV、σ、σ₁₀₀、最大回撤及樣本數。
- **顯著性摘要**（原面板 F）：顯著性閘門結果、切片有效樣本（以 block 數計）、**最虧三類情境**、**策略完整度**（翻前／翻後與逐桌型，見 D.7）。最虧三類情境必須同時顯示樣本數、區間與探索性標籤，不得只依點估計排序後斷言漏洞（核心規格 5.4）。
- **行為頻率**（原面板 F）：VPIP／PFR／3-bet／C-bet／Fold to C-bet／WTSD／W$SD，各顯示分子／分母與 Wilson interval。
- **逐位置表**：列 = 位置，欄 = EV／bb/100／獲勝手數比例／VPIP／PFR。**必須依當手在桌人數切片**；使用者在 9 人桌的 BTN 與在 7 人桌的 BTN 是不同母體，不得合併為同一列。切片的有效樣本以 block 數計。
  - **位置標籤**取自引擎依 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4.1 寫入每手 log 的欄位，不自行推導。
  - **dead button／dead small blind 手預設排除**於逐位置表，並在表頭顯示被排除的手數與占比。這些手少收一份強制下注，底池結構與一般手不同。使用者可切換為納入，切換狀態必須顯示於切片脈絡。
  - 整體卡的 EV／bb100 **不排除** dead 手；排除只作用於逐位置切片。
  - 切片組合最多 30 種（在桌人數 6–9 × 各自位置數），樣本碎片化嚴重；每列必須顯示自己的有效樣本與可分辨差距，不得只在表尾寫一次。
- **桌次卡**：桌次數、平均存活手數、結束原因分佈（使用者破產／人數不足／達手數上限）、補位次數。使用者的桌次存活手數本身是策略強弱的觀測值。
- **13×13 EV 熱力圖**：每格可切換 EV／勝率／使用頻率；顯示值、樣本數、CI 狀態與探索性標示；提供色階圖例及非紅綠的色盲可辨識方案。
- **逐街分解**：preflop／flop／turn／river 的 EV 與頻率長條。
- **對照**：自身 vs 全桌 Bot 平均的 EV 差距與剝削幅度。

### F.5 樣本不足與誠實揭露

| 規則 | 內容 |
|---|---|
| 灰階標示 | 樣本低於閾值的手牌格灰階 ＋ 「樣本不足」 |
| CI／區間一律顯示 | EV／bb100 顯示 95% CI；比例顯示分子／分母與適用區間；sampled Equity 顯示 MC 誤差 |
| 跨 0 標示 | CI 跨 0 → 「本樣本無法判定優劣」 |
| 可分辨差距 | 顯示「此手數可分辨的最小差距」 |
| 不假精確 | 對外最多一位小數；內部保留完整精度 |
| 熱力圖定位 | 13×13 熱力圖標示為**探索性**，不對單格做優劣宣稱；漏洞旗標一律走 BH-FDR 或預先登錄控制 |

### F.5.1 執行前的統計效力預覽（面板 A 連動）

誠實揭露不能只發生在報表。使用者在**面板 A 設定手數時**，UI 必須即時顯示該設定的預期效力，否則業主會在跑完 10K 手後看到滿版「無法判定」而認為產品有問題。

**面板 A 手數滑桿旁必須即時顯示**：

| 顯示項 | 內容 |
|---|---|
| 整體 bb/100 預期 CI 半寬 | 依核心規格 5.3.1 的公式與目前的 σ 估計值計算，例：「10K 手 → 約 ±18 bb/100」 |
| 可分辨差距 | 「此手數可分辨約 X bb/100 以上的差距」 |
| 逐位置切片效力 | 「逐位置表預期 N 列可判定／共 M 列」 |
| 熱力圖效力 | 「13×13 熱力圖預期 K 格達樣本門檻」，K 為 0 時直接標示「本手數下熱力圖僅供探索」 |

- σ 估計值優先取**使用者自己的歷史 run**；無歷史時用 M0 凍結的預設值，並標示為預設估計。
- 預覽是估計值，必須標示「估計」，不得呈現為保證。
- 使用者選擇低於「整體 bb/100 可判定」所需手數時，顯示提示但**不阻擋執行**——探索性短 run 是合法用途。

---

## 面板 G — Log 檢視器與重播

### G.1 版面

- **左側逐手列表**（虛擬捲動），右側為選定手的牌桌重播與決策細節。
- 列表與細節皆為唯讀。log 是既成事實，不提供編輯。

### G.2 逐手列表的欄位

| 欄位 | 內容 | 說明 |
|---|---|---|
| 手序 | `#0`–`#999999` | 全域遞增，跨桌次不重設 |
| 桌次 | 第幾個桌次 | 桌次邊界在列表上以分隔線標示 |
| 在桌人數 | 6–9 | 逐位置統計依此切片，列表也需可見 |
| 使用者位置 | 位置 label | 取自引擎寫入該手 log 的欄位，UI 不自行推導（同 B.3） |
| 底牌 | 緊湊牌方塊（V.9） | 使用者自己的底牌恆可見 |
| 公共牌 | 緊湊牌方塊 | 翻前結束的手顯示「未發」 |
| 損益 | bb，正負著色（V.3） | 取自儲存層寫入的 `hero_delta`，**不由 payouts 重算** |
| 標記 | dead button／dead SB／補位 | 這些手的底池結構與一般手不同，需可辨識 |

### G.3 篩選與搜尋

| 篩選 | 說明 |
|---|---|
| 桌次 | 單一或範圍 |
| 在桌人數 | 多選 |
| 使用者位置 | 多選，使用 8.4.1 的標籤字串 |
| 街別 | 打到哪一街結束 |
| 損益 | 大於／小於某值 |
| 手牌類別 | 169 類擇一或多選 |
| dead 手 | 只看／排除 |

- **100 萬手 DB 載入指定手牌 p95 ≤ 200 ms**（核心規格 7.2），冷啟動與 warm cache 分開記錄。
- 篩選後顯示「符合 N 手／共 M 手」，避免使用者誤把篩選結果當成全體。

### G.4 重播器

- 速度 **1x–10x**，可暫停與逐步。
- 逐街、逐座位呈現行動與尺度，行動編碼依 V.7。
- 底池、邊池與各座籌碼隨行動更新，**數值一律取自 log 的事件，不由 UI 重算**。
- 重播與原執行必須逐事件一致；不一致即為引擎或 log 格式的缺陷，應報錯而非靜默容忍。

### G.5 底牌可見範圍

- **預設顯示全部底牌**（核心規格 2.4）。複盤的用途就是看清楚每個位置手上是什麼；遮住等於讓分析工具失去意義。
- 提供「只顯示實際亮出的底牌」開關，切到該模式時未亮牌的座位顯示佔位方塊，供使用者驗證自己的讀牌。
- 遮蔽仍發生在 IPC 邊界：切到限制模式時，未亮出的底牌不會傳到前端，開關實際切換的是請求參數。
- 這與隱藏資訊隔離不衝突。隔離的約束對象是**引擎的決策路徑**（核心規格 2.4）：牌桌上的 Bot 看不到他人底牌，桌外複盤的人看得到。

### G.6 逐行動原因

選定某個行動後，顯示該決策的完整 trace（C.5 的七步）。這是 Bot 行為可解釋性的唯一入口。

- 使用者自己的行動顯示所用策略節點與命中的規則；走 fallback 時明確標示 fallback 版本與原因（無規則命中／合法行動被遮蔽光）。
- 保留層數受該座 `explanationDepth` 影響；層數不足時顯示「該 run 未保留此層」，不臆測補齊。

### G.7 匯出與清理

- 匯出 JSON：單手、篩選結果或整個 run。
- 依日期範圍刪除 run。刪除前顯示將刪除的 run 數與手數，並要求二次確認。
- 全量保留為預設，不自動清理。

> **待業主確認**：逐手列表是否需要「加註記／標星」功能，讓業主標出想回頭討論的手。目前草案未含此功能。

---

## UX 效能驗收

所有正式數字只接受在 **Intel Core Ultra 7 258V／32GB 或更低規格筆電**測得的結果；不得使用更高規格替代。門檻沿用 [`9max平台核心規格.md`](9max平台核心規格.md) 第七章，特別包含：

- 參數輸入與即時驗證 p95 ≤100ms；面板與已載入篩選 p95 ≤250ms。
- 聚合圖表更新 p95 ≤500ms，較久時顯示 loading／progress。
- 100 萬手 DB 載入指定手牌 p95 ≤200ms。
- 互動 Bot 回應 p95 ≤1 秒；動畫 p95 frame time ≤33ms，且可關閉。
- 批次運算期間仍須通過參數、報表、暫停與取消門檻。
