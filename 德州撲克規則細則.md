# 德州撲克規則細則（引擎實作用）

> 版本：v1.2　｜　日期：2026-08-16　｜　狀態：**現行權威規格（Normative）**
> 上層文件：[`9max平台核心規格.md`](9max平台核心規格.md) 第二章的規則附則
> 適用：No-Limit Texas Hold'em，現金桌（cash game）規則

本文件把核心規格第二章「必須由引擎的合法行動產生器統一決定」的部分，補成**可實作、可驗收**的條文。依業主 2026-08-15 指示，**牌局規則完全比照現實德州撲克**；規則來源以 TDA（Tournament Directors Association）現行規則與通行現金桌房規為準。

與核心規格衝突時，牌局規則以本文件為準；產品邊界、統計與效能仍以核心規格為準。

---

## 一、下注結構（No-Limit）

### 1.1 名詞

| 名詞 | 定義 |
|---|---|
| 當前下注額（current bet） | 本街目前最高的單人累計投入額 |
| 加注增額（raise increment） | 本街最近一次完整加注造成的「當前下注額」增加量 |
| 完整加注（full raise） | 加注後金額 ≥ 當前下注額 + 加注增額 |
| 短額全下（incomplete all-in） | 全下後金額 > 當前下注額，但 < 完整加注門檻 |

### 1.2 最小加注額

- **翻前**：當前下注額初始為最大的強制下注（BB，或存在 straddle 時為最大的 straddle）。加注增額初始 = 該金額。
- **翻後**：當前下注額初始為 0；首次下注最小為 1 BB，加注增額初始 = BB。
- 每次完整加注後，加注增額更新為該次的實際增加量。

> 範例（盲注 1/2，無 straddle）：UTG 最小加注 to 4（增額 2）。若 UTG raise to 7（增額 5），下一位最小 raise to 12（7 + 5），不是 to 9。

> 範例（盲注 1/2，UTG straddle to 4）：當前下注額 4，增額 4，首次加注最小 to 8。

### 1.3 短額全下不重開加注權（關鍵條文）

引擎必須實作 TDA 標準：

- 短額全下**不重新開放**加注權給「已對當前下注額行動過」的玩家。這些玩家面對該短額全下時，合法行動只有 **fold** 或 **call（補足差額）**，**不得 raise**。
- 尚未行動、或尚未面對過當前下注額的玩家，加注權不受影響。
- **多個短額全下的增額不累加**成完整加注。
- 若全下金額達到完整加注門檻，正常重開所有人的加注權，並更新加注增額。

> 範例（盲注 1/2）：A raise to 10（增額 8），B call 10，C all-in to 15（增額 5 < 8，短額）。
> 此時 A 已對 10 行動過 → A 只能 fold 或 call 到 15，**不能再加注**。B 同理。
> 若 C all-in to 18（增額 8，達門檻）→ A、B 的加注權重新開放。

引擎因此必須逐玩家追蹤「本街最後面對的下注額」與「該玩家行動後是否發生過完整加注」，不能只看當前下注額。

### 1.4 籌碼不足

- 籌碼不足以 call 全額時，全下為部分 call，建立 side pot。
- 籌碼不足以做完整加注時，只能選擇短額全下（適用 1.3）或 fold。

### 1.5 其他行動規則

- 允許 check-raise（現實規則允許，不設限）。
- 一街之內，行動持續到「所有未棄牌且未全下的玩家，對當前下注額都已行動且投入相等」為止。
- 僅剩一名未全下玩家時，該街結束，其餘公共牌直接發完並攤牌。

---

## 二、翻前強制下注

### 2.1 盲注

- SB、BB 為 **live blind**，翻前保有行動權。
- 無人加注時，BB 擁有 **option**：可 check 結束翻前，或加注。
- 桌型範圍為 6-max～9-max（預設 9-max）。6-max 以下不在產品範圍內，引擎不需要 5-max 以下的位置映射。

### 2.2 Ante 四種模式

| 模式 | 定義 |
|---|---|
| `none` | 不收 ante |
| `perPlayer` | 每名在座玩家各付 ante |
| `bbAnte` | 由 BB 位一人代付全桌 ante 總額 |
| `btnAnte` | 由 BTN 位一人代付全桌 ante 總額 |

**共同規則**：

- Ante 於發牌前投入，屬 **dead money**，**不計入該玩家的當街下注額**，因此不影響任何人的 call 金額或最小加注額。
- Ante 全額進 main pot。

**籌碼不足的處理（現實規則）**：

- 玩家籌碼不足以付全額 ante → 以剩餘籌碼全下付 ante，仍然發牌，並依實際投入取得對應的 pot 分層資格。
- `bbAnte`／`btnAnte` 時，付款玩家籌碼不足以同時付 ante 與 blind：**先付 ante，剩餘籌碼作為 blind**。
- 其餘玩家的 ante 義務不因代付者籌碼不足而改變（`bbAnte`／`btnAnte` 模式下代付者付不出的部分即為短收，不向他人追補）。

> 註：「ante 先或 blind 先」在現實各房規有分歧。本規格採 **ante 優先**並寫死；若業主要求改為 blind 優先，改本節一行即可，但必須同步更新驗收測試向量。

### 2.3 Straddle

**基本規則**：

- Straddle 是**發牌前**投入的 **live blind**，成為新的當前下注額。
- 最後一個（金額最大的）straddle 位取得翻前 **option**：翻前行動從其**左邊第一位**開始，該位擁有翻前最後行動權。
- **翻後行動順序不受 straddle 影響**，一律從按鈕左側第一位仍在牌局的玩家起算。

**金額規則**：

- UTG straddle = 2 × BB。
- Re-straddle（double straddle）必須為**前一段的 2 倍**（非「大於前一段」）。
- 核心規格 2.1 `straddle.amounts` 已同步採用此規則。

**位置規則**：

- `straddle.seats` 指定的座位必須從 UTG（BB 左邊第一位）起**連續**，不得跳過座位。
- 允許 BTN straddle（Mississippi straddle）作為具名變體：此時翻前行動從 BTN 左邊（SB）開始，BTN 持有翻前 option。若採用，須在 `straddle.mode` 另立具名 enum，不得與一般 straddle 混用。

**與最小加注的交互**：加注增額初始 = 最大 straddle 金額（見 1.2）。

---

## 三、行動順序

| 街別 | 起始行動者 |
|---|---|
| Preflop（無 straddle） | BB 左邊第一位（UTG） |
| Preflop（有 straddle） | 最大 straddle 位的左邊第一位 |
| Flop／Turn／River | 按鈕左側第一位仍在牌局的玩家（一般為 SB） |

- 位置輪轉以 **BB 位每手前進一個在桌玩家**為基準，SB 與按鈕依序回推，可能出現 dead small blind 與 dead button。完整規則見 8.4。
- Dead button 時，翻後起始行動者仍為「按鈕**位置**左側第一位仍在牌局的玩家」，最後行動權屬於按鈕位置右側最近的在牌局玩家。

---

## 四、攤牌、亮牌與 muck

### 4.1 亮牌順序（現實規則）

- 最後一次**主動下注或加注**的玩家先亮牌。
- 若最後一街無人下注（全部 check），從按鈕左側第一位起順時針亮牌。

### 4.2 Muck（對資訊模型的影響，必須實作）

- 依現實規則，輸家可 **muck** 而不亮牌。只有實際亮出的底牌才是公開資訊。
- **引擎的 log 保存完整底牌**，但 `DecisionView`、對手 range estimate 與 Bot 決策路徑**只能取得依現實規則實際公開的底牌**。
- Muck 政策採具名設定，寫入 `RunManifest`：
  - `alwaysShow`：全部攤牌亮牌（資訊最多，偏離現實）
  - `realistic`（**預設**）：依現實規則，只有必須亮牌者與主動選擇亮牌者公開
- 此設定會改變對手模型可用的資訊量，因此屬於 run 設定的一部分，不同設定的統計結果不得混用比較。

> 這是「規則完全符合現實」對引擎的實質約束：不是顯示問題，是資訊集問題。

---

## 五、分池

### 5.1 Side pot

- 依各玩家的實際總投入分層建立 main pot 與各 side pot，每層保存參與資格。
- 已棄牌玩家的投入留在對應層的 pot，但不具分配資格。
- 分配由最外層（最後形成的 side pot）往內、或由 main pot 往外皆可，結果必須一致。

### 5.2 Split pot 與 odd chip

- 同一 pot 內並列最佳牌者均分該 pot。
- 無法整除的 odd chip，從**按鈕左側**起、在該 pot **有分配資格**的最近座位順時針分配（符合 TDA 標準，核心規格 2.3 已定）。
- 每個 odd chip 的去向寫入事件 log。

---

## 六、Rake

沿用核心規格 2.1：

- 以整手可抽水底池計算，先依比例向下取整至最小籌碼單位，再套用每手 cap。
- `noFlopNoDrop` 為真且該手未發出 flop 時，不抽水。
- Rake 從 pot 扣除，贏家實得為扣後金額；main／side pot 分配不得重複扣除 rake。

---

## 七、明確不實作的現實規則（含等價性說明）

| 現實規則 | 處置 | 理由 |
|---|---|---|
| Burn card（每街發牌前燒一張） | **不實作** | 牌堆為均勻隨機排列時，燒牌與不燒牌的機率分布完全等價；實作只會增加 RNG stream 消耗且無資訊價值。此決定寫入 `RunManifest`。 |
| Run it twice | **不實作** | 現金桌選項，需雙方合意；v1 產品範圍外。 |
| Chop the blinds（分盲） | **不實作** | 需玩家協議，模擬器無此互動。 |
| String bet／口頭宣告／單一動作規則 | **不適用** | UI 為離散合法行動選擇，物理性下注動作不存在。 |
| Time bank／超時自動棄牌 | **不實作** | 單機模擬無時間壓力；互動模式不設決策時限。 |
| Rabbit hunting、show one show all | **不實作** | 不影響牌局結果與資訊集。 |

上述每一條都必須在 `RunManifest` 的規則變體欄位留下明確紀錄，不得以「預設行為」帶過。

---

## 八、籌碼 session 政策（業主 2026-08-15 更新決議）

現實德州撲克的規則不規定玩家破產後要不要補碼，補不補、補多少在現實中是玩家自由，與發牌、比牌的規則無關。因此「規則完全比照現實」無法回答這一題，模擬器必須自選一種並寫死。

**決議：破產離桌（`bustOut`）。** 籌碼歸零的玩家離桌，桌上人數隨之減少，剩下的人繼續打。

### 8.1 條文

- 每手結束結算後，籌碼為 0 的玩家離桌，不再參與後續手牌。
- **不實作**既有玩家的補碼。離桌的玩家不會帶著原有身分回到桌上。
- 籌碼跨手結轉。`stackBySeat`（預設 200BB）只是**桌次開始時**的起始深度，不是每手的重置值。
- 離桌造成的位置變動依 8.4 的 dead button／dead blind 規則處理。
- 政策名稱寫入 `RunManifest`。

### 8.1.1 自動補位（`autoRefill`）

- 可設定開關。開啟時，Bot 離桌後於**下一手開始前**補入新 Bot，把在桌人數補回 `autoRefill.targetPlayers`。
- `targetPlayers` 範圍 6～9，**下限恆為 6**，且不得大於開桌人數 `players`。
- 新 Bot **沿用該座位既有的人格與行為參數設定**。Bot 組合在整個 run 中維持不變，`RunManifest` 的 Bot 快照因此仍然成立；不得隨機指派人格，否則 run 不可重現也不可比較。
- 新 Bot 以該座位設定的起始深度買入。
- 補位發生在手與手之間，不得在一手進行中插入玩家。
- 補位事件寫入事件 log（座位、手序、買入深度），供重播還原。
- 關閉自動補位時，桌上人數只減不增，降到 6 人以下即結束桌次（見 8.3）。

### 8.2 起始籌碼深度

- `stackBySeat` 以整數 BB 保存，**預設值為 200 BB**。可調整：全桌統一設定，每座亦可獨立覆寫。
- 開局後籌碼即開始漂移，因此起始深度只約束**開局狀態**，不約束中局的有效籌碼。中局的策略索引依 8.5 的 bucket 判定。

> **200BB 起始的三個連帶後果**（實作與內容規劃須一併考慮）：
>
> 1. **桌次變長**。籌碼更深，破產更慢，一個桌次的存活手數會明顯高於 100BB 起始。桌次數量因此減少，作為統計 cluster 的效力更弱——estimator 更倚賴 moving block bootstrap（見 8.6）。
> 2. **bucket 需要更高的上界檔位**。全桌開局都是 200BB，若沿用原本以 `[125,∞)` 收尾的分檔，開局狀態會全部擠在同一桶且中局深籌碼無區分度。分檔已於 8.5 擴充。
> 3. **postflop 更複雜**。200BB 深度下 SPR 普遍偏高、多街博弈更長，postflop 規則集與 preflop baseline 的內容量都會高於淺籌碼設定。

### 8.3 桌次（table instance）與 run 結構

**桌次結束條件（滿足任一即結束）**：

1. **使用者座位破產離桌。** 使用者是全部統計的主體，沒有使用者的牌局不產生有效資料。桌次結束後開新桌次，使用者以起始深度重新入座。
2. **自動補位關閉，且在桌人數降到 6 以下。** 核心規格 1.2 明定最低為 6-max，不得繼續打成 5 人以下。
3. 達到 run 的手數上限。

**座位數不變量（引擎斷言）**：

> **任何一手開始時，在桌人數必須 ≥ 6。**

上一手結算、離桌處理與補位處理全部完成後，引擎在發牌前斷言此條件。開啟自動補位時由補位滿足（補至 `targetPlayers`），關閉時由條件 2 結束桌次滿足。「在桌人數可能短暫低於 6」只描述**手與手之間**的瞬時狀態，**不得**被實作成發出一手 5 人以下的牌。此斷言與籌碼守恆同級，屬規則層不變量，失敗即中止 run 並不出報表（核心規格強制規則 5）。

**桌次開始時**：所有座位回到 `players` 設定的人數與起始深度，按鈕位置重置，Bot 組合與策略不變。

**run 結束條件**：累計手數達到 `handLimit`，或使用者取消。

**RNG**：每個桌次由 master seed 與桌次索引派生獨立 stream（核心規格 3.4）。桌次邊界與補位事件必須寫入事件 log 與 `RunManifest`，重播需能還原到指定桌次的指定手。

> 開啟自動補位時，桌次只會因使用者破產而中斷，一個 run 可能只有少數幾個桌次，甚至只有一個。這會讓「桌次」失去作為統計 cluster 的效力，estimator 因此改以 moving block bootstrap 為主（見 8.6）。
>
> 關閉自動補位時，一桌會逐步降到 6 人以下而結束桌次。**200BB 起始比淺籌碼設定更耐久**，單一桌次的存活手數會拉長、桌次總數相應減少（在 100BB 設定下約為數百到數千手一桌，200BB 會更長）。實際衰減曲線由 M0 實測，因為它同時決定三件事：批次的開桌收桌開銷（核心規格 7.3 的時間門檻）、統計上可用的桌次 cluster 數量（8.6），以及 UI 規格 D.3 的「未校準桌型手牌比例」預估。

### 8.4 Dead button 與 dead blind（現實規則，因本決議而必須實作）

玩家離桌後，位置輪轉不能只是「按鈕移到下一個在桌座位」。依 TDA 標準：

1. **BB 位每手往順時針方向移動到下一個仍在桌的玩家。任何玩家都不會跳過 BB。**
2. **SB 位為 BB 位的前一個座位。** 若該座位的玩家已離桌，該手**不收 SB**（dead small blind）。
3. **按鈕位為 SB 位的前一個座位。** 若該座位的玩家已離桌，該手為 **dead button**，無人持有按鈕。
4. Dead button 時，翻後行動仍從按鈕位左側第一位仍在牌局的玩家起算；最後行動權屬於按鈕位右側最近的在牌局玩家。
5. 同一玩家不會因他人離桌而連續兩手付 BB。

Odd chip 的「按鈕左側起」在 dead button 時，以按鈕**位置**（而非持有者）左側起算。

### 8.4.1 位置標籤（統計切片與策略索引用）

8.4 決定的是**行動順序**。統計切片（核心規格 5.3、UI 規格 F.4）與策略節點索引（核心規格 4.1）需要的是每個玩家的**位置標籤**，兩者必須分別定義，否則 dead button 手的歸屬無法判定。

**產生規則**（每手發牌前計算一次，寫入該手 log）：

1. 先依 8.4 決定 BB 位、SB 位、按鈕位三個**座位**。
2. BB 位座位上的玩家標 `BB`。此標籤恆存在（BB 位定義為下一個在桌玩家）。
3. SB 位座位若有在桌玩家，標 `SB`；若無，該手為 dead small blind，**無任何玩家標 `SB`**。
4. 按鈕位座位若有在桌玩家，標 `BTN`；若無，該手為 dead button，**無任何玩家標 `BTN`**。
5. 其餘在桌玩家（設共 k 位）依下列規則取得標籤——**按鈕端與最早位雙向錨定**：
   - 最靠近按鈕者 = `CO`；次靠近 = `HJ`；再次 = `LJ`（**僅當 k ≥ 4**）。
   - **最早的一位恆為 `UTG`**（2026-08-16 定案：最早位一律叫 UTG，不叫 LJ）。
   - `UTG` 與 `LJ` 之間若仍有剩餘位置，自 `UTG` 往按鈕方向依序為 `UTG+1`、`UTG+2`。

此規則在無 dead 的情況下與 [`UI面板詳細規格.md`](UI面板詳細規格.md) D.4 的位置集合表完全一致：

| k（非盲注非按鈕人數） | 在桌人數 | 由早到晚的標籤 |
|---|---|---|
| 6 | 9 | UTG／UTG+1／UTG+2／LJ／HJ／CO |
| 5 | 8 | UTG／UTG+1／LJ／HJ／CO |
| 4 | 7 | UTG／LJ／HJ／CO |
| 3 | 6 | UTG／HJ／CO（**k=3 時無 `LJ`**） |

dead button／dead SB 會使 k 增加（該座位的標籤空出但玩家仍在），標籤依同一規則產生；例如 7 人在桌且 dead button 時 k=5，標籤為 UTG／UTG+1／LJ／HJ／CO，該手無人持有 `BTN`。

此組字串是引擎、策略內容（preflop baseline 的欄位鍵）與 UI 三方共用的唯一命名，不另設別名或顯示用轉換表。

**每手另記錄兩個布林旗標**：`deadButton`、`deadSmallBlind`。

**對統計的處理**：

- 逐位置表**預設排除** `deadButton` 或 `deadSmallBlind` 為真的手，並顯示被排除的手數。這些手少收一份強制下注，底池結構與一般手不同，混入會污染逐位置比較。
- 使用者可切換為納入，切換狀態必須顯示在切片脈絡中。
- 整體 bb/100 **不排除** dead 手；排除只作用於逐位置切片。

**對策略索引的處理**：

- 策略節點**不**因 dead 旗標細分。dead 手沿用同位置標籤的策略內容，避免 baseline 內容量翻倍。
- 該手的決策 trace 標記「使用近似策略（dead blind 情境）」，並計入策略完整度的說明欄，不計為 fallback。

### 8.5 有效籌碼 bucket

籌碼跨手漂移後，任一手的有效籌碼可能落在 0 到全桌總籌碼之間的任意值（9 人 × 200BB = 1800BB），單一起始深度不足以索引策略。因此：

- 策略節點的 `effectiveStackBucket` 依**分檔區間**判定，不依起始深度。
- 分檔邊界於 **M0 凍結**，與 exact/sampling 門檻一併定案（核心規格 5.1）。
- 邊界一律採**左閉右開半開區間**，避免邊界值同時落入兩桶。邊界建議值（M0 待實測確認）：

`[0,15)`／`[15,25)`／`[25,40)`／`[40,70)`／`[70,110)`／`[110,160)`／`[160,240)`／`[240,400)`／`[400,∞)` BB

- **上界檔位是 200BB 起始的必要調整**。原方案以 `[125,∞)` 收尾，在 200BB 起始下會把「開局的 200BB」到「贏遍全桌的 1800BB」全部歸為同一桶，深籌碼博弈完全失去索引解析度。現行分檔讓 200BB 落在 `[160,240)`，並保留兩個更深的檔位。
- **代價**：bucket 由 6 檔增為 9 檔，preflop baseline 的內容格數同比例上升（見 M0 閘門 2 的重新估算）。若 M0 實測顯示深籌碼檔位的策略差異不足以支撐獨立內容，可合併 `[240,400)` 與 `[400,∞)`，但須在凍結時明確記錄理由。
- 有效籌碼以「英雄與該節點相關對手之中的較小者」計算，並在四捨五入前先換算為 BB 的定點值；bucket 判定使用該定點值，不得先取整再判定。
- 牌手顧問的 preflop baseline 必須涵蓋**全部 9 個 bucket**，不只開局深度所在的那一檔。落在無對應內容的 bucket 時進入 fallback，並計入策略完整度（UI 規格 D.7）。

### 8.6 對統計的影響

**統計主體只有使用者座位**（核心規格 5.0）。Bot 的破產、離桌與補位都不改變統計母體，因為母體自始至終是使用者的手牌序列。Bot 的聚合數字只作為對照顯示，不進入顯著性判定。

| 項目 | 因本決議產生的要求 |
|---|---|
| Estimator | **moving block bootstrap** 為主，block 長度依 M0 實測自相關長度凍結；桌次邊界為強制 block 斷點。開啟自動補位時桌次可能只有一個，不得預設以桌次做 cluster bootstrap |
| 逐位置報表 | 位置定義隨在桌人數改變。使用者在 9 人桌的 BTN 與在 7 人桌的 BTN 不可合併統計，逐位置表必須**依當手在桌人數切片**（UI 規格 F.4） |
| 有效樣本 | 以 block 數計，不以原始手數計；block 數不足時標示樣本不足 |
| 補位的副作用 | 新 Bot 以起始深度買入，會改變桌上的有效籌碼分佈。該手的 `effectiveStackBucket` 依實際籌碼判定，不因「剛補位」而特別處理 |
| 桌次長度 | 桌次存活手數本身是觀測值，須列入報表（反映策略強弱） |

### 8.7 side pot

逐座覆寫不同深度或籌碼漂移後，各座有效籌碼自然不等，多層 side pot 會常態出現，R9／R10 的驗收情境可直接由一般對局產生。`bbAnte`／`btnAnte` 造成的極小 side pot 為預期行為。

---

## 九、引擎驗收測試向量

M1 必須包含下列可人工驗算的案例，全部通過才算規則層完成：

| # | 案例 | 驗收點 |
|---|---|---|
| R1 | 1/2，UTG raise to 7，次位最小加注 | 必須為 to 12，非 to 9 |
| R2 | 1/2，A raise to 10、B call、C all-in to 15 | A、B 只能 fold/call；raise 不在合法行動集內 |
| R3 | 承 R2，C all-in to 18 | A、B 的 raise 重新出現在合法行動集內，最小 to 26 |
| R4 | 兩個連續短額全下（to 13、to 15） | 增額不累加，已行動者仍不得加注 |
| R5 | 1/2 + UTG straddle to 4 | 當前下注額 4；行動從 straddle 左邊起；straddle 持有 option；最小加注 to 8 |
| R6 | 1/2 + straddle to 4 + re-straddle to 8 | re-straddle 必須恰為 2 倍；行動從 re-straddle 左邊起 |
| R7 | `bbAnte`，BB 籌碼 < ante + BB | ante 先付足，餘額為 blind；該玩家 pot 資格分層正確 |
| R8 | `perPlayer` ante，某座 all-in for ante | 該座仍發牌；ante 為 dead money 不影響他人 call 額 |
| R9 | 三人不等深度全下（以逐座覆寫設為三種不同起始深度，或由跨手漂移自然產生） | main + 2 side pot 分層正確；棄牌者投入留在 pot 但無資格 |
| R10 | 三人 split，pot 無法整除 | odd chip 由按鈕左側起、有資格者順時針取得；log 記錄去向 |
| R11 | 河牌全 check 後攤牌 | 亮牌從按鈕左側起；`realistic` 模式下輸家可 muck |
| R12 | 河牌 A 下注、B call | A 先亮牌；B 敗則可 muck，未公開底牌不得進入 `DecisionView` |
| R13 | 任一案例的籌碼守恆 | 開始籌碼總額 = 結束籌碼總額 + 該手 rake，誤差為 0 |
| R14 | `noFlopNoDrop` 開啟且翻前結束 | 該手 rake = 0 |
| R15 | SB 位玩家於前一手破產離桌 | 該手 dead small blind，不收 SB；BB 位仍前進一個在桌玩家 |
| R16 | 按鈕位玩家於前一手破產離桌 | 該手 dead button；翻後行動起點與最後行動權依 8.4 第 4 條判定 |
| R17 | 連續多人離桌 | 無任何玩家連續兩手付 BB；BB 不被跳過 |
| R18 | 自動補位關閉，在桌人數由 6 降至 5 | 桌次立即結束，不得打成 5-max；桌次邊界寫入 log |
| R19 | 使用者座位破產 | 桌次立即結束，即使 Bot 尚有籌碼；新桌次使用者以起始深度重新入座 |
| R20 | 桌次全生命週期的籌碼守恆 | 桌次起始總籌碼 + 補位買入總額 = 結束時在桌總籌碼 + 該桌次累計 rake，誤差為 0（破產離桌者帶走 0 籌碼，故不另立項） |
| R21 | 自動補位開啟，Bot 破產離桌 | 下一手開始前補入新 Bot，人數回到 `targetPlayers`；新 Bot 沿用該座位的人格與行為參數及起始深度；補位事件寫入 log |
| R22 | 自動補位開啟，同一手內多名 Bot 破產 | 補位一次補足至 `targetPlayers`，且全部發生在手與手之間，不得在手內插入玩家 |
| R23 | 補位後重播 | 依 log 還原補位時點與新 Bot 設定，逐事件與原執行一致 |
| R24 | dead button 與 dead SB 手的位置標籤 | 依 8.4.1 產生：`BB` 恆存在；dead 時無人取得 `SB`／`BTN`；其餘玩家按鈕端為 CO／HJ／LJ、最早位恆為 `UTG`、中間填 `UTG+1`／`UTG+2`；k=3 時無 `LJ`；`deadButton`／`deadSmallBlind` 旗標寫入該手 log |
| R25 | 每手開始的座位數不變量 | 任一手發牌前在桌人數 ≥ 6；補位開啟時由補位滿足，關閉時桌次已先結束。斷言失敗即中止 run |

---

## 十、對核心規格的修正紀錄（已完成，僅供追溯）

> 本章是**歷史紀錄**，不是待辦清單。下列各項均已回填至 [`9max平台核心規格.md`](9max平台核心規格.md)；閱讀規格時一律以核心規格現行條文為準，不得依本章推論核心規格仍有錯誤。

**核心規格 v1.0 已完成**：

| # | 修正項 | 狀態 |
|---|---|---|
| 1 | 2.1 `straddle.amounts`：「後一段必須大於前一段」→「後一段必須為前一段的 2 倍」 | ✅ 已回填 |
| 2 | 2.2 補上「規則內容依本文件第一至三章」 | ✅ 已回填 |
| 3 | 2.3 錯字「开始籌碼總額」→「開始籌碼總額」 | ✅ 已回填 |
| 4 | 2.4 補上 muck 政策對 `DecisionView` 的約束（本文件 4.2） | ✅ 已回填 |
| 5 | 2.1 補上籌碼 session 政策欄位（`stackPolicy`） | ✅ 已回填 |

**核心規格 v1.1 已完成**：

| # | 修正項 | 狀態 |
|---|---|---|
| 6 | 2.1 桌次結束條件：「在桌人數降至 2」→「自動補位關閉且降至 6 以下」，與本文件 8.3 一致 | ✅ 已回填 |
| 7 | 2.1 `stackPolicy`：「桌上人數只減不增」補上「關閉自動補位時」前提，消除與 8.1.1 自動補位的矛盾 | ✅ 已回填 |
| 8 | 2.1 新增座位數不變量（每手開始 ≥6 人），對應本文件 8.3 與 R25 | ✅ 已回填 |
| 9 | 2.2 補上位置標籤與行動順序分離，指向本文件 8.4.1 | ✅ 已回填 |
