# 0907 實作審查：32bbdd4

日期：2026-09-09  
範圍：`c7afc11..32bbdd4`，包含前次報表修正、0907 Stage 0–6 與快照還原。  
結論：本次確實已有各階段實作，但以下問題會改變實際策略或還原結果，建議修正後再驗收。本次未修改產品程式。

## 1. [P1] 英雄翻後覆寫套用到所有對手 Bot

位置：`apps/engine/src/bot/agent.rs:252–254`，另見 `apps/ipc/src/run.rs` 安裝策略的路徑。

`execute()` 將英雄規則集裝到同一個 `BotAgent`，而 `resolve_postflop()` 對每個行動座位都使用這份規則集。`hero_seat` 的判斷只限制 coverage 計數，沒有隔離規則解析。對手命中 `UserOverride` 時也會進入絕對覆寫的中性人格管線。

例如英雄設定某情境 100% 過牌，同情境的對手也可能被強制過牌；模擬結果因此同時改變雙方策略，違反「覆寫只套用使用者座位」。

重現：指定英雄為座位 0，安裝一條 UserOverride，讓座位 1 決策並開啟 trace；實際 `neutralised_by_absolute_override=true`。不依賴隨機抽中某個動作，直接驗證錯誤來源路徑。

建議：英雄與對手使用獨立規則集合，解析前依座位選擇。補上「同一節點、不同座位」的來源、分布與人格效果測試；只檢查 coverage 分母不能保護策略隔離。

## 2. [P1] 快照還原遺失已保存條件，且無效條件被當成萬用

位置：`apps/ipc/src/snapshot.rs:418–435`，以及 `rebuild_line()`。

寫入端保存 `effectiveStackBucket`、`potType`、`heroPosition`，但 `rebuild_condition()` 沒有讀回這三項，直接由 `PostflopCondition::default()` 補成 None。原本只適用特定深度、3-bet pot、BTN 的規則，還原後會擴大適用範圍。

此外，`and_then(parse_*)` 將「欄位不存在」與「欄位存在但內容非法」都轉成 None。把快照 `handStrength` 改成未知字串，還原仍成功，該條件變成不限牌力；這與函式宣告的「個別規則解析不出來時整份失敗」相反。

重現：

- 建立帶 `StackBucket::Deep`、`PotType::ThreeBet`、`PositionLabel::Btn` 的規則，保存再還原，三項全部變成 None，條件相等斷言失敗。
- 將有效快照的 `handStrength` 改為 `typo`，預期拒絕，實際仍回傳 Some。

建議：完整讀回每個條件；僅缺省欄位可代表萬用，有值卻無法解析必須回傳錯誤。對 enum、範圍長度／端點、line 與版本資訊做嚴格驗證。round-trip 測試需涵蓋每個可保存欄位，而不只目前 UI 能產生的簡單節點。

## 3. [P1] 非法覆寫先被丟棄，診斷與執行都不報錯

位置：`apps/ipc/src/postflop.rs:615–620`、`to_intent()`、`postflop_diagnostics()`，以及 `apps/ipc/src/run.rs`。

`to_rule_set()` 遇到錯誤 node key 或無法建立的頻率分布直接 continue。診斷先呼叫這個函式，所以失敗的輸入已消失，後面的 `analyse()` 無從報錯。run 同樣直接使用轉換結果，並未在執行前拒絕完整的非法請求。

重現：合法 node key 配 `check=9000`，合計 90%；`postflop_diagnostics()` 仍回傳 `can_save=true`，規則集退回工程通則。使用者可能以為策略已生效，快照卻記錄了被靜默替換的內容。

另有相同模式：未知動作經 `filter_map()` 丟掉；規則條件未知字串經 `and_then()` 變為萬用。這些也應被視為輸入錯誤。

建議：轉換改為有明確錯誤的 Result，診斷保留每筆原始輸入的定位與錯誤；開始 run 前完整驗證，任一 error 拒絕整份請求。前端應驗證候選草稿，而非僅查上一份已套用 overrides 的 diagnostics。

## 4. [P1] 面對下注尺度使用包含該下注的底池作分母

位置：`apps/engine/src/strategy/postflop_view.rs:108–117`。

`DecisionView.pot` 是當下底池，已包含對手投入；用 `to_call / pot` 分檔，會把原底池 100、對手下注 100 的滿池下注算成 100/200=50%，命中 Half 而非 Pot。類似地，實際半池下注也會落入較小檔，使用者編輯「面對滿池」的覆寫不能命中一般滿池下注。

重現：`pot=200`、`to_call=100` 的無先前英雄投入情境，分類結果為 Half，預期 Pot。並已核對引擎建構 DecisionView 的底池與跟注額語意。

建議：以公開 action history 還原該下注的基準底池與下注量；定義再加注、已有英雄投入、多人中途跟注及短碼跟注的分類方式，不能只對所有情境套 `pot - to_call`。補由引擎實際事件產生的 1/3、1/2、2/3、滿池與 overbet 測試，避免手工 fixture 也沿用錯誤分母。

## 驗證

既有測試通過：

- `poker-engine`：hand_strength 16、postflop_execution 23、postflop_rules 40。
- `poker-ipc`：postflop_strategy 23、report 13、snapshot 單元測試 9。
- 共 124 個相關既有測試通過；前端 TypeScript／Vite build 通過。

補充探針共五項，全部成功執行並因上述缺陷失敗：對手覆寫隔離、完整條件 round-trip、非法快照拒絕、90% 輸入診斷、滿池尺度。最初 engine 探針的 include 方式有編譯問題，改成獨立複製測試後重跑，最終結果為兩項行為斷言失敗，非編譯失敗。

探針只在臨時測試檔執行，測後移除；未更動 Claude 的產品程式與原測試。未執行 Windows Tauri、全 workspace、效能 benchmark 或瀏覽器互動驗收。前次審查文件屬於舊提交；本文件是新版本的審查結果。
