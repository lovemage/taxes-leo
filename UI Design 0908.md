# UI Design 0908

日期：2026-09-08  
修訂：2026-09-09，太空艙儀表板、材質素材與正式音效範圍  
狀態：核心重播與共用外觀已實作（2026-09-10）；進階項目見下方實作紀錄；已生成一張材質貼圖與三段音效初稿，尚未接入產品  
適用版本：目前工作目錄的 v0.1.4  
關聯計畫：[0907plan.md](0907plan.md)

## 1. 結論與範圍

0907 計畫的方向可保留：八組牌力、雙軸牌面、稀疏覆寫、引擎統一解析、run 快照與來源診斷，已涵蓋主要工程需求。但分類器的部分定義、儲存操作語意與畫面配置仍需補齊，直接進入 UI 實作容易返工。

**依使用者補充，本次 UI 第一優先是「像真實牌桌遊戲正在進行的重播動畫」**。入座／離座、逐張發牌、籌碼從玩家推到桌前、收池與派彩都要有連續動作，能看清楚每筆籌碼的來去。其他面板同步採太空艙儀表板的共用設計，讓重播、參數調整與結果查看有一致風格；資料功能仍依後端實作進度接入。

視覺採固定視角的 2.5D 牌桌：牌張有厚度與翻面，籌碼有面額、側緣、陰影與堆疊，桌上物件具有一致的落點與遮擋關係。先沿用深色桌面與既有外框，以牌與籌碼的動作建立真實感；不需要真人角色或整套 3D 場景。詳細元件、時序與資料契約見 §5.5。

本次更新計畫並產出本地材質貼圖與音效初稿，尚未修改播放器或套用新 UI。素材明細見 §4.4，音效規格見 §5.5 G。

本文件包含 0907 審查與 0908 UI 計畫。原 0907 文件保留，這裡的修正建議尚未視為其已採納的工程決議。正式實作前須同步核心規格與 UI 詳細規格；README 指定的四份規範文件仍是基線。

本次沒有啟動實際應用程式；現況判斷來自原始碼、規格與既有設計圖。下列尺寸與驗收值是提案，不能當成已完成的畫面測試或效能結果。

## 2. 0907 計畫審查

**2026-09-09 更新**：目前工作目錄 HEAD 為 `c7afc11`，提交內容是 Stage 0 的 report／generated types 前置工作，尚未包含完整翻後實作。最新 [實作審查](0907實作審查-0909.md) 記錄已驗證問題與測試結果。以下 §2.1、§2.2 保留 09-08 初次計畫審查，部分定義已被後續 0907 文件修訂補充，不能直接視為現行實作缺陷清單。

### 2.1 優先修正的定義

| 優先度 | 對應章節 | 問題與影響 | 建議補充 |
|---|---|---|---|
| P0 | §3.2 `improves_board` | 現有 `eval::evaluate()` 至少接收五張牌。翻牌、轉牌只有三／四張公共牌，不能直接拿公共牌呼叫它；照文字直譯會觸發 assert。 | 為三、四張公共牌另定義部分牌型比較規則，或改以底牌對最佳成牌的貢獻判斷。先列出公對、公共三條、底牌僅改善 kicker 的預期結果，再實作專用判斷；不修改五張牌評估器的合法輸入範圍來硬湊。 |
| P0 | §3.2、§3.3 | 「成牌」「複合聽牌」「污染補牌」「後門潛力」尚不足以唯一決定分類；例如兩種弱聽牌是否一律算強聽牌、轉牌是否還可有後門標記。 | 定義各旗標的計算式、街別限制、去重與污染扣除條件。以公開資訊判定，不讀取其他座位的實際隱藏底牌；固定牌例須給出唯一結果。 |
| P1 | §2.1、§3.2 | 名稱「堅果」與 ≥95% 百分位並不等義。依文件的平手折半公式，所有人共用最強公共牌時百分位為 50%，即使英雄不可能輸，也不會命中 `nuts`。 | 區分「絕對不敗」與「相對牌力分組」的用途。八組維持單一分類，但補公共牌共用成牌的明確例外或說明；UI 不把百分位顯示為勝率，也不把工程門檻當成已簽核的撲克結論。 |
| P1 | §4.1、§4.2 | 多人池有多個對手籌碼，計畫提到逐對手數值，卻只給一個 `effective_stack_bucket`。同時，「真正可達」節點缺少連續數值與 history 壓縮後的等價類定義。 | 指明有效籌碼是相對當前下注者、最大可爭取籌碼或其他規則；補邊池案例。定義 canonical context 的等價類、區間端點與可達證據，避免不同合法動作集合被合併。 |
| P1 | §5.2、§6.1、§6.2 | 「儲存」沒有說明是否跨重啟保留；「恢復官方預設」也未定義在沒有官方內容或仍有通則覆寫時的結果。 | 本期使用「套用至本次設定」，與 run 快照分開。恢復繼承值只移除指定覆寫；官方值必須經明確來源查詢取得，不能把 fallback 改名為官方。詳見 §6。 |
| P1 | §5.3、§6.1 | 靜態節點覆蓋率與實際 run 命中率都被稱為完整度，分母容易混淆；節點查詢未說明大量結果如何傳輸。 | 分開回傳可達節點覆蓋率、實際決策命中率及 fallback 次數。節點清單按條件查詢／分頁，頻率明細按選取載入；完整快照留在 run 路徑，不要求每次畫面刷新傳回全部節點。 |

P0 是開始分類器實作前必須補齊的契約；P1 應在對應 DTO 或互動流程凍結前解決。百分位門檻需要實測校準，這份審查不宣稱已算過各牌例的正確門檻。

### 2.2 已合理處理，應保留的部分

- 使用者覆寫走完整 pipeline，以中性參數維持絕對頻率；合法化前後差異保留 trace。
- 相同輸入的分類採確定性方法，先 benchmark 再接入決策。
- 官方內容與工程 fallback 分開，保留版本及顧問簽核狀態。
- run 保存自包含內容與 hash，UI 修改不能改寫既有 run。
- 河牌移除三種聽牌組；牌面外觀與乾濕分開。
- 策略庫另外排期，避免第一版同時承擔命名、匯出與完整編輯功能。

### 2.3 與現況的差異

目前 `App.tsx` 仍以 React state 保存設定，策略編輯在執行期間鎖定。0907 的快照隔離不代表現在已支援「模擬中編輯下一次設定」；第一版 UI 先維持鎖定，後續若開放需另外設計目前 run 與下一次設定的區隔。

report IPC、外殼端點與 generated types 已在 `c7afc11` 提交；本次審查發現的統計問題見獨立審查文件。報表畫面仍應以真實契約接入，不把端點完成等同於整個 0907 計畫完成。本次不切換或建立 branch。

## 3. UI 現況與改善重點

| 現況依據 | 目前問題 | 設計處理 |
|---|---|---|
| `Strategy.tsx` 的 `PostflopRules` | 牌面 × 六動作只有「可用／不成立」，重複資訊占用整張表，缺少可比較的頻率。 | 改成牌力組 × 動作的頻率總覽，選一列進入編輯器。功能需等 0907 引擎與 IPC 完成。 |
| 同元件 `minWidth: 860`、`App.tsx` 側欄固定 300px | 最小 1100px 視窗扣除側欄與導覽後，主表放不下。 | 依內容區可用寬度切換側欄／抽屜配置，總覽不強制同時常駐右側編輯器。 |
| `StrategyNav.tsx` | 街別採 2×2 選項，翻後牌面只列文字；加入更多條件後側欄會更長。 | 街別移到主內容頂部，側欄集中情境篩選；線路維持第一級可見。 |
| `tokens.css`、`Strategy.tsx` | 部分說明只有 9–10px，次要字色偏暗；警告與小字承擔不少必要資訊。 | 關鍵原因、欄位名稱與數值至少 12px；常用文字 13px。未簽核狀態集中呈現。 |
| `App.tsx` | 重播與測驗等面板沿用牌桌設定側欄，與當下任務關聯有限。 | 重播側欄放 run／手牌篩選；測驗放題目設定。避免每頁都重複不相關參數。 |
| `tokens.css` 的計算工作區 | 上下固定約 1:2，高度未依未執行、執行中、完成調整。 | 初始狀態壓縮摘要、保留設定空間；完成後在原區域展示結果與入口，不自動跳頁。 |

已查看 `mockups/v1.4-current/02-main-workspace-v1.7.png`，取用細線桌、深色層級與數字對齊。這是既有設計參考，非目前執行畫面；不把圖中全部功能視為已實作。已被否決的 `01-main-workspace.png` 不作為重做基礎。

## 4. 視覺規格

### 4.1 色彩與層級

太空艙風格以石墨金屬機殼、內嵌深色顯示區、細微邊緣高光及低亮度綠色狀態訊號構成。共用既有語意色，集中補材質 token，不讓各面板自行配一套科幻風格。

| 項目 | 提案 |
|---|---|
| 全域背景 | `--bg-base: #0b0c0e` |
| 主要面板 | `--bg-surface: #16181c` |
| 編輯區、浮層 | `--bg-raised: #1e2126`，只用於需要區隔的區塊 |
| 分隔 | `--border: #2a2e34`；優先留白與單一分隔線，減少卡片內再包卡片 |
| 主／次文字 | `#e8eaed`／`#9aa0a6`；必要資訊不使用更暗的 tertiary 色 |
| 強調色 | 綠色 `#22c55e`，主要操作、選取文字與焦點使用 |
| 狀態 | 警告、失敗、成功一律有文字或圖示，不能只用顏色 |

選取列、導覽項、分段選項一律使用整格直角底色＋文字加重，或細底線。禁止左側強調邊框、側邊狀態條與圓角膠囊選取態。容器本身可沿用 V.4 的圓角階梯，不以一鍵改成全直角破壞既有元件。

### 4.2 字體、間距與密度

- 延用現有 sans 與 mono 字體堆疊；確認 Windows 的實際字體 fallback，不能假定宣告名稱就已安裝。
- 標題 16–18px，區段標題 14px，正文／輸入值 13px，表格與說明 12–13px。主要結果 24–28px，只用於少量關鍵指標。
- 數字使用等寬與 `tabular-nums`，數值靠右，單位獨立對齊；不用字級放大來掩蓋樣本數與區間。
- 間距使用 4／8／12／16／24px；頁面內距 16–24px，相關欄位距離小於區段距離。
- 提供標準／緊湊密度；依既有 V.6 調整列高與內距，不隱藏欄位。可操作列建議標準 36px、緊湊 28–32px。
- 密度偏好可本機保存，須與策略內容分開；不為此引入新的全域狀態框架。

### 4.3 行動與圖示

維持既有行動字母、四色花色及文字圖例。現有範圍矩陣配色與行動 log 配色不同，矩陣必須保留自己的圖例；這期不單方面改色，避免與顧問校準工作台不一致。

先盤點既有圖示元件，再決定是否統一為單一圖示套件。現有 UI 只有 React／React DOM，這次提案不要求導入 Tailwind、整套 UI framework 或動畫套件。

一般面板的 hover、展開與狀態轉換維持約 120–180ms；牌桌重播另採 §5.5 的完整事件時序，取代目前簡單淡入與單點飛行。降低動態模式須保留相同結果與操作能力。

### 4.4 太空艙儀表板與材質

**設計定位**：像太空艙內可實際操作的控制台，具有完整的控制組件、清楚讀數與一致的機械層次。整個應用共用這套語彙，重播牌桌是控制台中央的遊戲視窗。

- 機殼層：深石墨拉絲金屬，限用於頂部控制列、側欄外殼、群組標頭及控制面板邊緣；貼圖以低透明度覆在既有深色底上。
- 內嵌顯示層：純色深黑底，單層細框與輕微內陰影，表格、矩陣、CI、提示及數值不直接疊在明顯紋理上。
- 觸控／按鍵層：霧面深灰、上緣細高光、按下時 1px 位移和陰影縮短；禁止把功能鍵做成無法讀字的貼圖。
- 刻度層：少量可解讀的刻度線及單位，服務數值範圍；不加入亂碼、虛構雷達、裝飾性警告、頻閃或無功能儀表。
- 狀態層：運作中、暫停、停用與錯誤都有名稱或符號。小型指示燈可以輔助，但不整片發光，也不用左側強調邊框。

新增建議 token：`--material-shell`、`--material-opacity`（起點 0.10–0.16）、`--surface-inset`、`--edge-highlight`、`--control-shadow`、`--readout-bg`。值在控制元件樣板頁驗證後凍結。Windows 陰影與縮放效果須實際檢查。

已生成本地素材：

| 素材 | 狀態與用途 |
|---|---|
| [石墨拉絲金屬貼圖](assets/ui/cockpit/graphite-brushed-v1.png) | 內建 imagegen 生成，已目視檢查；用於機殼與框體。尚未證明無縫重複，先按面板單張裁切使用，不能宣稱已是 seamless。 |
| [素材來源與生成提示](assets/ui/cockpit/ASSETS.md) | 記錄生成方式、完整 prompt、音效來源與待驗證事項。 |

貼圖先低透明度使用，避免原圖的條紋壓過內容。按鈕、刻度、數字與選取態由 HTML／CSS／SVG 繪製，支援焦點、翻譯、DPI 與無障礙；不把生成的整張儀表板當成可操作介面。

### 4.5 共用控制元件

| 元件 | 造型與互動 | 必備狀態 |
|---|---|---|
| 主要／次要按鈕 | 同一組霧面機械按鍵；主要操作綠底深字，次要操作深灰底，文字不發光。 | default、hover、focus-visible、pressed、disabled、busy；處理中保留寬度並防重複觸發。 |
| 模式切換 | 直角分段按鍵，選中格完整填色並加重文字。 | selected、unselected、locked；不能只靠燈號看出目前模式。 |
| 開關拉桿 | 短行程滑塊／撥桿，有開／關文字與固定停點，保留原生 checkbox 語意。 | on、off、disabled、focus；Space 可切換，動畫 100–140ms。 |
| 數值滑桿 | 內嵌軌道、金屬滑塊與少量主要刻度，旁邊保留數字輸入。 | 拖曳、鍵盤步進、上下限、鎖定；不能只靠拖曳才能輸入精確數值。 |
| 數字輸入／步進器 | 內嵌讀數窗、等寬數字、單位與 +／− 控制；正常文字清晰，不使用難辨識的全七段字體。 | 編輯中、有效、錯誤、已覆寫、唯讀；輸入未完成時不跳回舊值。 |
| 下拉與參數群組 | 同色機殼、規律標頭與折疊箭頭，說明在需要時展開。 | 有值、無值、展開、停用；套用後保留值與來源。 |
| 指標讀數 | 大數值、固定單位、樣本數與 CI，必要時加入有明確上下界的刻度。 | 正常、負值、未知、計算中；未知不能寫成 0。 |
| 播放控制 | 播放／暫停／逐步／倍速／音量共用控制台外觀；重要操作有文字或焦點提示。 | 動畫中、暫停、載入、手末、靜音；不因面板切換改位置。 |

參數頁依牌桌、座位、人格、行為、策略分群，每群是同一種機殼＋讀數區。可調與尚未實作欄位有清楚差異，材質不能讓停用欄位看起來仍可操作。密度切換只改間距與列高，不藏掉參數。

先製作「控制元件樣板頁」，同時展示全部狀態，再套入計算、Bot、策略、報表與重播控制。這是本次 UI 的正式工作項，不再只列為可選美化。

## 5. 主要畫面配置

### 5.1 應用程式框架

保留 48px Header、56px 導覽與 24px 狀態列。Header 顯示模式、唯一一組執行控制與執行狀態。每個面板在主內容頂部有明確標題，導覽圖示支援 hover 與鍵盤焦點說明。

以 Tauri 的 1440×900 預設視窗設計，1100×700 最小視窗驗收。寬度以實際內容區為準，不只按螢幕解析度判斷。

- 1440px 左右：情境側欄 280px，主內容呈現總覽；選取後可開右側 300–320px 編輯區，但必須確認總覽剩餘寬度。
- 1100–1279px：選取列以抽屜編輯，側欄可收合；頻率表維持足夠寬度，不把全部區塊硬塞成四欄。
- 低於產品最小視窗的瀏覽器預覽／放大情境：側欄改為可開啟面板；表格可局部水平捲動，整個頁面不出現不可操作的裁切。

### 5.2 策略工作區，後續改善

```text
Header  模式／執行控制／狀態
導覽 │ 情境側欄          │ 自身策略   翻前｜翻牌｜轉牌｜河牌
     │ 無人下注／面對下注 │ 條件摘要＋進階條件數量
     │ 牌局線路           │ 牌力組 × 六種動作頻率總覽
     │ 牌面外觀           │ 選取列 → 頻率編輯區／抽屜
     │ 乾燥／濕潤         │ 來源、驗證摘要、覆蓋率
     │ 面對尺度（適用時）  │ 指定底牌分類預覽（另開）
Status bar
```

總覽一次只顯示目前情境的八個牌力組，河牌五組。六個動作是固定欄位，非法動作顯示「0% · 不適用」，可讀取原因。每列另有來源文字與已覆寫標記，避免以彩色底圖取代可讀數字。

牌力組總覽兼任選取入口，選中後維持同一列的位置。進階條件收在可展開區，但只要有啟用條件，就在表格上方摘要列完整顯示其值；牌局線路不可藏入進階設定。

「面對下注」的三個主動尺度欄以「加注」命名，無人下注時以「下注」命名；%Pot 的換算基準由引擎契約解釋，UI 不自行計算 raise-to。

翻前保留 13×13 矩陣與既有逐格覆寫流程，共用字體、工具列、來源標記及編輯器外觀；不連帶改動翻前策略內容。

### 5.3 Bot 與座位

座位清單固定顯示位置、名稱、人格與籌碼，選中使用整列直角底色。右側參數每列呈現名稱、數值輸入、必要的 slider 與單位；已覆寫欄位有文字提示及重設入口。

「官方預設 → 人格值 → 逐座覆寫 → 最終值」可展開逐層對照，最終值常駐。尚未生效的參數放在獨立停用區，明確說明原因。不要把不能執行的功能畫成可按的主要按鈕。

### 5.4 計算、報表與重播

| 畫面 | 設計內容 | 前置條件 |
|---|---|---|
| 計算 | 初始顯示設定摘要；執行中顯示階段、手數與可用控制；完成保留耗時及「查看重播／報表」入口。準備階段不顯示假百分比。 | 沿用目前 run 事件與鎖定邏輯。 |
| 報表 | 整體指標、樣本數與 CI 同層；往下看位置／牌型切片，篩選範圍常駐。無結果、進行中、部分結果、失敗各自有文字。 | 先確認 report IPC、型別匯出與資料來源；尚未可用的指標不得填假值。 |
| 重播 | 左側 run／手牌篩選，主區細線牌桌，底部時間線與行動紀錄。保留選中手牌，切換步驟不跳動配置。 | 以快照還原畫面；大小盲與單位取自被重播 run，不以當前新設定取代。 |

報表不可用「樣本不足」覆蓋仍可計算的 CI。分清楚真實缺值、零值與未完成計算。牌桌外框先沿用既有規格，牌張厚度、籌碼堆疊與接觸陰影在重播元件內實現。

### 5.5 牌桌遊戲式重播，第一優先

#### A. 觀看體驗與畫面占比

進入重播，牌桌成為主畫面，桌面兩側與下方保留玩家、手牌、籌碼的位置。逐手列表預設收合，詳細 trace 以抽屜查看；播放控制常駐桌下，不因行動紀錄增長被推到畫面之外。一般高度下，牌桌以重播可用內容高度約 65–75% 為配置起點，最小視窗以不重疊、控制可見為準。

固定鏡頭，英雄座位優先位於下方。同一桌次以穩定 seat ID 固定各座落點；有人離座後留下空位，不把其餘玩家重新平均排列。莊家按鈕依引擎位置移動，不能以座位陣列索引推測。跨桌次重置場景，不能演成同桌玩家突然補滿籌碼。

每個玩家周圍固定四個區域：身份／狀態、底牌、身後籌碼、桌前下注。中央公共牌、已收底池、邊池及棄牌區各有固定位置。動畫只在這些落點之間移動，不穿過其他玩家的名稱或公共牌。

#### B. 可組合的牌桌元件

以下為預計元件，尚未新增檔案。優先放在 `apps/ui/src/components/replay/`，由現有 `TableView` 組合。

| 元件 | 外觀與責任 | 主要動作 |
|---|---|---|
| `TableStage` | 固定視角桌面、桌緣、物件分層與縮放座標。 | 視窗變更時同步所有起終點；鏡頭不隨每次下注晃動。 |
| `PlayerSeat` | 頭像或位置章、名稱、剩餘籌碼與狀態；空位保持原位置。 | 入座淡入並落位，離座先收走該手物件再淡出。 |
| `DealerButton` | 有厚度的 D 按鈕，與 SB／BB 文字標記分開。 | 手間滑到下一位置；dead button 可停在空位。 |
| `CardSprite`／`HoleCards` | 共用牌面、牌背、厚度、接觸陰影與兩張底牌的扇形排列。 | 從牌堆飛入、輕微旋轉落定、翻面、棄牌回收。 |
| `DeckAndMuck` | 發牌起點與棄牌終點。 | 接收棄牌；不捏造紀錄中不存在的燒牌身分或牌序。 |
| `BoardCards` | 五個固定牌位，翻牌三張、轉牌與河牌各一張。 | 先移入再翻開，後續牌不推擠已發牌。 |
| `ChipStack` | 面額配色、側緣刻紋、堆疊高度、陰影及精確金額標籤。 | 身後籌碼減少／增加，桌前下注逐次堆高。 |
| `BetChips` | 每座獨立的本街桌前籌碼區。 | 跟注追加差額，加注疊入新增籌碼，全下推入可用籌碼。 |
| `PotGroup` | 已收主池與邊池，以名稱、金額區隔。 | 街末收池，攤牌後按池別派彩。 |
| `ChipTransferLayer` | 跨元件飛行中的籌碼群，統一控制遮擋與落點。 | 玩家→桌前、桌前→底池、底池→贏家、未跟注款→原玩家。 |
| `ActionBadge`／`WinnerMarker` | 過牌、跟注、加注、全下、獲勝文字與短暫反白。 | 與行動同步出現；不使用左側狀態條或整屏慶祝特效。 |
| `ReplayTimeline` | 單一播放時鐘、事件進度、速度與定位。 | 暫停、續播、逐步、拖曳、倒退與跨手播放。 |

素材採可縮放 SVG／CSS 層次，先做一套牌背、一套四色牌面、籌碼面額與 D 按鈕。一般圖示可沿用既有元件，牌與籌碼是專用遊戲素材，不以通用圓點代替。使用本地資產，記錄外部素材授權；第一版不依賴遠端素材載入。

#### C. 動作腳本與節奏

下列為 1× 播放的初始調校值，完成可播放原型後再微調。這是重播演出節奏，不宣稱還原玩家真實思考時間。

| 事件 | 動作次序 | 初始時間 |
|---|---|---|
| 入座／新桌 | 玩家資訊落位 → 身後籌碼堆出現 → 莊家標記定位。 | 300–450ms，可讓多座略微錯開。 |
| 強制下注 | 依紀錄扣減身後籌碼 → 小群籌碼推到指定區 → 保留精確金額。 | 每筆 250–350ms。Ante 與盲注落點由事件種類區別。 |
| 發底牌 | 從固定牌堆逐座發第一張，再逐座發第二張；落位後依觀看模式顯示牌面。 | 單張飛行 180–240ms、發出間隔 70–100ms，可重疊飛行。 |
| 過牌 | 玩家短暫顯示「過牌」，焦點轉往下一個行動者。 | 250–350ms；沒有籌碼移動。 |
| 跟注／下注／加注 | 行動標記出現 → 身後籌碼拆出 → 推入自己桌前 → 新舊下注堆合併落定。 | 350–550ms，落定後留 150–250ms 閱讀。 |
| 棄牌 | 兩張底牌合攏、翻回牌背並滑向棄牌區，座位轉淡但仍在桌上。 | 220–350ms。 |
| 全下 | 身後籌碼推到桌前、剩餘金額定為 0，顯示 All-in；保留底牌及玩家座位。 | 450–650ms；0 籌碼不等於離座。 |
| 街末收池 | 各座本街下注先匯入主池／邊池，桌前歸零，再處理下一街。 | 350–500ms。 |
| 翻公共牌 | 翻牌三張先滑入牌位，再依序翻面；轉牌／河牌各自移入及翻面。 | 翻牌共 600–850ms；單張 300–450ms。 |
| 攤牌 | 依可見性事件翻開可亮底牌，標出引擎回傳的贏家與組牌。 | 每座 250–350ms，結果停留 600–900ms。 |
| 退款／派彩 | 退款獨立回到原座；底池按來源拆出籌碼群送往各贏家，身後堆增加，來源堆減少。 | 每筆 450–700ms；拆池可依序或同階段並行。 |
| 離座／下一手 | 本手結果停留 → 清理牌張及臨時物件 → 依後續座位事件移除玩家 → 移動 D 按鈕。 | 離座 300–450ms，手間留 500–800ms。 |

沒有攤牌而直接勝出的手牌，不補演所有人的亮牌或尚未發出的公共牌。全下後自動發完公共牌也須走紀錄中的事件順序。發牌起點不跟著 D 按鈕換位置；D 是位置標記，並非桌面發牌器。

#### D. 籌碼堆疊與金額一致性

籌碼要看得到堆高、拆出與落下，不只讓數字跳動。依 run 最小籌碼單位設定一組展示面額，再將金額映射成多個籌碼堆；顏色加面額數字，不只靠紅綠辨識。

大額籌碼採有上限的壓縮堆疊，初版以每區最多 3 堆、每堆最多 8 個可見側緣作為效能起點。標籤始終顯示精確金額，壓縮圖形不能讓人誤認畫面每片都代表全部實際片數。面額拆換可以是視覺表現，不能改變真實下注或餘額。

下注路徑必須是「身後 → 自己桌前」，收池才是「桌前 → 中央」，派彩則是「中央 → 贏家身後」。同一筆轉移中，物件只能屬於來源、飛行中或目的地之一，不能先在目的地出現完整籌碼，又額外播放一份飛行籌碼。

精確金額標籤在事件提交點一次更新到引擎提供的終態，不逐幀累加或取整。來源／目的地籌碼的淡出、移動與堆疊可插值；若未來加入數字滾動，須與正式金額分層，暫停與定位時以完整快照為準。

#### E. 現有資料缺口與播放器契約

目前 `TableView.tsx` 的底牌是原地縮放淡入；下注用單一小圓點直接飛向底池，金額不隨籌碼移動；`FrameView` 提供完整終態，但 `committed` 是「本手累計投入」，不能直接畫成本街桌前籌碼。`SeatView` 的 occupied、payout、refund 是手牌層資料，尚不足以描述逐筆離座與拆池派彩時序。

因此先擴充重播呈現契約，再接完整動畫。以下為候選欄位，實作前由 Rust 型別凍結，非宣稱現有欄位：

- 每事件具有穩定 `runId／instanceId／handId／eventId`、事件種類與完整終態；另提供前一終態或可直接取得的來源快照。
- 提供本街各座下注、已收主／邊池、各座剩餘籌碼及總底池的明確定義，說明總底池是否含未收桌前下注，避免重複計入。
- 由 Rust 重建 `transfers[]`，包含來源、目的地、精確增量、用途與來源池別。`raiseTo` 是累計加注到的金額，不能當成本次飛出的籌碼量。
- 提供明確的發牌順序、亮牌事件、當前行動者／下一行動者與座位占用轉換。發牌動畫可以拆成展示子片段，但順序由引擎／重播層提供。
- 由記錄還原退款、抽水、分池、平手分配與零頭籌碼；前端不能從勝負標記自行分錢。缺少池別的舊紀錄只演合計派彩並標示簡化，不虛構主／邊池分配。
- 同桌相鄰手可由 Rust 比對 occupied 得到離座轉換；非連續跳手或換桌次直接建立目標場景，不推測中間有人入座／離座。未記錄原因只標「離座」，不自稱淘汰或主動退出。
- 補上 run 原始 BB／最小籌碼單位，重播金額格式不能受目前新設定改變。

`RevealedOnly` 現在依「本手曾亮出」決定傳牌，而非逐事件可見；全揭露也為目前 `Replay` 的初始值。第一版保留既有分析模式，另讓「依牌局時序亮牌」可明確選擇。要做到真實時序，Rust 必須按事件提供牌面可見性，不能用 CSS 遮住提前提供的牌面充當完整時序隔離。切換模式時重取資料並清掉舊的動畫與牌面快取。

#### F. 播放、暫停與定位

採「完整快照＋可取消的事件轉場」。每段轉場只有呈現責任，動畫結束回呼不計算籌碼、不推導牌局狀態。

- 連續播放才演完整搬移。提供 0.5×／1×／2×／4×；加速同時縮短事件與閱讀停留，不能讓上一段籌碼還未落定就跨到下一手。
- 暫停凍結共同時鐘、牌張、籌碼與延遲中的子片段；續播從同一進度恢復。不能只停下一個 frame 計時器，讓 CSS 動畫繼續跑完。
- 上一步／下一步、跳街與拖曳定位直接顯示事件邊界快照，不補播跳過的動作；倒退不反向重演派彩。
- 快速換手、改可見性或離開頁面，取消舊轉場及音效；API 回應依 run／hand／visibility 修訂識別過濾，舊回應不得蓋掉目前畫面。
- 下一手可預載，但只載入小範圍；資料未到先停在本手結算畫面並提示載入，不能把舊牌配上新手數。
- 視窗縮放時用同一桌面座標系重算落點，或取消轉場後顯示正確終態，不能讓籌碼飛到過期座標。
- 降低動態模式直接切換快照或短淡入；派彩、動作與離座仍有文字提示。

在現有 React／TypeScript 內實作，優先評估可共同控制時間的 Web Animations API 或集中式動畫控制器。避免讓互不相干的 CSS animation、`setTimeout` 各自決定進度。第一版不引入 3D 引擎；這是應用內的互動重播，不另建影片渲染專案。

#### G. 音效與完整手牌驗收

**發牌、收籌碼、勝利音效納入正式交付**。採短、乾淨、低調的遊戲聲音，不加背景音樂或勝利號角；音效以桌面動作為主，控制台外觀不要求每個按鈕都發出科幻電子聲。

| 音效初稿 | 內容 | 觸發點 |
|---|---|---|
| [發牌](assets/audio/cockpit/deal-soft-v1.wav) | 0.19 秒，紙牌滑動與輕落桌聲，峰值 −20 dBFS。 | 發出牌張時；密集發牌最短間隔 70ms，最多 2 個聲部。 |
| [收籌碼](assets/audio/cockpit/chips-collect-soft-v1.wav) | 0.46 秒，低調的籌碼碰撞群，峰值 −18 dBFS。 | 一次收池群組落定時，不能每片籌碼都觸發。 |
| [勝利](assets/audio/cockpit/win-subtle-v1.wav) | 0.72 秒，柔和三音確認，峰值 −23 dBFS。 | 英雄獲勝的結果呈現時，每手最多一次；不能以退款或多筆派彩觸發多次。 |

這三段已以本地程式合成，48kHz／16-bit mono WAV，具固定種子與淡入淡出；[生成原始碼](assets/audio/cockpit/generate.py) 可重現。已檢查檔頭、長度、峰值與無削波，尚未在應用或耳機上試聽，列為音色初稿而非已驗收音效。主體為原創合成，未使用網路錄音樣本。

播放設定提供總開關、總音量及音效分類音量；初始總音量 50% 作調校起點，本地記住偏好。使用者按播放後才解鎖 AudioContext；音訊不可用時仍可正常重播。音效保持正常音高，2×／4× 壓縮觸發密度，不把聲音加速變尖。

音效由與動畫共用的事件時鐘觸發，透過 event ID 去重。暫停／換手／倒退／拖曳時停止舊音源，恢復時不補播已錯過的聲音；下一手載入失敗不能繼續播放上一手聲音。最多 3 個同時聲部並留混音餘裕，必要時以 limiter 防止疊加削波。

驗收包含連續播放 10 分鐘、耳機與筆電喇叭試聽；勝利聲不得明顯壓過發牌／籌碼聲。靜音和降低動態是分開偏好，任何一項關閉都不能損失牌局資訊。

至少準備六段可重播的測試紀錄：一般翻牌到河牌、翻前全棄、下注後退款、多人全下與邊池、平手拆池、同桌結算後離座／dead button。另加跨桌次與非連續跳手案例。

視覺完成標準是使用者能從畫面辨認「誰動作、牌從哪裡來、下注停在哪裡、底池何時收走、最後籌碼給誰」。測試不得只檢查動畫 class 有沒有出現，需錄下完整一手，並在關鍵事件停格比對來源／飛行／落定狀態與引擎終態。

## 6. 翻後編輯流程與狀態

### 6.1 編輯流程

1. 選街別、下注狀態、線路與牌面，引擎回傳可用選項及目前規則。
2. 選牌力組，建立該節點的本地草稿。畫面保留完整條件摘要，避免改錯節點。
3. 編輯六個頻率欄。顯示合計、尚差／超出百分點，不自動調整其他欄。
4. 透過純函式驗證完整覆寫候選清單；錯誤須阻擋套用，warning 可保留。
5. 「套用至本次設定」將驗證通過的草稿寫入 UI 覆寫集合。開始 run 時才凍結快照。

頻率採 0.01% 精度並轉成整數萬分比，範圍 0–10,000，合計必須精確為 10,000。空字串、負值、超出範圍及超過兩位小數均有明確驗證；不得用浮點近似相等判定 100%。前端驗證只改善輸入體驗，Rust 仍完整驗證。

### 6.2 草稿、套用與恢復

| 狀態／操作 | 行為 |
|---|---|
| 未修改 | 顯示來源與簽核狀態，套用停用。 |
| 草稿已修改 | 顯示「尚未套用」；切換節點／面板時提供套用、捨棄、留在原處。驗證未過不能選套用。 |
| 已套用 | 顯示「已套用至本次設定」。本期不宣稱已另存為跨重啟策略檔。 |
| 開始執行前 | 有未處理草稿時先解決草稿，不靜默忽略它開始 run。 |
| 執行中 | 規則可檢視，編輯與重設鎖定；顯示使用中的快照版本。 |
| 恢復繼承值 | 從草稿移除指定覆寫，重新解析並預覽繼承來源；確認套用後才改本次設定。不能刪除整批其他節點覆寫。 |
| 套用官方值 | 僅在引擎回傳可適用的官方規則時提供，先預覽差異；若以新使用者覆寫套用官方數值，來源仍標為使用者覆寫，另記原官方版本。這是對 0907「恢復官方預設」的建議語意，須同步 DTO。 |

來源必須能區分使用者覆寫、官方、一般規則、工程基準與最終 fallback。顧問簽核是獨立欄位，不因套用／重設就變成已簽核。

查詢識別需包含選取條件與覆寫修訂號；快速切換條件時忽略舊回應，避免舊節點頻率寫入新節點。驗證進行中不可重複套用，失敗保留草稿並提供重試。

### 6.3 指定底牌預覽與診斷

分類預覽另外輸入兩張底牌與對應街別的公共牌，阻擋重複牌、錯誤張數及非法牌碼。顯示分類、目前牌力百分位、raw／有效補牌與判定原因；明示百分位是相對合法對手組合的目前牌力，不能當成未來攤牌勝率。

沒有具體底牌、底池與籌碼時，節點編輯器只顯示設定頻率；不捏造工程 fallback 的固定百分比或實際合法化結果。重播有完整 context 時，再顯示「設定頻率 → 實際分布」及尺寸合併／遮罩原因。

診斷按規則聚合，點選問題可定位欄位。編輯頁標示「可達節點覆蓋率」，報表標示「本 run 決策命中率／fallback 次數」，各有分子分母；沒有 run 時使用破折號或「尚無執行資料」，不顯示 0% 命中率。

## 7. 實作階段與依賴

| 階段 | 工作與預計檔案 | 完成條件 |
|---|---|---|
| Cockpit-0 材質與控制元件 | 已有石墨貼圖與音效初稿；接著完成控制元件樣板頁及各狀態，凍結材質／數字／按鍵／滑桿 token。 | 所有元件同風格，值與焦點清楚；材質不影響表格閱讀。 |
| Replay-0 基線與資料盤點 | `panels/Replay.tsx`、`components/TableView.tsx`、現有播放器 hook、`apps/ipc/src/view.rs` 與重播建幀路徑；記錄目前完整一手，確認 §5.5 E 缺口。 | 有發牌、下注、收池、派彩、離座對應契約與測試紀錄；區分舊資料可重建與不可重建項目。 |
| Replay-1 遊戲元件 | 建立 TableStage、PlayerSeat、牌面／牌背、ChipStack、BetChips、PotGroup、DealerButton；以 fixture 預覽。 | 1440×900 與 1100×700 看得清全部座位及籌碼；堆疊、厚度、落點與遮擋關係可檢視。 |
| Replay-2 一手完整動作 | Rust 重播 DTO／型別與 transfer 資料、集中式時間控制器；串起逐張發牌、桌前下注、收池及派彩。 | 真實紀錄可連續播放一整手，金額／牌面／來源池正確；fixture 不冒充實際重播。 |
| Replay-3 複雜情境與控制 | 退款、邊池、平手、全下發牌、離座、dead button、跨桌；暫停、倍速、拖曳、可見性切換與回應競態。 | 複雜牌局也可定位還原，不重複扣款、派彩或殘留上一手物件。 |
| Replay-4 視覺與效能驗收 | 完整手牌錄影、停格、Windows WebView2、DPI 與減少動態測試；必要時精簡堆疊與陰影。 | 通過 §8 重播驗收，提供可操作版本與錄影佐證。發牌、收籌碼、勝利三類音效皆接入並通過音量與事件同步驗收。 |
| Cockpit-1 全面套用 | 計算、Bot、策略、報表與重播控制套用太空艙元件；§5.2／§6 新功能依後端接入。 | 已有面板完成一致外觀；尚缺後端的控制明示停用，不能用假資料冒充完整功能。 |

重播優先，不需要等 0907 的牌力分類器與翻後編輯器全部完成；若要在重播中新增「決策為何如此」的策略說明，再接該計畫的 trace。新增重播時序的資料契約與報表缺陷修正分開協調；本次審查不直接覆寫 Claude 的實作。

傳輸集中於 `apps/ui/src/api.ts`，DTO 由 Rust 產生並匯出。前端負責顯示插值與播放，不重算下注、分池、派彩、位置輪轉或牌力。

## 8. 驗收清單

### 重播主驗收

- [ ] 真實紀錄能演完「座位就緒 → 強制下注 → 逐張發牌 → 各街動作／收池 → 攤牌或直接獲勝 → 退款／派彩 → 清桌／必要離座」。
- [ ] 籌碼有可辨識的堆疊與增減；下注先停桌前，不能每次直接飛中央；每筆轉移不重複出現兩份。
- [ ] 引擎提供的增量、餘額、底池與派彩逐事件一致；退款不顯示為獲利，累計投入不誤當本街下注。
- [ ] 棄牌只收牌，全下保留座位；離座只在確認的座位轉換演出，同桌空位不使其他玩家換位。
- [ ] 發牌先後、公共牌翻面、亮牌時機符合所選模式，未來公共牌不提前顯示。
- [ ] 多人邊池、平手拆池、退款、抽水與零頭籌碼正確；舊資料缺池別時明示簡化演出。
- [ ] 0.5×／1×／2×／4×、動畫中暫停／續播、倒退、拖曳與連續換手都沒有殘影、錯牌或雙重派彩。
- [ ] 中途換可見性、換手或 API 亂序回應不顯示上一份牌面；跨桌次不演成同桌續局。
- [ ] 提供六種完整牌局錄影與關鍵停格；可在應用內實際操作，不只交動畫示意影片。
- [ ] 指定基準硬體上的 1× 重播以穩定 60fps 為目標，記錄慢幀與記憶體；連播至少 100 手無持續堆積的動畫、計時器或 DOM。目標尚未實測。

### 太空艙面板與音效驗收

- [ ] 所有既有面板的按鈕、滑桿、開關、讀數、群組標頭均使用同套元件與 token，沒有新舊風格混雜。
- [ ] 機殼貼圖低對比，表格與文字底部乾淨；無材質載入時仍可操作；重複貼圖接縫經過驗證才啟用。
- [ ] 每個控制具有鍵盤焦點、停用、處理中與錯誤狀態，選取態無左側強調邊框。
- [ ] 三類音效同步、可靜音、可調音量；倍速不變調，暫停與定位無殘留聲；完整試聽不刺耳、不突出。

### 通用與策略功能驗收

- [ ] 1440×900、1366×768、1100×700 的主要操作可見；Windows 125%／150% DPI 及文字放大後，輸入與抽屜底部按鈕仍可到達。
- [ ] 選取／狀態沒有左側強調邊框；沒有圓角膠囊選取態；容器圓角與選取底色符合既有規格。
- [ ] 必要說明不低於 12px；一般文字對比目標 4.5:1，焦點／必要控制項邊界目標 3:1，須實測。
- [ ] Tab 順序符合閱讀順序，焦點可見；抽屜可用 Escape 關閉並返回觸發點，有未套用草稿時先處理草稿。tooltip 可由鍵盤取得。
- [ ] 頻率輸入驗證涵蓋 99.99%、100%、100.01%、空值與非法動作；沒有靜默正規化使用者草稿。
- [ ] 切換翻牌到河牌不殘留無效牌力組；切換下注狀態不殘留不適用線路與尺度；修正後的選取以引擎回應為準。
- [ ] 快速切換節點、查詢失敗、套用失敗與重試不遺失草稿、不串錯頻率；恢復單一節點不改其他覆寫。
- [ ] 編輯頁的設定頻率與 run 快照一致；重播可說明合法化差異，讀取原 run 單位與設定。
- [ ] 未簽核、無官方規則、繼承通則、最終 fallback 都能辨識；讀取不到資料時不顯示假 0%。
- [ ] 報表保持數值、樣本數與可計算 CI；靜態覆蓋與實際命中不混用。
- [ ] 降低動態偏好生效；批次計算不因 UI 動畫或每手重繪而變慢。以既有指定硬體量測，沿用核心規格效能門檻。
- [ ] 純 UI 改動通過 `pnpm --filter @taxes-leo/ui typecheck` 與 `build`；涉及 Rust／DTO 時追加既有 cargo 測試、Clippy、bindings 一致性與 Windows Tauri 檢查。

本次交付更新計畫、貼圖與音效初稿，未接入應用；上述產品與聽感驗收尚未執行。另有報表程式審查及測試，見獨立審查文件。

## 9. 審查依據

- [0907 翻後計畫](0907plan.md)，尤其 §3、§4、§5、§6。
- [UI 面板詳細規格](UI面板詳細規格.md)，V.1–V.14、D.5–D.9、F、G。
- [README](README.md)，產品範圍、規範文件與目前進度；其中歷史進度敘述仍須以程式碼核對。
- [App](apps/ui/src/App.tsx)、[策略內容](apps/ui/src/panels/Strategy.tsx)、[策略導覽](apps/ui/src/panels/StrategyNav.tsx)、[視覺 token](apps/ui/src/tokens.css)。
- [重播面板](apps/ui/src/panels/Replay.tsx)、[牌桌元件](apps/ui/src/components/TableView.tsx)、[重播 DTO](apps/ipc/src/view.rs)。
- [牌力評估器](apps/engine/src/eval.rs)、[桌面視窗設定](apps/desktop/tauri.conf.json)、[前端相依](apps/ui/package.json)。
- [既有 v1.7 畫面參考](mockups/v1.4-current/02-main-workspace-v1.7.png)。


## 2026-09-10 實作紀錄

本輪已接入產品程式，不再僅是設計稿。

- 共用外觀：石墨機殼貼圖、內嵌數字顯示區、按鈕按壓／焦點、金屬滑桿及方形切換開關；不使用左側選取邊框。矩陣與動作語意色保留。外殼改用低飽和青綠操作色。
- 重播使用完整工作區，移除無關的牌桌設定側欄。固定座位編號、Hero 在下方，空位保留；只在同桌連續手牌的 occupied 改變時顯示入／離座提示。
- 底牌分兩輪由中央發出，公共牌逐街翻出，棄牌收起；支援減少動態偏好。
- Rust 新增 dealHole、collect、refund 事件；streetBets 與 collectedPot 分開，避免把整手累計投入當成當街下注。結算 stacks 加回引擎記錄的 payouts 與 refunds。
- pot 保留本手總投入以相容分析資料；結算與退款畫面明確顯示「本手總投入」。中央籌碼顯示 collectedPot，結算後為零。
- 下注、收街、退回、派彩有移動籌碼提示；籌碼堆高度為視覺摘要，實際金額以旁邊 BB 數值為準，不代表可逐顆換算的面額。
- 同一個展示時鐘控制事件、發牌及籌碼移動；暫停凍結、0.5–4x、逐步與拖曳定位，結算在 1x 停留 2 秒。背景分頁恢復後不直接跳過整手。
- 手動啟用的低音量發牌／籌碼／Hero 勝利音效，預設關閉、音量 50%；暫停／切手停止，定位不補播；速度不改音高。退回不觸發勝利音效。
- 切換手牌／可見範圍時忽略過期 API 回覆；BB 換算改讀 run 快照的 bigBlind。

### 已執行驗證

- IPC 原有 112 項單元／型別匯出測試通過。
- 重播 7 項整合測試通過，包含 300 手含抽水資料的展示籌碼守恆與派彩／退回入帳。
- 2 項播放時間測試通過；前端 TypeScript 與 Vite 正式建置通過。
- 使用獨立示範伺服器與瀏覽器檢查，避免影響既有 5180／7801 服務；實際確認暫停後事件位置及卡片樣式保持不變。
- 截圖保存於 `mockups/ui-design-0908/`：1440×900 儀表板、1440×900 與 1100×700 重播、1366×768 結算。

### 尚未視為完成的進階範圍

- 既有 HandRecord 沒有逐邊池派彩順序，本輪忠實顯示合併派彩，不假造分池。精確分池需要後續擴充儲存契約。
- 入／離座僅能辨識 occupied 的變化；同座即時補員沒有玩家身分事件，不能假裝已能辨識換人。
- 尚未完成策略面板結構重排、標準／緊湊密度偏好、獨立元件展示頁，以及 Windows Tauri／125%／150% DPI 的人工驗收。
- 音檔已接入並通過建置，但最終聽感仍需在玩家實際喇叭或耳機上驗收。

本機預覽：`POKER_DEV_ADDR=127.0.0.1:7802 cargo run -p poker-devserver`；UI 目錄執行 `POKER_DEV_API=http://127.0.0.1:7802 npm run dev -- --port 5181`。

## 2026-09-12 策略條件補充：位置、過牌後加注與尺寸

狀態：已於 0.1.6 實作位置／歷史限定覆寫、逐座及逐位置 Open 尺寸、持續下注歷史條件與五檔面對區間。通用節點保留為繼承層。

### 翻後相對位置與 check-raise

- 翻前位置（UTG／CO／BTN／SB／BB）與翻後相對位置分開選。單挑提供 IP／OOP；多人底池須表達第一個、中間、最後行動及後方有效對手數，不能直接以 UTG＝OOP、BTN＝IP 取代真實存活玩家順序。
- 增加「本街自己尚未行動／已過牌後面對下注／下注後面對加注」條件。不能只區分無人下注與面對下注，也不能把 check-raise 限定為翻前進攻者才有的動作。
- 使用者提出的強牌先過牌、遭下注後加注，拆成兩個可編輯決策：無人下注時的 check 權重；已 check 且面對下注時的 fold／call／raise 權重及加注尺寸。兩次決策按實際歷史匹配，不預先承諾一定加注。
- 介面將這條路徑顯示為「過牌後面對下注」，其中的加注標示 check-raise。若對手沒有下注，不能自行生成加注事件；若短碼加注沒有重新開放加注權，依引擎合法動作停用。
- 節點鍵／條件 DTO／覆寫匹配／衝突診斷／run 快照／trace 必須一起擴充。既有無位置限制的設定保留為通用規則，位置限定的設定不得誤套其他位置。

### 每位玩家的 open 尺寸

現況：`BaselineRules::engineering_placeholder` 設定 `open_size_centi_bb = 250`；一般 unopened baseline 因此使用 2.5 BB。組合表另有小／大加注尺度（unopened 對應 2.5／8 BB），不能把所有策略來源一概宣稱為唯一固定 2.5 BB。

- Hero 與各 Bot 提供獨立的 open raise-to BB 欄位；0 繼承原策略（一般 2.5 BB，原大尺寸與全下頻率保留），可輸入 2～100 BB，並依翻前位置覆寫。尺寸例子如 2、2.2、2.5、3 BB 僅示範輸入，並非策略建議。
- open 指前方未有人自願進池時的首次加注總額，不與隔離 limper、3-bet、4-bet 共用同一欄。已有 straddle 時須標明 BB 與 live blind 的差異並使用引擎合法下限。
- 尺寸與動作頻率分開；修改 open 尺寸不自行重算或宣稱重新求解範圍。未來多尺寸混合須另外設定各尺寸權重。
- 對應 UI、Bot／Hero 設定、IPC、baseline／組合表尺寸覆寫優先序、run 快照及重現測試一併處理；對手面對新的 open 時，後續跟注與再加注須讀實際下注額，不能沿用假定前方固定 2.5 BB 的常數。
- 以整數百分之一 BB 保存設定；若籌碼最小單位無法精確表達（例如 BB=2 單位時輸入 2.2 BB），顯示引擎取整後的實際尺寸，不能假裝精確執行。短碼與最低加注限制的調整需在 trace 可查。

### C-bet、轉牌與河牌線路名稱

- 翻前最後加注者於翻牌延續下注：Flop C-bet。
- 翻牌下注被跟、轉牌由同一進攻者再下注：轉牌持續下注／Double barrel。若同一路徑河牌再次下注：河牌持續下注／Triple barrel。
- 翻牌過牌後才於轉牌首次延續翻前進攻：Delayed C-bet；不能與 double barrel 合併。
- 「Turn C-bet」亦有使用，但介面採街別加具體歷史名稱，避免把所有轉牌下注都叫 C-bet。
- 現有 `CbetChance` 僅依 previous_street_aggressor 匹配，包含非翻前加注者在前街取得主動權的情況。因此不能只根據街別把既有 key 的標籤換成 double／triple barrel：須保留翻前進攻者、前街 bet／raise／check-through 與進攻者是否更換的歷史。
- 例如翻牌 check-raise 後轉牌下注，應顯示「加注後延續下注」，不可誤標成原翻前進攻者的第二槍。

用語參考：[PokerStars Learn：Double barreling](https://www.pokerstars.com/poker/learn/strategies/3-top-tips-for-double-barreling-in-poker/)、[Continuation betting](https://www.pokerstars.com/poker/learn/lesson/c-betting/)。本節引用用語，不採用文章中的打法建議作為 baseline。

### 面對下注改成明確區間

使用者指定小注包含 1/3 以下、最高一檔包含滿池以上；1/2 是否獨立仍屬可選設計。本輪建議包含 1/2 的五檔，完整涵蓋正比例且邊界不重疊：

| 介面區間 | 精確條件（r 為下注比例） |
|---|---|
| 1/3 或以下 | 0 < r ≤ 1/3 |
| 超過 1/3 至 1/2 | 1/3 < r ≤ 1/2 |
| 超過 1/2 至 2/3 | 1/2 < r ≤ 2/3 |
| 超過 2/3、未滿 1 池 | 2/3 < r < 1 |
| 1 個底池或以上 | r ≥ 1 |

- 這是「對手下注的分類區間」，不是我方只能下這五種尺寸。我方下注與加注尺寸另行設定。
- 不以最近標準尺寸或四捨五入分檔。Rust 使用整數交叉相乘比對 1/3、1/2、2/3、1 的邊界，避免比例取整把邊界附近分類錯誤。
- 首次下注：對手下注額 ÷ 該次下注前底池；面對加注：維持現有明確的「加注增量 ÷ 加注者跟平後底池」契約，UI 顯示基準說明，不能把 raise-to 總額當下注增量。
- 無人下注獨立處理；是否 all-in 是額外狀態，短碼 all-in 不一定是大注。
- 現有 Quarter／Third／Half 等近似尺寸桶與新版區間語意不同，需新版本的節點集合與快照。不得直接替舊 `third`／`pot` key 改名而靜默改變舊設定涵蓋範圍；遷移需可檢視差異，舊 run 維持原版本解讀。

### 驗收要點

1. 同牌面／牌力，OOP 的過牌與 check-raise 覆寫不影響 IP；非翻前加注者也能走 check-raise。
2. 不同玩家與不同位置的 open 尺寸各自生效；設定修改不影響已開始的 run，重播顯示實際取整後金額。
3. C-bet、double barrel、delayed C-bet、check-raise 後延續下注有各自的歷史案例，不依街別硬套名稱。
4. 下注比例在所有邊界的恰好值、稍低、稍高皆有測試；後方跟注不改變對手原下注的分類。

### 0.1.6 實作與驗證紀錄

- 策略頁提供翻前位置、翻後相對位置、本街行動及持續下注歷史四組條件，節點鍵附加 scope。位置限定覆寫優先於通用設定；通用編輯不讀入較窄位置的代表值。恢復與批次恢復包含新節點。
- 本街行動採「尚未行動／過牌後被下注／已投入後面對加注」，最後一項涵蓋跟注後再被加注。相對位置排除已棄牌或全下玩家，另提供其餘對手皆全下。
- Open 以百分之一 BB 儲存，0 繼承，非零範圍 2～100 BB。UI 顯示籌碼取整值；實際動作再依合法下限與餘額限制。尺寸與全下頻率分離，快照保存設定。
- 節點集合使用 `postflop/v2-position-size-ranges`；舊近似桶保留原匹配語意。完整度只計通用基礎節點，不把局部位置設定當成全位置覆蓋。
- 新增回歸案例覆蓋區間邊界、多人相對位置、check-raise、持續下注歷史、覆寫隔離及快照、逐座／逐位置 open、實際 open 回應與合法動作限制。
- 瀏覽器人工操作已檢查 scoped 儲存、切換位置繼承、Hero／Bot 尺寸隔離、取整顯示及 UI 0.1.6 更新說明。修復開發伺服器牌力預覽 URL 空白解碼。
- Windows 安裝與實機 DPI／音效聽感仍需在 Windows 環境驗收；本機瀏覽器驗證不代表這些項目已完成。
