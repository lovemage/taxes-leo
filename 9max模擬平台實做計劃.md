# 9-max 德州撲克模擬平台 — 實做計劃（Windows 桌面版）

**現行規格依據**：[`9max平台核心規格.md`](9max平台核心規格.md)（產品、牌局、策略、統計、UX 與效能的權威基線）  
**牌局規則附則**：[`德州撲克規則細則.md`](德州撲克規則細則.md)（完全比照現實德州撲克規則）  
**配套 UI 規格**：[`UI面板詳細規格.md`](UI面板詳細規格.md)  
**版本**：v2.6（本機單人 6–9 人 Bot 桌）  
**日期**：2026-08-16
**目標**：完成可使用的 v1 —— 單機 Windows 桌面應用，可設定「自身策略 + 5～8 個 Bot（6–9 人可調，預設 9）」、調整全部參數、執行 play、視覺查看全部 log、並提供自身方勝率／EV 數據儀表板

---

## 〇、修訂摘要（v1 → v2 方向轉彎）

| 項目 | v1（原計劃） | v2（本版） |
|---|---|---|
| 部署形態 | Ubuntu 伺服器 + Web 控制台 + PWA + 手機 App | **單機 Windows 桌面應用** |
| 運算位置 | 伺服器 Rust worker（強調 hands/sec） | **本機 Rust 引擎**，速度非主要考量，正確性／可重現優先 |
| 資料層 | PostgreSQL + Redis + Parquet | **本機 SQLite／檔案**（無伺服器） |
| 核心重點 | 批次模擬 + 報表可信度 | **完整參數面板 + 策略編輯器 + play + 全量 log 檢視 + SVG 牌面** |
| 視覺參考 | 未明定 | **GTO Wizard 設計**（數據優先、範圍矩陣、頻率熱力色） |
| 帳號／訂閱／多使用者 | v1 即有（M4） | **全部移出**（後續階段） |

**保留下來的鐵則**：統計效力（CI、顯著性閘門）、不變量守恆、可重現性、BOT 參數 schema、策略 JSON 規格、誠實揭露（不稱「GTO 正解」）。

**v2.1 更新（依業主確認）**：

| 變更 | 內容 |
|---|---|
| 座位數可變 | 6-max～9-max（預設 9），Bot 可任意增減（最少 6 人） |
| 牌桌方向 | 橫向橢圓桌 |
| log 保留 | 全量保留 |
| 新增 | 本機方數據儀表板（勝率／EV 值，參考 GTO Wizard 呈現） |

**v2.2 修訂（依正確性審查）**：修正 3-max 位置規則；多人 Equity／All-in EV 改為多人聯合範圍＋分段分池；v1 排除 combo 下鑽 UI；籌碼改固定點精算；補 log 容量門檻、schema codegen、RNG／checkpoint 規範；M0 改為完整垂直切片；補牌手內容依賴與時程前提。

**v2.3 修訂（依業主 2026-08-15 決議）**：產品固定為本機單人操作，桌上其餘座位全為 Bot，不做真人多人連線；停用所有過期決議作為規格來源，重新建立現行權威規格；交付優先改為「參數呈現 → 統計呈現 → 操作順暢 → 牌桌動畫」；效能以 Intel Core Ultra 7 258V／32GB 筆電為硬體上限，正式驗收只能使用同級或更低規格。

**v2.4 修訂（依業主 2026-08-15 追加決議）**：

| 變更 | 內容 |
|---|---|
| 牌局規則 | 完全比照現實德州撲克，細則另立 [`德州撲克規則細則.md`](德州撲克規則細則.md) |
| 座位數 | **6–9 人（預設 9）**，推翻先前的 3–9；2／3／4／5-max 均不做 |
| 籌碼政策 | **破產離桌**；`stackBySeat` 為桌次開局起始深度，籌碼跨手結轉（深度數值於 v2.6 定為預設 200BB、可調） |
| 自動補位 | 可開關；開啟時 Bot 離桌後補入新 Bot，補位目標 6–9（下限 6），新 Bot 沿用該座位人格設定 |
| 統計主體 | **只有使用者座位**；Bot 不計入策略統計，只作對照顯示 |
| 手數設定 | **以 10K 為單位，範圍 10K～1000K** |

**v2.5 修訂（依 2026-08-16 文件審查與同日業主決議）**：牌局規則、桌型、統計主體、籌碼政策等產品方向均不變。本次修訂消除文件間的矛盾、補上會導致實作分歧的規格缺口、把兩個先前未被識別的可行性風險寫成 M0 閘門，並落實業主當日的兩項範圍決議（面板合併、位置命名）。**修訂後規格已就緒，無待決項，可直接進入開發。**

| 類別 | 內容 |
|---|---|
| **新增 M0 閘門** | ① Equity 每決策時間預算（批次 p50 ≤1 ms）實測；② preflop baseline 內容體積量化與產製管線定案。兩者是本計劃最可能推翻時程的風險，先前未列 |
| 消除矛盾 | 桌次結束人數門檻統一為 6（核心規格原寫 2）；「人數只減不增」補上「關閉自動補位時」前提 |
| 補規格缺口 | dead button／dead SB 手的位置標籤與統計歸屬（規則細則 8.4.1、測試向量 R24）；每手開始人數 ≥6 的不變量（R25）；有效籌碼 bucket 改半開區間 |
| 補統計效力 | 新增最小手數對照表（核心規格 5.3.1）與面板 A 的執行前效力預覽（UI 規格 F.5.1） |
| 補里程碑缺口 | M2 增列顧問校準用的最小報表工具；M4 補使用者文件、正式效能驗收與簽章憑證前置期 |
| 業主決議 | **面板 F 與 H 合併**為單一「報表與數據儀表板」，面板總數 8→7；**位置命名定案：最早位一律 `UTG`**（6 人桌為 UTG／HJ／CO）；工期與人力不列為待決項，直接進開發 |
| 業主追加需求 | **策略可儲存並命名**（策略庫，UI 規格 D.9）；**左側座位列表顯示位置 label**；Bot 可**快速複製／刪除**；可**一鍵重設為官方預設 9-max 組合**。連帶補上面板 B 的完整欄位級規格與「run 進行中鎖定 Bot 設定」的可重現性約束 |

**v2.6 修訂（依業主 2026-08-16 追加決議）**：

| 變更 | 內容 | 連帶影響 |
|---|---|---|
| **移除 Bot 等級** | 取消 L1–L5 難度分級。Bot 只由**人格**區分；原「等級層」10 欄保留為**逐座可調的行為參數**（`botBehavior`），因為它們是引擎管線的必需輸入（可用尺度、決策噪音、exploit cap） | Bot schema 三層 → **兩層**；Bot 組合 **35 組 → 7 組**；顧問校準與校準 run 排程同步從 35 輪降為 7 輪，**M2 的內容工作量大幅下降** |
| **起始深度預設 200BB** | 以整數 BB 保存。**仍可調整**，全桌統一設定並可逐座覆寫 | ① 有效籌碼 bucket 由 6 檔增為 **9 檔**（原以 `[125,∞)` 收尾會讓 200BB 開局全擠一桶）；② preflop baseline 格數 **21 萬 → 約 32 萬**；③ 桌次變長、桌次數減少，統計更倚賴 moving block bootstrap；④ 200BB 下 SPR 偏高，postflop 內容更複雜 |

> **兩項變更的淨效果相反**：移除等級省下 Bot 校準工作（35 → 7 組），200BB 則增加 preflop 內容量（21 → 32 萬格）。**內容仍在關鍵路徑上，M0 閘門 2 的重要性沒有下降。**

---

## 一、總策略

1. **產品形態**：單一 Windows 桌面程式（Win10/11 x64）。一桌固定為 **1 名本機使用者 + 5～8 個 Bot**，共 6-max～9-max（預設 9-max）；業主在本機完成「建桌 → 設 Bot → 調策略／手動決策 → play／模擬 → 看報表 → 逐手重播」完整循環，**不依賴任何伺服器，也不做真人多人連線**。
2. **架構鐵則**：Rust 引擎是牌局的**唯一權威**，前端（UI）只是 view。任何牌局邏輯（規則、下注、邊池、抽水、BOT 決策、統計）只在 Rust 引擎執行；UI 不複製、不重現任何遊戲邏輯，只透過 IPC 下指令、拿結果、畫畫面。
3. **速度不是目標**：不追求 hands/sec 極限。可接受較慢，但**必須**：(a) 完全可重現（同 seed + 同版本 → 同結果）；(b) 不變量全過；(c) UI 不卡頓（模擬在背景執行緒跑，畫面保持可操作）。
4. **產品優先級**：規則與資訊隔離正確 → 參數易懂好調 → 統計可信好讀 → UI 順暢 → 牌局動畫。動畫保留但只服務理解，不得阻擋操作、壓縮核心工期或進入批次模擬路徑。
5. **單一程式碼庫**：使用引擎／型別／schema 分層是為了維持本機程式的一致性與可測性，不以未拍板的手機、雲端或多人功能擴張 v1。

---

## 二、v1「可以使用」的定義

一台 Windows PC（一般 8–16 GB RAM、無需顯卡）安裝後，使用者可以：

| # | 功能 | 驗收條件 |
|---|---|---|
| 1 | **牌桌設定** | 盲注、籌碼深度（**預設 200BB**，可調、可逐座覆寫；破產離桌）、抽水（rake %、cap、no flop no drop）、ante（`none`／逐人／BB／BTN 四種模式）、straddle（含 double）、手數、亂數種子，全可調 |
| 2 | **可變座位（6–9 人，預設 9）** | 1 個「自身座位」＋ 5～8 個 Bot；左側座位列表顯示**當前手的位置 label**；Bot 可**新增／複製（含完整設定）／刪除／交換座位**（下限 6 人、使用者座位不可刪）；每個 Bot 可獨立選人格（7 種）與行為參數（**無等級分級**）；可**一鍵重設為官方預設 9-max 組合**；自身座位以醒目底色標示 |
| 3 | **Bot 參數（兩層 schema）** | 人格參數（11 欄）＋行為參數（10 欄）＋逐座位覆寫，全部可調（見面板 C） |
| 4 | **自身策略編輯器** | preflop 13×13 範圍矩陣、postflop 規則、下注尺度樹、fallback 全可調；**可儲存自己的策略並命名**，於策略庫中重新命名、複製、刪除、匯出與切換（見面板 D，GTO Wizard 風格） |
| 5 | **執行 play** | 兩種模式：**互動對打**（自己手動打 N 個 bot）與**批次模擬**（自身策略自動跑 N 手）；顯示進度、可暫停／取消 |
| 6 | **全量 log 檢視 + 重播** | 本機保存**全部**逐手 log，可瀏覽、篩選、逐手重播 1x–10x；每手可看「原始規則 → 人格修正 → 行為參數 → 正規化 → 最終選擇」 |
| 7 | **報表** | bb/100 ± 95% CI、樣本數、各位置結果、行為頻率、顯著性閘門 |
| 8 | **本機方數據儀表板** | 自身方 EV／bb/100、獲勝手數比例、逐位置／逐手牌 EV 熱力圖（GTO Wizard 式呈現，見面板 F；術語定義見 UI 詳規 F.2） |
| 9 | **SVG 牌面** | 52 張牌面＋牌背＋花色符號全部為 SVG（無點陣圖），任意縮放不模糊 |

**明確不做（產品邊界）**：真人多人連線、房間／配對／邀請／觀戰、伺服器權威牌局、帳號、雲端同步、真錢／籌碼交易。v1 另不做 9 人全局 CFR 求解、即時 re-solve、對局中學習／強化學習、GPU、PWA、手機 App 與 6-max 以下桌型（2／3／4／5-max 均不做）。真人多人連線不是後續預留項目；若未來提出，視為新產品範圍重新評估。

---

## 三、技術選型（已定案）

| 層 | 建議 | 理由 |
|---|---|---|
| 引擎 | **Rust**（沿用 v1 決策） | 正確性／不變量測試文化、可重現、單一權威；速度非主因但順帶夠快 |
| 桌面殼 | **Tauri 2.x**（Rust 主行程 + WebView2 前端） | 直接使用 Rust 引擎；前端用 React/TS；SVG 原生支援；安裝時偵測 WebView2，缺少時由 installer 安裝所需 runtime |
| 前端 | **React + TypeScript + Vite** | 範圍矩陣、圖表、重播器在 React 生態成熟 |
| 本機資料 | **SQLite**（schema version 化） | 存全量 log、策略、報表；免伺服器 |
| 圖表 | TS charts 套件（MIT/Apache，先做授權審查） | 報表視覺化 |

**非現行方向**：手機、Web、雲端與真人連線不納入本計劃，也不以其假設增加當前抽象層。若日後提出，另立產品與技術決策。

---

## 四、里程碑

### M0 — 垂直切片 + 技術驗證 spike（2–3 週）

**目標**：先打通一條最小完整路徑，並在開工前驗證所有高風險假設，降低後段整合風險。

| 工作 | 產出 |
|---|---|
| **垂直切片**：Rust 引擎發一手 9-max 牌 → IPC → SQLite 落 log → UI 渲染 → 逐手重播 | 證明「引擎→IPC→儲存→UI→重播」全鏈路可通 |
| GTO Wizard 介面研究：範圍矩陣、頻率熱力色、尺度樹 | UI 參考規格（面板 D、第六章） |
| 牌面 SVG spike：52 張牌面＋牌背＋花色路徑 | 可用的 SVG 牌面元件 |
| 引擎骨架 spike：最小規則循環 + 零和／籌碼守恆斷言（固定點） | 證明引擎路線可行 |
| **資訊集隔離 spike**：完整 `GameState` → 每座 `DecisionView`；以替換隱藏牌測試 Bot 輸出不變 | 證明 Bot 無隱藏牌存取路徑 |
| **多人 Equity spike**：6／9-max 代表節點比較 exact 與 deterministic sampling | 凍結 exact/sampling 門檻、sampling budget、誤差與 cache key |
| **Equity 時間預算實測（新增，最高風險）**：在 258V 基準機上量測 postflop 多人節點的單次決策成本 | 證明批次模式每決策 **p50 ≤1 ms、p99 ≤5 ms**（核心規格 7.3 由 12 小時／100 萬手反推）；分別凍結**批次**與**互動**兩組 sampling budget |
| **內容體積量化與產製管線（新增，關鍵路徑）**：精算 preflop baseline 的實際格數並決定產製方式 | 格數清單（桌型 × 位置 × bucket × 節點情境 × 169）＋ 產製方案（solver 批次產出／顧問抽查簽核／分批範圍）＋ 首批交付定義 |
| **面板欄位級規格補齊**：A／C／E／G 四個面板 | 補入 [`UI面板詳細規格.md`](UI面板詳細規格.md)，與既有面板 B／D／F 同層級；M3 不得在缺欄位級規格下開工 |
| ~~位置命名定案~~ **（已完成 2026-08-16）** | 最早位一律 `UTG`；6 人桌 UTG／HJ／CO。命名序列見規則細則 8.4.1，即 baseline 內容表的欄位鍵 |
| **變異數削減 spike**：CRN／duplicate 在 6–9 人下的實際削減倍率 | 決定 M2 estimator 決策表 |
| **log 容量測試**：10 萬／100 萬手的每手大小、DB 上限、寫入吞吐、分頁延遲 | log 容量規格定案（見下） |
| **258V 基準測試環境**：258V／32GB 或更低規格筆電，記錄 Windows、電源模式與 build | 後續所有效能驗收的固定基線 |
| 拍板 schema codegen、RunManifest、RNG、checkpoint 格式、凍結面板清單 | 決策紀錄 |

**閘門**：垂直切片全鏈路可跑通；DecisionView 隱藏資訊測試通過；多人 Equity 的 exact/sampling 路徑與誤差預算定案；參數矩陣與統計圖表在指定硬體流暢渲染；固定點不變量斷言通過。過不了就回頭調整技術選型，不進 M1。

**新增的兩道硬閘門（不過不進 M1）**：

1. **Equity 時間預算**：批次模式單一決策 p50 ≤1 ms、p99 ≤5 ms，在 258V 基準機實測。未達標時依核心規格 7.3 的處置順序調整（降 budget → 節點級 cache → 預算內近似並量化誤差 → 調降手數／時間門檻，需業主同意），**不得以提高硬體處置**。此閘門若失守而未處置，100 萬手 ≤12 小時的承諾在 M2 必然跳票。
2. **內容體積可行**：preflop baseline 的實際格數必須算出並有可執行的產製方案。粗估為 (9+8+7+6) 位置 × **9 個 stack bucket** × 7 種節點情境 × 169 格 ≈ **32 萬格頻率分佈**，且每格是多 action 的分佈而非單一數字——這不是顧問逐格手填得完的量。M0 未定案產製管線即進 M2，M2 會在第一週卡死。
   > **格數已因 200BB 決議上修**：bucket 由 6 檔增為 9 檔（規則細則 8.5 為 200BB 起始加了上界檔位），內容量從約 21 萬格增為約 32 萬格。若 M0 實測顯示最深的兩檔策略差異不足以支撐獨立內容，可合併 `[240,400)` 與 `[400,∞)`，格數回到約 28 萬。

**M0 工期說明**：原估 2–3 週未含面板欄位級規格補齊。若四個面板（A／C／E／G）的欄位級規格併入 M0，M0 為 **3–4 週**；若另行安排，須在 M3 開工前完成，並於時程表獨立列項。

**log 容量規格（M0 定案，M2 驗收）**：

| 項 | 門檻 |
|---|---|
| 每手 log 大小 | 由 DB 上限反推：2 GB ÷ 100 萬手 ≈ **2 KB／手**（含 index 攤提）。此為推導值不是獨立門檻；**不得為壓進 2 KB 而刪除決策 trace 的必要欄位**，寧可調整序列化格式或壓縮策略。<br>**初步實測（2026-08-16，緊湊二進位編碼）**：9-max 全跟注情境每手 **229 位元組**（含 SQLite page 與 index 開銷），外推 100 萬手約 **218 MB**。全跟注是行動數的高估情境（實戰多數手在翻前結束），因此仍有約 1.8 KB／手餘裕供 M2 的決策 trace 使用。正式數值仍依 258V 基準機重測 |
| 100 萬手 DB 上限 | ≤ 2 GB（含 index，不含可安全清除的 WAL 暫存） |
| 寫入吞吐 | 在核心規格第七章基準下完成 10 萬手 ≤60 分鐘、100 萬手 ≤12 小時，且 UI 門檻仍通過 |
| 分頁／重播延遲 | 100 萬手 DB 載入指定手牌 p95 ≤ 200 ms；cold／warm 分開記錄 |
| 壓縮 | SQLite ＋逐手事件緊湊序列化；完整 RunManifest 與內容快照不可省略；重播以事件為主 |
| 清理／匯出 | 全量保留 ＋ 可匯出 JSON ＋ 可依日期範圍刪除 |

### M1 — 引擎 Layer 0（約 4–6 週）

- 依 [`9max平台核心規格.md`](9max平台核心規格.md) 第二章與 [`德州撲克規則細則.md`](德州撲克規則細則.md) 建立完整規則：6–9 個位置、逐座籌碼、straddle（含 double）、ante（`none`／`perPlayer`／`bbAnte`／`btnAnte` 四種模式）、rake（%＋cap＋布林 no-flop-no-drop）、side pot、split pot 與 odd chip
- **合法行動產生器**依規則細則第一章實作：最小加注額、短額全下不重開加注權（增額不累加）、straddle 後的行動順序與 option
- **破產離桌與位置輪轉**依規則細則 8.3／8.4 實作：桌次生命週期、dead small blind、dead button、BB 不被跳過
- **位置標籤**依規則細則 8.4.1 產生並寫入每手 log（含 `deadButton`／`deadSmallBlind` 旗標）。行動順序與位置標籤是兩套邏輯，必須分離；統計層與策略層一律讀 log 欄位，不得自行從座位索引推導
- **座位數不變量**：每手發牌前斷言在桌人數 ≥6（核心規格 2.1），與籌碼守恆同級，失敗即中止 run
- Hand evaluator，對照公開測試向量驗證
- **規則測試向量 R1–R25**（規則細則第九章）全數通過
- **完整狀態與資訊集分型**：`StrategyProvider` 只接受 `(DecisionView, OwnRange, OpponentRangeEstimates) → ActionDistribution`；完整 `GameState` 不得進入 Bot API
- **驗收（鐵則，不過不准往上蓋）**：
  - 籌碼、rake、side pot、odd chip、ante/straddle 一律以**最小籌碼單位（整數）或 fixed-point** 精確計算；**每手籌碼守恆誤差必須為 0**（不做浮點容差）；浮點只出現在統計輸出（bb/100、CI）
  - 統計層零和抽核 `(N/100) × Σbb100ᵢ + rake_bb = 0`（浮點，容差依 estimator 訂，屬報表級而非規則級）
  - ≥3 人不等額 all-in 分池案例、odd chip、straddle/ante 下注順序與最小加注額
  - Evaluator 測試向量全過
  - **可重現性**：完整 RunManifest 保存設定與策略／Bot 快照，不只保存 hash；RNG 依發牌、策略取樣、Equity 計算分 domain stream；暫停／續跑與不同平行度逐事件一致
  - **資訊隔離**：替換不可見牌但保持 DecisionView 與 seed 相同時，ActionDistribution 逐位元一致

### M2 — 策略與 BOT 層（約 6–8 週）

| 項目 | 規格來源 |
|---|---|
| RangeTracker：自我 range＋對手 reach-weighted range；只使用公開資訊 | 核心規格 2.4／5.1 |
| Preflop：`node × handClass × action × size → frequency`，v1 UI 編輯 169 類 | 核心規格 4.1 |
| Postflop：支援多人相對位置、存活人數、當前街 action history 與規則遮蔽檢查 | 核心規格 4.2 |
| **7 組人格**（無等級分級）；人格層與行為層兩層參數依具名公式與 generated schema 套用 | 核心規格 4.3 |
| 多人 Equity：低狀態空間 exact，大型節點 deterministic sampling | 核心規格 5.1 |
| Analytics：paired／batch means／cluster bootstrap／Wilson／FDR 依決策表選擇 | 核心規格 5.3 |
| 報表：CI 跨 0 標「無法判定」；樣本不足另依樣本與 CI 寬度門檻判定 | 核心規格 5.3／5.4 |
| **顧問校準工具（新增）**：供牌手顧問在 M2 期間校準 7 組人格參數與簽核報表判讀標準的最小介面 | 見下方說明 |

**內容依賴（牌手顧問，關鍵路徑）**：

| 交付物 | 規模 | 格式 | 時點 |
|---|---|---|---|
| 6–9 人 preflop baseline 表（逐位置 × **全部有效籌碼 bucket** × 169 格頻率） | **約 32 萬格**（(9+8+7+6) 位置 × **9 bucket** × 7 節點情境 × 169），每格為多 action 的頻率分佈。bucket 因 200BB 起始由 6 檔增為 9 檔 | JSON（策略 schema 相容） | M2 起前（首批），其餘分批 |
| postflop heuristic 規則集初版 | 規則條數由 M0 凍結的面板 D.5 維度決定 | 規則清單（面板 D.5 欄位） | M2 中期 |
| **7 組 BOT 人格參數校準** | 7 人格 × 21 欄參數（人格 11 ＋ 行為 10）。**移除等級後由 35 組降為 7 組** | bot-config schema 值 | M2 中期 |
| 報表判讀標準與簽核 | — | 文字規格 | M2 末 |

**內容規模警語（關鍵路徑，M0 必須先定案產製方式）**：32 萬格不是顧問逐格手填得完的量。M0 的「內容體積量化與產製管線」閘門必須先決定產製方式——例如以開源 solver 批次產出後由顧問**抽查與簽核**、或先交付高頻節點的子集並明確標示其餘走 fallback——並據此定義「首批交付」的範圍。**在產製管線定案前，M2 的 6–8 週不成立。**

每批內容由牌手顧問簽核後才進引擎；未簽核不得作為 baseline 上線。抽查簽核模式下，簽核對象是**產製方法與抽樣結果**，不是逐格內容，此差異須在驗收文件寫明。

**顧問校準工具（新增，解決 M2 依賴 M3 UI 的順序問題）**：

M2 要求顧問校準 7 組人格參數並簽核報表判讀標準，但所有面板都排在 M3——顧問在 M2 期間沒有任何介面可看。M0 的垂直切片 UI 不足以支撐參數校準。因此 M2 必須包含下列其一：

- **選項 A（建議）**：CLI ＋ 靜態 HTML 報表輸出。工程成本低，顧問可離線審閱，產物可直接附在簽核文件。
- **選項 B**：把面板 C（Bot 參數）與面板 F（報表）的最小唯讀版提前到 M2，M3 再補完整編輯功能。成本較高但成果可沿用。

不做此工具則顧問無法在 M2 內完成校準，M2 的簽核交付會實質延後到 M3。

**驗收**：在不高於 258V／32GB 的筆電上，10 萬手 ≤60 分鐘、100 萬手 ≤12 小時、working set ≤12GB、100 萬手 DB ≤2GB、指定手牌 p95 ≤200ms；批次期間參數與報表 UI 仍通過核心規格第七章；**7 種 BOT 人格**各完成至少 1 萬手且趨勢落入顧問簽核區間。

> **Bot 校準量測的定位（釐清）**：核心規格 5.0 規定「Bot 不計入策略統計」，指的是**產品報表**的統計主體只有使用者座位。上述「7 種 BOT 各 1 萬手」屬**開發期專用的內部校準量測**，走獨立的量測管線，不進產品報表、不進顯著性判定，也不出現在面板 F。兩者不衝突。
>
> 另注意：一桌只有 5–8 個 Bot 座位，要讓 7 種人格各自累積 1 萬手需要**專門設計的校準 run 排程**（例如每輪固定一組人格佔滿全部 Bot 座位）。移除等級後排程從 35 輪降為 7 輪，成本大幅下降。

### M3 — 桌面應用 UI（約 6–8 週）→ **「可以使用」里程碑**

**開工前提**：面板 A／C／E／G 的欄位級規格必須已補入 [`UI面板詳細規格.md`](UI面板詳細規格.md)（M0 凍結項）。目前該文件有面板 B／D／F 的欄位級規格；在缺欄位級規格的狀態下開工，6–8 週的估算不成立。

- Tauri 殼 + React 前端；引擎以 Tauri command（IPC）暴露，前端零遊戲邏輯
- 七大面板全部落地（見第五章）；實作順序為**牌桌／Bot／策略參數 → 報表／儀表板 → 執行與 log → 牌桌動畫 polish**
- 面板 A 的手數設定須實作執行前統計效力預覽（UI 規格 F.5.1）
- 互動對打（手動 play N 個 bot）＋批次模擬（背景執行緒，不阻塞 UI）＋逐手重播
- 全量 log 落 SQLite，可瀏覽／篩選／逐手重播
- SVG 牌面套用全站；牌局動畫支援 off／fast／normal，不得影響引擎狀態或批次路徑
- **驗收**：單機從「建桌 → 設 Bot → 調參數／策略 → 使用者手動 play 或批次模擬 → 看報表 → 逐手重播」全流程可用；參數與統計呈現通過 UI 詳規；核心規格第七章 UX 效能全過；invariant 未通過的模擬不出報表

### M4 — 收尾與發佈（約 1–2 週，不含下列前置與驗收作業）

- 策略／log 匯入匯出（JSON）
- 打包 **portable exe + installer** 兩版（NSIS／MSI）、圖示、版本號
- Windows 分發注意：程式碼簽章、SmartScreen／防毒誤報處理（見第八章風險）
- 內建基準策略版本標示；更新採具版本與簽章的本機內容包匯入，不建立帳號或雲端同步

**先前未列入、必須另行安排的項目**：

| 項目 | 說明 | 建議時點 |
|---|---|---|
| **程式碼簽章憑證採購** | OV／EV 憑證需公司驗證，前置期通常 1–3 週，且為**年費成本**（尚未列入報價）。EV 憑證另需硬體 token 或雲端 HSM | **M0 啟動**，M4 才申請會卡發佈 |
| **正式效能驗收** | 核心規格第七章全部門檻在 258V 基準機上完整跑一輪，含 100 萬手批次（單次即 12 小時）與冷／warm 分開記錄 | M3 末～M4，需獨立排入約 1 週 |
| **儲存層驗收** | 核心規格第八章的 schema migration、崩潰復原、2GB DB 搜尋、匯出與清理 | M3 末，目前未排入任何里程碑 |
| **使用者說明文件** | 業主要能自行操作全流程與判讀報表；含統計術語與「無法判定」的解釋 | M4 |
| **圖表套件授權審查** | 技術選型表已列「先做授權審查」但未指定時點與負責人 | M0，與技術選型一併定案 |
| **WebView2 分發** | installer 需打包 WebView2 bootstrapper 並處理離線安裝情境 | M4 |

---

## 五、UI 面板規格（v2 核心，完整參數面）

所有參數都要能調，且**任何修改都要即時驗證合法**（頻率合計 100%、參數在合法區間），非法即標紅並給修復提示。

> **欄位級規格狀態**：本章是面板的**範圍界定**，不是欄位級規格。目前面板 B／D／F 在 [`UI面板詳細規格.md`](UI面板詳細規格.md) 有欄位級規格；**A／C／E／G 四個面板尚未撰寫**，列為 M0 凍結項，M3 開工前必須補齊。

### 面板 A — 牌桌設定

| 群組 | 參數 |
|---|---|
| 盲注 | SB／BB 金額 |
| 籌碼深度 | 桌次開局起始深度，以整數 BB 輸入，**預設 200BB**；全桌統一設定並可逐座覆寫 |
| 籌碼政策 | **（唯讀說明，非可調參數）** 固定 `bustOut`：籌碼歸零者離桌、籌碼跨手結轉、不實作既有玩家補碼。**關閉**自動補位時人數只減不增；開啟時由補位補入新 Bot |
| 桌次結束條件 | **（唯讀說明，非可調參數）** 使用者破產／補位關閉時人數降至 6 以下／達手數上限，任一即結束並開新桌次。另有硬性不變量：任一手開始時在桌人數必須 ≥6 |
| 自動補位 | 開關 ＋ 補位目標人數（6–9，下限 6，不得大於開桌人數） |
| 抽水 | rake %、每手 cap、`noFlopNoDrop` 布林開關；未發 flop 時是否不抽水 |
| Ante | `none`／逐人 ante／BB ante／BTN ante（四種模式），金額 |
| Straddle | 無／單 straddle／double straddle，金額與位置；後段須為前段 2 倍 |
| 手數 | **僅適用批次模擬**，以 10K 為單位，範圍 10K～1000K。滑桿旁須即時顯示該手數的**預期統計效力**（預期 CI 半寬、可分辨差距、逐位置可判定列數、熱力圖達標格數），見 [`UI面板詳細規格.md`](UI面板詳細規格.md) F.5.1 |
| 互動對打手數 | **互動模式不設手數上限**，打到使用者主動結束或使用者破產為止；手數欄位在互動模式下隱藏而非顯示為 10K |
| 亂數 | 種子（seed），固定或隨機 |

### 面板 B — 座位與 Bot 設定（6–9 人，預設 9，可增減）

- **左側座位列表常駐**：每列顯示「**位置 label**／座位號／名稱／人格 × 等級／籌碼／狀態點」。自身座位置頂並以醒目底色標示
- **位置 label**：取自引擎依規則細則 8.4.1 寫入每手 log 的欄位，隨每手輪轉；**UI 不得自行從座位索引推導**。dead button／dead SB 手該手無 `BTN` 或 `SB` 標籤，列表須能呈現「本手無此位置」
  - **更新頻率**：互動模式每手即時更新（可加輪轉動畫）；**批次模式不逐手更新**（低頻取樣 ≥500 ms，否則違反核心規格 7.2 的 UI 門檻）；重播依進度更新
- **Bot 操作**：新增／**複製（含完整設定：人格、行為參數、逐座覆寫、起始深度）**／**刪除**／交換座位／匯入匯出
  - 上限 9 人、**下限 6 人**（刪到 6 人即禁用）；**使用者座位不可刪除**
  - 複製需有空位；滿桌時須選定覆蓋目標並二次確認
- **重設為官方預設**：一鍵回復「官方預設 9-max 組合」，另提供魚桌／Reg 桌／混合桌樣板。可選**單座重設**（僅該座，不改人數）或**全桌重設**（座位數一併回到 9 人，須明確告知）
- **自動補位**：可開關；開啟時 Bot 破產離桌後於下一手前補入新 Bot，補回目標人數（6–9，下限 6），新 Bot 沿用該座位的人格與行為參數。**補位一律發生在手與手之間**，且任一手開始時在桌人數必須 ≥6
- 逐座：人格（P1–P7）＋行為參數（10 欄），**無等級分級**；人格可重複、任意設置
- **變更時機（可重現性鐵則）**：**run 進行中（含暫停）一律鎖定為唯讀**，不得增刪、複製、交換 Bot 或改參數——`RunManifest` 保存的是 run 開始時的 Bot 快照，且 Bot 組合須在整個 run 維持不變（核心規格 3.3、規則細則 8.1.1）。要改設定須先停止該 run，變更後屬新的 run
- 欄位級詳細規格：見 [`UI面板詳細規格.md`](UI面板詳細規格.md) 面板 B

### 面板 C — Bot 參數（兩層 schema，逐層可調）

> **2026-08-16 決議：移除 Bot 等級（L1–L5）。** Bot 只由人格區分，不再有難度分級。原「等級層」的 10 欄**保留為逐座直接可調的行為參數**——它們是引擎管線的必需輸入（可用尺度、決策噪音、exploit cap 等），不是分級的附屬品。Bot 組合因此從 35 組（7 × 5）簡化為 **7 組人格**。

**人格層（persona，11 欄）**：`rangeWidth`、`preflopAggression`、`postflopAggression`、`callPersistence`、`foldDiscipline`、`bluffFrequency`、`valueThreshold`、`betSizePreference`、`trapFrequency`、`tiltResistance`、`adaptationStyle`

**行為層（botBehavior，10 欄）**：`decisionNoisePp`、`preflopCoverage`、`postflopBucketCount`、`allowedBetSizes`、`bluffComplexity`、`multiStreetPlanningDepth`、`opponentModelEnabled`、`opponentModelUpdateHands`、`exploitAdjustmentCapPp`、`explanationDepth`。每欄的型別、單位、範圍、預設值與說明由 Rust structs 產生的 schema 定義。

**逐座覆寫**：兩層的任一欄皆可逐座覆寫，顯示「預設值 → 覆寫值」對照；最終行動依核心規格 4.3 的固定管線產生，逐步保存 baseline、persona delta、行為參數、覆寫、legal mask、noise 與最終分佈。

### 面板 D — 自身策略編輯器（GTO Wizard 風格，資訊密度最高）

- **preflop 範圍矩陣**：13×13 網格以 action tab／分色圖層編輯 Fold／Call／Raise(size)／All-in 分佈；每手牌在合法行動間合計 100%；**v1 UI 僅 169 格**，combo 下鑽列後續
- **postflop 規則清單**：依優先序排列；支援多人存活數、相對行動順位、當前街與前街 action history、有效籌碼與 SPR，並提示重疊、遮蔽與不可達規則
- **下注尺度樹（betSizeTree）**：預設尺度清單以 [`UI面板詳細規格.md`](UI面板詳細規格.md) D.6 為唯一定義來源（Pot 內 25%／33%／50%／66%／75%／100%，overbet 125%／150%／200%，加 All-in）；尺度可增刪，自訂上限 300% Pot
- **fallback**：未命中規則時的保底行動（基準策略）
- **策略庫（儲存與命名）**：使用者可把自己的策略**存檔並命名**，之後叫回來用或互相比較
  - 列表顯示名稱、official／custom、版本、桌型涵蓋、逐桌型完整度、更新時間；可搜尋、排序
  - 操作：開啟／**重新命名**／複製為新策略／刪除／匯出 JSON／設為執行用策略
  - 名稱必填、庫內唯一，重名即時標紅不得靜默覆蓋；儲存自動遞增版本，另存新檔產生新 `id`
  - official 策略唯讀，編輯時自動轉為「複製為 custom 後編輯」
  - **run 開始時已把策略完整內容快照進 `RunManifest`**，因此日後改名、修改或刪除策略都不影響既有 run 的重播與統計；反之，同一策略的不同版本產生的 run 不得合併統計
- **驗證**：節點頻率合計 100%；不合法行動先移除再正規化；儲存前即時提示 fallback 覆蓋率（策略完整度）
- 此面板**預設收合**，依核心規格第六章採漸進揭露；常用值固定可見，進階規則才展開
- 欄位級詳細規格與編輯 UX：見 [`UI面板詳細規格.md`](UI面板詳細規格.md) 面板 D（策略庫見 D.9）

### 面板 E — 執行

- 模式切換：互動對打（play）／批次模擬（simulate）
- 進度、預估時間、暫停／續跑／取消
- 批次模擬在背景執行緒跑，不播動畫、不阻塞 UI
- 互動對打：自身座位手動行動，N 個 bot 自動決策，即時動畫

### 面板 F — 報表與數據儀表板（原面板 F ＋ 原面板 H 合併）

> **2026-08-16 業主決議：面板 F 與面板 H 合併。** 統計主體限縮為「只有使用者座位」後，兩者報告同一母體、欄位重疊約八成，分立已無意義。**面板 H 不再存在**，面板總數為七個（A／B／C／D／E／F／G）。

- **範圍選擇器**：本次 run／指定 run／全部 run；顯示桌型、策略版本、Bot 組合、engine 版本與時間區間。跨 engine 版本不得合併統計
- **整體**：EV／bb100 ± 95% CI ＋ estimator 名稱、總盈虧、獲勝手數比例（分子／分母＋區間）、樣本數、All-in EV、σ／σ₁₀₀、最大回撤
- **顯著性**：顯著性閘門、切片有效樣本（以 block 數計）、最虧三類情境、策略完整度（翻前／翻後與逐桌型）
- **逐位置**：依當手在桌人數切片；dead button／dead SB 手預設排除並顯示排除手數
- **行為頻率**：VPIP／PFR／3-bet／C-bet／Fold to C-bet／WTSD／W$SD，各附分子／分母與區間
- **桌次卡**：桌次數、平均存活手數、結束原因分佈、補位次數
- **逐手牌 EV 熱力圖**：13×13 範圍矩陣，可切換 EV／Equity／使用頻率，設樣本閾值；定位為探索性，不對單格斷言優劣
- **逐街**：preflop／flop／turn／river 的 EV 與頻率分解；大型切片預設標示探索性
- **對照**：自身 vs 全桌 Bot 平均的 EV 差距、被剝削／剝削幅度
- CI 跨 0 → 「本樣本無法判定優劣」標示；樣本不足的手牌格以灰階標示，不顯示假精確（呼應強制規則 8）
- **統計主體只有使用者座位**（核心規格 5.0）；Bot 數字僅作對照，不進入顯著性判定
- 欄位級規格、指標定義與算法對照：見 [`UI面板詳細規格.md`](UI面板詳細規格.md) 面板 F

### 面板 G — Log 檢視器／重播

- **全量手瀏覽**：逐手列表，依座位／街別／輸贏／牌型篩選與搜尋
- **重播器**：1x–10x；逐街、逐座位行動與尺度；底牌、公共牌、籌碼、底池
- **逐行動原因**：原始規則 → 人格修正 → 行為參數 → 合法正規化 → 最終選擇
- 手牌細節：底池賠率、SPR、範圍估計

---

## 六、SVG 牌面與視覺規格（參考 GTO Wizard）

> **視覺方向的權威規格在 [`UI面板詳細規格.md`](UI面板詳細規格.md) 第 V 章**（App shell、色彩 token、字型、**間距與圓角**、**圖示**、**資訊密度**、行動編碼、牌桌視覺、四色牌、HUD、資料表格、設定面板元件）。本章只保留產品層級的原則；切版一律以 V 章為準。
>
> **參考素材**：`mockups/v1.4-current/` 的 `maxresdefault.jpg`、`692db06a6ef2bcf59251fe21_Use this.avif`（Table Wizard 版面與元件語彙）、`image.png`（GTO Wizard 資料表格密度）。
>
> ⚠️ **`01-main-workspace.png`（Codex 首版視覺草稿）業主已明確否決，不是重做基礎。** 落差對照見 V.14；不得在該草稿上修改。

1. **全 SVG，零點陣**：52 張牌面（花色符號為 SVG path + 角標 + 中央點陣排列）、牌背（SVG 花紋，預留換膚）、四花色符號（♠♥♦♣）均為向量；任意縮放不模糊、不發糊。**預設四色牌組**（♠ 淺灰／♥ 紅／♦ 藍／♣ 綠），可切回傳統兩色；花色符號必須始終繪出，顏色只是加速辨識（V.9）。
2. **範圍矩陣**：13×13 網格，頻率以熱力色呈現（GTO Wizard 式）；v1 僅顯示 169 格頻率，combo 權重顯示列後續。
3. **數據優先、冷靜深色介面**：**單一強調色（綠）**、無賭場符碼（金籌碼堆、賭場紅黑金、拉霸特效、絨布材質），主視覺以參數面板、儀表板與範圍矩陣為主，牌桌為功能畫面而非主視覺。整體調性須是**專業分析工具**，不是遊戲畫面。
4. **牌桌（橫向橢圓）**：**細線輪廓、無絨布**；6–9 個**圓形位置節點**環繞，節點內顯示位置 label，自身座位醒目；行動以**字母 chip**（F／X／C／B／R／AI）緊貼節點顯示（V.7／V.8）。手牌／公共牌用 SVG；發牌、下注、收池、showdown 動畫服務於理解，可 off／fast／normal。動畫是次要交付，不得優先於參數與統計呈現。
5. **不讀牌**：Bot 只顯示其可見資訊，**任何情況下不可見其他座底牌**（公平性鐵則）。

---

## 七、Monorepo 結構（沿用 v1，桌面化）

```
packages/
├── poker-types      # 牌、動作、遊戲狀態型別（前端/引擎共用）
├── bot-config       # 人格／行為兩層參數 schema 與驗證（前端/引擎共用）
├── api-client       # 桌面 IPC 客戶端（替代 REST，走 Tauri command）
├── validation       # 頻率合計、參數區間、不變量檢查
└── charts           # 報表圖表元件
apps/
├── desktop          # Tauri 桌面殼（M3 起）
├── ui               # React 前端（M3 起）
├── engine           # Rust 規則引擎（M1 起，**零外部相依**）
└── storage          # Rust 儲存層：事件 log、RunManifest、重播（SQLite）
```

- **引擎與儲存層分成兩個 crate**：`engine` 只放規則，不引任何外部套件，測試快且不受套件版本影響；SQLite（rusqlite，bundled）只出現在 `storage`。`storage` 依賴 `engine`，反向不成立。
- `api-client` 對桌面版變成 **IPC 型別化客戶端**（Tauri command 的型別化包裝），前端仍「不直接碰引擎」。
- **型別單一來源**：以 **Rust structs 為單一來源**，用 Specta／ts-rs 產生前端 TS 型別、schemars 產生 JSON Schema 供 `bot-config`／`validation` 驗證；IPC 邊界（Tauri command）由同一來源產生型別化客戶端，避免 Rust 與 TS 各自手寫型別漂移。

---

## 八、強制規則（不可放寬）

1. 每個 bb/100 附 95% CI；CI 跨 0 不得列入「主要優勢／漏洞」。
2. 對外不說「GTO 正解」，改用「近似均衡基準」；VPIP/PFR/3-bet 是觀測值，不是輸入旋鈕。
3. v1 不顯示單一 exploitability 數字。
4. 全量 log 存本機（SQLite）供逐手重播；報表只用聚合結果＋CI，不拿逐手盈虧直接下結論。
5. Invariant 未通過的模擬一律不出報表。
6. 牌局邏輯只在引擎執行，UI 永不放遊戲邏輯；Bot 永不讀其他座位底牌。
7. 同 seed＋同版本＋同設定 hash 重跑逐位元一致；RNG 演算法與 checkpoint 格式明定（見 M1）。
8. 面板 F 的每個 EV／比例數字一律附樣本數與 CI；樣本不足的手牌格以灰階標示「樣本不足」，不顯示假精確。
9. 桌上除本機使用者外全部為 Bot；不建立真人多人連線、伺服器牌局或帳號路徑。
10. 所有效能驗收使用 258V／32GB 或更低規格筆電，不得改用更高硬體通過門檻。
11. 具規範力的文件只有核心規格開頭列出的四份現行文件。未列入者——包含已移除的過期決議文件、簡報、報價附件與任何口頭結論——不得作為需求、開發或驗收依據。
12. 位置標籤由引擎依規則細則 8.4.1 產生並寫入每手 log；UI 與統計層只讀該欄位，不得自行從座位索引推導。
13. 任何一手開始時在桌人數必須 ≥6，引擎不得發出 5 人以下的手牌（與籌碼守恆同級的不變量）。
14. 介面與文件不使用「Win Rate」一詞；獲勝手數的指標稱「獲勝手數比例」，bb/100 直接寫 bb/100（見 UI 規格 F.2）。
15. 批次與互動兩種模式使用不同的 Equity sampling budget，兩者均寫入 `RunManifest`；不同 budget 的 run 不得直接比較統計。

---

## 九、時程總覽

| 里程碑 | 工期 | 累計 | 產出 |
|---|---|---|---|
| M0 垂直切片＋spike | 2–3 週（＋面板欄位級規格則 3–4 週） | 0.5–1 個月 | 全鏈路垂直切片、SVG 牌面、log 容量規格、變異數削減實測、**Equity 時間預算實測**、**內容產製管線定案**、六面板欄位級規格 |
| M1 引擎 Layer 0 | 4–6 週 | 約 2–2.5 個月 | 通過全部 invariant 的規則引擎 |
| M2 策略與 BOT | 6–8 週 | 約 3.5–4.5 個月 | 7 組 BOT 人格、自身策略、可信統計 |
| M3 桌面 UI | 6–8 週 | 約 5–6.5 個月 | **v1 可以使用** |
| M4 收尾發佈 | 1–2 週 | 約 5.5–7 個月 | Windows installer |

> **時程前提**：M2 的 6–8 週以牌手顧問內容**依 M2 內容依賴表時點交付並簽核**為前提；內容延遲會直接拖長 M2。在內容交付量與格式定案前，5.5–7 個月僅為粗估，不作為承諾。

> **估算性質**：上表為樂觀值，未含整合、缺陷修復與驗收往返的緩衝；累計欄為純序列加總。**業主決議不將工期與人力列為待決項，直接進開發**，故本表僅作規劃參考，不阻擋任何里程碑。M1 與 M2 的內容量偏重（M1 含確定性平行合併，M2 含多人 Equity 與整套 estimator），實際進度以閘門通過與否為準，不以週數為準。
>
> **另須留意未排入里程碑的工作**：儲存層驗收（schema migration、崩潰復原、2GB DB 搜尋）、正式效能驗收（100 萬手單次即 12 小時）、使用者文件——三者見 M4 的補充表。

---

## 十、人力需求

| 角色 | 人數 | 投入期間 | 負責 |
|---|---|---|---|
| Rust 工程師 | 1 | M0–M3 全程 | 引擎、BOT、統計、不變量測試 |
| TS/React 前端工程師 | 1 | M3–M4；**建議自 M1 起並行投入**（SVG 牌面、範圍矩陣、圖表元件不依賴引擎完成度） | UI、SVG 牌面、範圍矩陣、重播器、圖表 |
| 撲克專家（牌手） | 1，兼職 | M2 內容交付與簽核 | preflop baseline 表、策略 JSON 內容、報表判讀、人格參數校準 |
| UI/視覺（可與前端兼任） | — | M0 風格規範；M3 落地 | GTO Wizard 風格規範、牌面與牌桌視覺 |
| QA／測試 | 未獨立配置，由工程師兼任 | M1 起 | 核心規格第八章驗收矩陣的執行：合成資料驗 estimator、schema migration、崩潰復原、2GB DB 搜尋、效能驗收 |

> **驗收工作由工程師兼任**（業主決議不另配 QA）。第八章的驗收矩陣工作量不小，其中 100 萬手效能驗收單次即需 12 小時，排程時須把這段機器時間算進去，不要壓在里程碑最後一天。

> 牌手顧問的內容交付（preflop baseline、postflop 規則、7 組人格參數、簽核）為**關鍵路徑**，時點見 M2 內容依賴表；顧問延遲無法以工程資源補救。內容規模約 32 萬格（200BB 起始使 bucket 增為 9 檔），產製方式須於 M0 定案（見 M0 閘門 2）。

---

## 十一、風險與對策

| 嚴重度 | 風險 | 對策 |
|---|---|---|
| **高** | **Equity 成本壓不進批次時間預算**：12 小時／100 萬手 = 43 ms／手，一手約 20–40 個決策 → 每決策僅約 1 ms，而 postflop 多人節點需跑分層 Monte Carlo。此風險先前未列，若在 M2 才發現，100 萬手門檻會直接跳票 | M0 新增硬閘門實測；批次與互動分別凍結 sampling budget；未達標依核心規格 7.3 的處置順序（降 budget → 節點 cache → 預算內近似並量化誤差 → 調整門檻需業主同意），**不得提高硬體** |
| **高** | **preflop baseline 內容體積被低估**：約 21 萬格頻率分佈，在關鍵路徑上，顧問無法逐格手填 | M0 新增閘門：精算格數並定案產製管線（solver 批次產出＋顧問抽查簽核／分批交付＋明示 fallback 範圍）；未定案不進 M2 |
| 中 | **四個面板缺欄位級規格**（A／C／E／G），M3 的 6–8 週估算不成立 | 列為 M0 凍結項；M3 開工前補齊。凍結「面板清單」不等於凍結「欄位」 |
| 中 | **M2 的顧問校準缺介面**（UI 全在 M3） | M2 增列顧問校準工具（CLI＋靜態 HTML 報表，或面板 C／F 唯讀版提前） |
| 中 | 範圍矩陣 + 完整參數面板是大量前端工，範圍蔓延 | M0 凍結面板清單；策略編輯器分階段（先參數／矩陣，後 combo 下鑽） |
| 中 | **策略檔桌型涵蓋不足**：破產離桌使一張 9-max 桌在 run 內經歷 9／8／7／6 四種桌型，只為 9-max 校準的策略會大量落入 fallback，使用者卻以為在測自己的策略 | 策略檔顯示逐桌型完整度；執行前警告預估落在未校準桌型的手牌比例（UI 規格 D.3） |
| 中 | **簽章憑證前置期卡發佈**：OV／EV 憑證需公司驗證，1–3 週且為年費成本，尚未列入報價 | M0 啟動申請；成本另行報價確認 |
| 中 | 引擎正確性出錯 → 產品是「漂亮的隨機數產生器」 | invariant 測試列驗收；逐手重播與原執行逐位元一致 |
| 中 | 統計做錯 → 結論無意義 | 依核心規格第五章的 estimator 決策表、合成資料與多重比較驗收逐條查 |
| 中 | **業主對統計效力的預期落差**：10K 手下幾乎所有切片都會顯示「無法判定」，易被誤認為產品故障 | 核心規格 5.3.1 的最小手數表；面板 A 執行前效力預覽（UI 規格 F.5.1）；熱力圖明確定位為探索性 |
| 低 | 多人 Equity 全枚舉不可行 | M0 凍結 exact／deterministic sampling 門檻與誤差預算 |
| 低 | Bot 誤讀隱藏牌 | 完整 GameState 與 DecisionView 分型；替換隱藏牌測試列 M0／M1 閘門 |
| 低 | Windows 分發：SmartScreen／防毒誤報、未簽章 | M0 啟動簽章與 WebView2 分發規劃；M4 提供 portable 與 installer 兩版 |
| 低 | 跨 IPC 的確定性被破壞（UI 偷算） | 鐵則 6：UI 零遊戲邏輯，引擎單一權威 |

---

## 十二、已確認事項（全部拍板）

| # | 事項 | 決定 |
|---|---|---|
| 1 | 座位數 | **6–9 可變，預設 9**，Bot 任意增減（最少 6-max） |
| 2 | 牌桌方向 | 橫向橢圓桌 |
| 3 | log 保留 | 全量保留 |
| 4 | Bot 設置 | 可重複任意設置 |
| 5 | 技術棧 | Tauri 2.x（Rust 引擎 + React/TS 前端），只以 Windows 本機版需求設計 |
| 6 | play 範圍 | 互動對打 ＋ 批次模擬 ＋ 重播，**三者全做** |
| 7 | 策略編輯器深度 | 完整 13×13 矩陣＋postflop 規則編輯（GTO Wizard 等級），**預設收合** |
| 8 | 分發方式 | portable exe ＋ installer **兩版都出** |
| 9 | 儀表板顆粒度 | **先 13×13 格**，combo 下鑽列後續 |
| 10 | 真人多人連線 | **不做**；6–9 人只表示 1 名使用者 + 5～8 Bot 的本機桌型 |
| 11 | UI 優先級 | 參數與統計呈現優先；牌局動畫保留但次要 |
| 12 | 效能驗收機 | 258V／32GB 筆電為上限，只可使用同級或更低規格 |
| 13 | 規格來源 | 只採核心規格開頭列出的四份現行文件。`ui-design-spec.md` 為視覺參考素材、不具規範力；已移除的過期決議亦然 |
| 14 | 牌局規則 | **完全比照現實德州撲克**（TDA 標準），細則見 [`德州撲克規則細則.md`](德州撲克規則細則.md) |
| 15 | 籌碼政策 | **破產離桌**（`bustOut`）；桌次開局深度**預設 200BB**（可調、可逐座覆寫），籌碼跨手結轉。不做既有玩家補碼 |
| 16 | 自動補位 | **可開關**；開啟時 Bot 離桌後於下一手前補入新 Bot，補回目標人數（6–9，下限 6）。新 Bot 沿用該座位人格、行為參數與起始深度 |
| 17 | run 結構 | 多桌次串接。桌次於使用者破產／人數不足 6（補位關閉時）／達手數上限時結束 |
| 18 | 統計主體 | **只有使用者座位**。Bot 不計入策略統計，僅作對照顯示，不進入顯著性判定與漏洞旗標 |
| 19 | 手數設定 | **以 10K 為單位，範圍 10K～1000K** |

**產品方向尚待拍板：無。** 牌局規則、桌型、統計主體、籌碼政策均已拍板。2026-08-16 審查浮現的範圍問題也已於同日決議完畢：

| # | 項目 | 決議 |
|---|---|---|
| A | 面板 F 與 H 是否合併 | ✅ **合併**為單一「面板 F — 報表與數據儀表板」；面板總數 8 → 7（A／B／C／D／E／F／G） |
| B | 6／7 人桌最早位命名 | ✅ **一律 `UTG`**；6 人桌為 UTG／HJ／CO，`LJ` 在 6 人桌不出現 |
| C | 工期與人力配置 | ✅ **不列為待決項**，直接進開發。第九章的估算註記僅供參考，不阻擋任何里程碑 |
| D | 簽章憑證 | 屬採購動作而非決策：M0 啟動申請即可（前置期 1–3 週），年費成本另行確認 |

M0 仍須以實測凍結 exact/sampling 門檻（**批次與互動兩組**）、每決策時間預算、內容產製管線、Bot 參數數值、schema、RunManifest、log encoding 與有效籌碼 bucket 邊界；這些是已指定產出，不得回頭引用非現行文件。

---

## 十三、開發第一步

> 規格已就緒，無待決項，可直接開工。版本控制已建立；現行基線為核心規格開頭列出的四份文件。

**先做（決定後段可行性，愈早知道愈好）**：

1. 在 258V／32GB 或更低筆電建立 M0 benchmark，記錄 Windows 版本、電源模式與 build。
2. **Equity 每決策時間預算實測**（M0 閘門 1）：批次模式 p50 ≤1 ms、p99 ≤5 ms。未達標即依核心規格 7.3 的處置順序調整。
3. **preflop baseline 內容體積量化與產製管線定案**（M0 閘門 2）：算出精確格數，決定 solver 批次產出／顧問抽查簽核／分批範圍。

**同時進行**：

4. 引擎 repo 起步：先寫 side pot、零和守恆與資訊集隔離測試。
5. M0 垂直切片：Rust 發一手 9-max → IPC → SQLite → UI 渲染 → 逐手重播；DecisionView 隔離測試；log 容量測試。
6. 補齊面板 A／C／E／G 的欄位級規格，與面板 B／D／F 同層級。
7. **啟動程式碼簽章憑證申請**（前置期 1–3 週，M4 才辦會卡發佈）。

**開發順序原則**：先凍結參數面板與統計呈現，再排牌桌動畫 polish。
