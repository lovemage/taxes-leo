# 9-max 平台核心規格

> 版本：v1.2  
> 日期：2026-08-16  
> 狀態：**現行權威規格（Normative）**

本文件是 9-max 德州撲克模擬平台的現行產品、牌局、策略、統計與效能基線。

**現行文件清單（僅此四份具規範力）**：

| 文件 | 範圍 |
|---|---|
| 本文件 | 產品邊界、桌型、統計、效能的權威基線 |
| [`德州撲克規則細則.md`](德州撲克規則細則.md) | 牌局規則附則；與本文件衝突時，**牌局規則**以其為準 |
| [`9max模擬平台實做計劃.md`](9max模擬平台實做計劃.md) | 里程碑、工期、人力、風險 |
| [`UI面板詳細規格.md`](UI面板詳細規格.md) | 面板 D／F 的欄位級規格 |

未列於上表者（包含已移除的過期決議文件與任何口頭結論）**不得作為需求、開發、測試或驗收依據**。若現行文件之間衝突，除牌局規則外一律以本文件為準，並須同步修正文句，不得以口頭解釋帶過。

**修訂紀錄**：

| 版本 | 日期 | 內容 |
|---|---|---|
| v1.0 | 2026-08-15 | 建立現行權威基線 |
| v1.1 | 2026-08-16 | 修正桌次結束人數門檻（2 → 6）；`stackPolicy` 補「關閉自動補位」前提；新增座位數不變量；補 dead button 的位置標籤定義；補版本支援政策；統一 All-in EV 公式；補統計效力最小手數表；補每決策時間預算推導 |
| v1.2 | 2026-08-16 | **移除 Bot 等級（L1–L5）**，原等級層 10 欄改為逐座可調的行為參數，Bot 組合 35 → 7 組；**起始深度預設改為 200BB**（仍可調），連帶擴充有效籌碼 bucket 的上界檔位 |

---

## 一、產品邊界與優先順序

### 1.1 產品形態

- 單機 Windows 桌面應用程式，牌局、Bot、策略、統計、log 與重播全部在本機執行。
- 一桌固定為 **1 名本機使用者 + 5～8 個 Bot**，共 6-max～9-max，**預設 9-max**。
- 「6～9 人」只表示模擬桌型與座位數，**不代表真人多人連線**。
- 互動對打時由使用者在自己的座位手動選擇行動；Bot 座位由本機引擎決策。
- 批次模擬時使用者座位改由已選策略自動決策，不播放逐手動畫。

### 1.2 明確不做

- 不做真人多人連線、房間、配對、邀請、觀戰或聊天。
- 不做伺服器權威牌局、帳號、雲端同步或跨裝置同步。
- 不做真錢、代幣交易、儲值、兌現或其他金流玩法。
- 不做 6-max 以下桌型。最低為 6-max，2／3／4／5-max 均不在範圍內。
- 手機、Web、雲端與真人連線均不是目前產品路線。若未來提出，視為新的產品決策與獨立範圍，不得用「已預留」擴張 v1。

### 1.3 交付優先順序

1. 牌局規則、隱藏資訊隔離、籌碼與統計正確。
2. 參數容易找到、理解、比較、修改、復原與驗證。
3. 統計資料能顯示定義、樣本數、不確定性與切片脈絡。
4. 操作期間 UI 保持順暢，批次運算不阻塞介面。
5. 牌局動畫保留並須清楚，但屬輔助理解，不得犧牲前四項，也不得阻擋操作或批次模擬。

---

## 二、牌桌與牌局規則

### 2.1 桌型設定

| 欄位 | 現行定義 |
|---|---|
| `players` | 6～9，**預設 9**；恆為 1 名使用者，其餘為 Bot |
| `autoRefill.enabled` | 布林值；開啟時 Bot 離桌後自動補新 Bot 入座 |
| `autoRefill.targetPlayers` | 6～9，且不得大於 `players`；補位後的目標人數。**下限恆為 6** |
| `handLimit` | 執行手數上限，**以 10K 為單位**，範圍 10K～1000K |
| `smallBlind` / `bigBlind` | 以最小籌碼單位保存的正整數；`SB < BB` |
| `stackBySeat` | 每座的**桌次開局**起始深度，以整數 BB 保存，**預設 200 BB**。全桌統一設定並可逐座覆寫。開始一手後不可改當手籌碼 |
| `stackPolicy` | 固定為 `bustOut`：籌碼歸零者離桌，籌碼跨手結轉。不實作既有玩家補碼。**關閉**自動補位時桌上人數只減不增；**開啟**時離桌 Bot 由 `autoRefill` 於手與手之間補入新 Bot（見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.1.1） |
| 桌次結束條件 | 使用者破產／**自動補位關閉且在桌人數降至 6 以下**／達手數上限，三者任一即結束並開新桌次（見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.3） |
| **座位數不變量** | **任何一手開始時在桌人數必須 ≥ 6。** 上一手結算後人數不足 6 時，開啟自動補位則於下一手開始前補足至 `autoRefill.targetPlayers`，關閉則立即結束桌次。引擎在任何情況下不得發出 5 人以下的手牌 |
| `ante.mode` | `none`／`perPlayer`／`bbAnte`／`btnAnte` |
| `ante.amount` | 最小籌碼單位的非負整數；`none` 時必須為 0 |
| `straddle.mode` | `none`／`single`／`double` |
| `straddle.seats` | 指定合法座位與順序；double 必須有兩個不同座位 |
| `straddle.amounts` | 每段為最小籌碼單位正整數；首段為 2×BB，後一段必須為前一段的 **2 倍**（現實規則） |
| `rake.pct` | 0～100% 之固定點數值 |
| `rake.cap` | 每手總抽水上限，以最小籌碼單位或 BB 顯示、整數保存 |
| `rake.noFlopNoDrop` | **布林值**；開啟時，該手未發出 flop 即不抽水 |

Rake 以整手可抽水底池計算，先依比例向下取整至最小籌碼單位，再套用每手 cap；main／side pot 的分配不得重複扣除 rake。任何 rake 規則變體若不符合這個模型，必須新增具名 enum，不得以「三段」等未定義文字代替。

### 2.2 位置與行動順序

- 6～9 人依按鈕順時針映射座位。位置輪轉以 **BB 位每手前進一個在桌玩家**為基準，SB 與按鈕依序回推；玩家離桌時可能出現 dead small blind 與 dead button，任何玩家不得跳過 BB。規則見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4。
- **位置標籤**（用於統計切片與策略節點索引）與行動順序是兩件事，必須分別定義。每手每個在桌玩家都取得唯一的位置標籤，dead small blind 與 dead button 的標籤規則見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.4.1。位置標籤寫入每手 log，統計層與策略層一律使用該欄位，不得各自從座位索引重新推導。
- Straddle、double straddle、ante、all-in、短額加注是否重新開放行動及最小加注額，必須由引擎的合法行動產生器統一決定；規則內容依 [`德州撲克規則細則.md`](德州撲克規則細則.md) 第一～三章，完全比照現實德州撲克規則。
- UI 不得自行推導可下注金額、最小加注額、底池或 side pot。

### 2.3 籌碼、side pot 與 odd chip

- 所有牌局金額使用整數最小籌碼單位或等價 fixed-point；規則層不得使用浮點籌碼。
- 每手必須滿足：開始籌碼總額 = 結束籌碼總額 + 該手 rake，誤差為 0。
- Main pot 與每個 side pot 都保存參與資格；folded 玩家已投入籌碼留在底池，但不得取得分配。
- Split pot 先按份額整除，odd chip 依按鈕左側起、在該 pot 有資格的最近座位順時針分配。每個 odd chip 的去向必須寫入事件 log。

### 2.4 隱藏資訊與使用者可見範圍

- 引擎內部的完整狀態與決策提供者取得的資訊必須使用不同型別。
- `DecisionView` 只包含該座位可見的底牌、公共牌、公開行動、公開籌碼、位置、底池與策略所需的推導特徵；不得包含牌堆順序、其他未亮底牌或未公開結果。
- `StrategyProvider` 的唯一輸入是 `DecisionView`、自身 range 與由公開資訊推導的對手 range estimates，不得接收完整 `GameState`。
- 測試必須證明：只改變不可見牌、保持資訊集相同時，Bot 在相同 seed 下產生相同 action distribution。
- 互動牌桌只顯示規則允許公開的牌。
- **重播預設顯示全部底牌。** 這是策略分析工具，複盤的用途就是看清楚每個位置手上是什麼、當時的決策對不對；遮住反而讓工具失去意義。仍提供「只顯示實際亮出的底牌」開關，供使用者驗證自己的讀牌。
- 隔離的約束對象是**引擎的決策路徑**，不是複盤畫面：即使 log 保存完整牌局、即使畫面全部攤開，`DecisionView` 仍不得包含他人未亮底牌或牌堆順序。兩者不衝突——牌桌上的 Bot 看不到，桌外複盤的人看得到。
- 攤牌後的公開範圍依現實規則判定：輸家可 muck，只有實際亮出的底牌才是公開資訊。muck 政策為具名設定（`realistic` 為預設／`alwaysShow`），寫入 `RunManifest`；不同設定的統計結果不得混用比較。詳見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 4.2。

---

## 三、執行模式與可重現性

### 3.1 互動對打

- 使用者手動選擇 Fold／Check／Call／Bet／Raise／All-in；UI 只呈現引擎回傳的合法行動與金額區間。
- Bot 回應期間 UI 必須保持可操作並顯示明確狀態。
- 動畫可選 `off`／`fast`／`normal`；動畫不得延遲引擎狀態提交，也不得成為重播正確性的資料來源。

### 3.2 批次模擬

- 使用者座位由所選策略自動決策；不播放牌桌動畫。
- 支援暫停、續跑與取消；暫停／續跑不可改變最終結果。
- 使用者可在運算中瀏覽既有報表與 log；寫入須採背壓或批次交易，避免 UI 餓死。

### 3.3 RunManifest 與重播

每次互動或批次執行都建立不可變的 `RunManifest`，至少包含：

- engine、schema、log format、RNG 演算法與版本；
- master seed、每手 stream 派生規則、執行模式；
- 完整桌型與每座初始籌碼快照；`stackPolicy` 與桌次結束條件；
- 桌次邊界索引（每個桌次的起訖手序、結束原因、存活手數）；
- 使用者策略、Bot persona、行為參數、逐座覆寫的完整快照與內容 hash；
- 基準策略與內容包版本；
- 建立時間、完成狀態、checkpoint 版本。

重播以已保存的牌局事件為主，不重新執行策略決策。若為節省容量而重算衍生數值，必須能由同一 `RunManifest` 與仍受支援的版本完全重建；只有 hash 而沒有內容快照不合格。

**版本支援政策**：

- log format 與 `RunManifest` 採獨立的整數版本號。應用程式必須能**讀取並重播所有既有版本**的 run；捨棄舊版讀取能力視為破壞性變更，需業主書面同意。
- schema migration 只做「舊 → 新」單向升級，且必須可在不遺失事件資料的前提下完成；migration 失敗時保留原 DB 不覆寫。
- RNG 演算法或發牌邏輯變更會改變同 seed 的結果，屬**重跑不一致**而非重播失效：既有 run 仍可依保存事件完整重播，但不得與新版本產生的 run 直接比較統計。UI 必須在跨 engine 版本比較時顯示警告。
- `RunManifest` 記錄產生該 run 的 engine 版本；報表比較兩個 engine 版本的 run 時一律標示為不可比。

### 3.4 RNG

- RNG 演算法與版本固定並寫入 manifest；升級 RNG 視為格式版本變更。
- 每手使用由 master seed、hand index 與用途 domain 派生的獨立 stream；發牌、策略混頻與 Monte Carlo Equity 不得共用同一 stream。
- 平行執行的結果合併順序固定；相同 manifest 重跑須產生逐事件一致結果。

---

## 四、策略與 Bot 決策規格

### 4.1 Preflop 節點

Preflop 不是「每格只有一個頻率」。資料模型必須是：

`node × handClass × action × size → frequency`

`node` 至少包含桌型人數、使用者位置、有效籌碼 bucket、ante、rake、straddle 與完整公開翻前 action history。每個 hand class 在移除不合法行動後，Fold／Call／Raise(size)／All-in 等頻率合計必須為 100%。UI 可用 action tab、分色堆疊或切換圖層呈現，但不得把 call 與 3-bet 合併成無法解讀的單一頻率。

v1 UI 編輯至 169 hand classes；combo 權重由 schema 與引擎支援，combo 級 UI 下鑽不在 v1。

### 4.2 Postflop 節點

Postflop 規則至少包含：街別、board features、hand/draw bucket、存活人數、英雄相對行動順位、各對手相對位置、pot type、當前街與前街公開 action history、面對尺度、英雄與相關對手有效籌碼、SPR、合法行動與尺度。

- 多人底池不得只用單一 `IP/OOP` 表達位置。
- 規則採第一條命中時，編輯器必須偵測重疊、被遮蔽與永遠不會命中的規則。
- 不合法行動先 mask，再正規化；若剩餘權重為 0，必須進入 fallback，不得除以 0 或任選行動。
- Fallback 的版本與命中次數寫入 log，並在報表顯示策略覆蓋率。

### 4.3 Bot 決策管線（兩層參數）

> **2026-08-16 決議：移除 Bot 等級（L1–L5）。** Bot 只由**人格**區分，不再有難度分級。原「等級層」的 10 欄參數保留，改為**逐座直接可調的行為參數**（`botBehavior`）——它們是引擎管線的必需輸入（可用尺度、決策噪音、exploit cap 等），不是分級的附屬品。Bot 組合因此從 7 人格 × 5 等級 = 35 組，簡化為 **7 組人格**。

Bot 參數為兩層：**人格層（`persona`，11 欄）** 與 **行為層（`botBehavior`，10 欄）**，兩層皆可逐座覆寫。

Bot 決策管線固定如下：

1. 基準策略產生合法節點的 action logits／weights。
2. Persona 只產生具名、受上下限約束的偏移。
3. 行為參數套用可用尺度、規則覆蓋、規劃深度、對手模型與誤差模型。
4. 逐座覆寫套用在對應欄位；不得直接注入未登錄參數。
5. 引擎套用 legal-action mask 與 exploit adjustment cap。
6. 若啟用 decision noise，以具名公式混合合法分佈，再正規化。
7. 使用該決策專屬 RNG stream 取樣最終行動。

每個參數必須在 generated schema 中具有型別、單位、合法範圍、預設值、顯示名稱、說明與可覆寫層級。實際偏移公式、套用前後值及最終分佈均寫入決策 trace，供 UI 解釋。

---

## 五、Equity、EV 與統計規格

### 5.0 統計主體

- **所有策略統計的主體只有使用者座位。** EV、bb/100、勝率、行為頻率、169 格熱力圖、逐位置表、逐街分解與策略完整度，一律只計算使用者的決策與結果。
- **Bot 的結果不計入策略統計。** Bot 的聚合數字僅能作為對照基準顯示（例如「使用者 vs 全桌 Bot 平均」），不進入顯著性判定、漏洞旗標與多重比較控制。
- Bot 因破產離桌或自動補位而更替，不影響統計母體，因為母體自始至終是使用者的手牌序列。
- 自動補位進場的 Bot 沿用該座位既有的人格與行為參數設定，Bot 組合在整個 run 中維持不變，`RunManifest` 的 Bot 快照因此仍然成立。

### 5.1 Equity 計算模式

- Complete-hand evaluator 必須精確。
- Range Equity 使用混合計算：狀態空間低於門檻時精確枚舉；多人或大型 range 節點使用可重現的分層 Monte Carlo。
- M0 必須在 6／9-max、preflop／flop／turn／river 代表節點量測時間與誤差後，凍結 exact/sampling 門檻、每種模式樣本預算及 cache key。
- M0 同時凍結 `effectiveStackBucket` 的分檔邊界。破產離桌使籌碼跨手漂移，200BB 的起始深度只約束開局，中局需依 bucket 索引策略（見 [`德州撲克規則細則.md`](德州撲克規則細則.md) 8.5）。
- 取樣模式必須顯示 Monte Carlo 樣本數與計算誤差；不得把取樣值標示為「精算」。
- 聯合範圍必須套用 reach weight、card removal、活躍玩家與各 pot eligibility；多人平手按實際並列人數分配。

### 5.2 All-in EV

**適用範圍**：一手牌只有在出現 all-in 且尚有未發完的公共牌時，才產生 All-in adjusted 結果。以該手**最後一次 all-in 導致行動終止的節點**為界，該節點之前的所有投入與收付均為已實現的事實，之後的 runout 以 equity 取代。無 all-in 或 all-in 發生在 river 行動結束後的手牌，All-in EV 等於實際結果。

**唯一公式**（本節為 All-in EV 的權威定義，其他文件的表述以此為準）：

`allInAdjustedNet = Σ_segments( equityInSegment × potInSegmentAfterRake ) − totalContribution`

- `segments` 為 main pot 與各 side pot；只有對該段有資格的玩家參與該段 equity 計算。
- `potInSegmentAfterRake` 為該段扣除應攤 rake 後的金額；rake 不得在段間重複扣除。
- `totalContribution` 是該手**全部街**的自身總投入（含 blind、ante、straddle）。因此前幾街的實際收付已隱含在此項內，不另立加項。
- 已 fold 玩家不參與任何段的 equity，但其投入保留在對應段的 pot 內。

實際結果與 All-in adjusted 結果分欄保存，禁止混用。

### 5.3 指標與區間

| 指標／情境 | 現行方法 |
|---|---|
| 獨立批次的 bb/100／EV | **moving block bootstrap**；block 長度依 M0 實測的自相關長度凍結，桌次邊界為強制 block 斷點。桌次數足夠多時可改用桌次 cluster bootstrap，實際使用的 estimator 名稱必須顯示於報表 |
| 逐位置切片 | 位置定義隨在桌人數改變，必須**依當手在桌人數切片**；9 人桌與 6 人桌的同名位置不得合併 |
| 有效樣本 | 以 block 數（或桌次數）計，不以原始手數計；block 數不足時標示樣本不足 |
| CRN／duplicate 策略比較 | 對配對差值估計 CI；不得用兩次獨立 CI 相減 |
| 獲勝手數比例、VPIP、PFR 等比例 | 顯示分子／分母；獨立近似用 Wilson interval，有 cluster 時對 cluster bootstrap。**不使用「Win Rate」一詞**（撲克圈慣指 bb/100，見 [`UI面板詳細規格.md`](UI面板詳細規格.md) F.2） |
| 精確枚舉 Equity | 標示「模型內精確」，不顯示虛構 sampling CI |
| Monte Carlo Equity | 顯示樣本數與 Monte Carlo CI／誤差上限 |
| 169 格與大量情境切片 | 預設標示探索性；若產生「漏洞」旗標，使用 BH-FDR 或預先登錄的多重比較控制 |

CI 跨 0 只能標示「本樣本無法判定優劣」，不得直接等同「樣本不足」。樣本不足由預先定義的最低有效樣本、CI 寬度或 Monte Carlo 誤差門檻判定。

### 5.3.1 統計效力與最小手數

`handLimit` 下限為 10K，但 10K 手不足以支撐大多數切片。以每手結果標準差 σ ≈ 90 bb/100 估算（M0 須以實測值取代），95% CI 半寬 ≈ 1.96σ/√(N/100)：

| 分析層級 | 切片數 | 半寬 ±5 bb/100 所需手數 | 半寬 ±10 bb/100 所需手數 |
|---|---|---|---|
| 整體 bb/100 | 1 | 約 12.4 萬 | 約 3.1 萬 |
| 逐位置（單一在桌人數） | 6～9 | 約 100 萬 | 約 25 萬 |
| 逐（在桌人數 × 位置） | 最多 30 | 約 370 萬 | 約 93 萬 |
| 169 格熱力圖 | 169 | 遠超 1000K 上限 | 遠超 1000K 上限 |

**因此**：

- moving block bootstrap 的 CI 通常比上表的獨立同分佈估計**更寬**，上表是樂觀下界，不是保證值。
- 169 格熱力圖在 `handLimit` 上限 1000K 下仍無法對每格做顯著性判定。該圖**定位為探索性視覺化**，不得產生「漏洞」結論，也不得對單格做優劣宣稱；旗標一律走 BH-FDR 或預先登錄控制（見 5.3 表）。
- UI 必須在使用者設定手數時**即時預覽該設定的預期可分辨差距**，不能等跑完才在報表揭露（見 [`UI面板詳細規格.md`](UI面板詳細規格.md) F.5.1 與實做計劃面板 A）。
- 上表為規劃用估計值。M0 必須以實測 σ 與自相關長度重算並凍結，凍結後的表寫入產品內說明。

### 5.4 呈現原則

- 所有 EV／bb100 顯示樣本數、估計方法與 95% CI。
- 所有比例顯示分子／分母與區間；分母為 0 時顯示 N/A。
- 報表不得宣稱 GTO 正解或以單一 exploitability 數字包裝規則式 Bot。
- 「最虧三類情境」必須同時顯示樣本數、區間與探索性標籤，不得只依點估計排序後斷言漏洞。

---

## 六、UI／UX 呈現原則

### 6.1 參數優先

- 首層顯示常用參數與目前生效值；進階層顯示繼承來源、預設值、覆寫值與合法範圍。
- 支援搜尋、分類、重設單欄、重設群組、複製座位、比較變更、未儲存提示與 undo／redo。
- 任何修改先在前端依 generated schema 即時驗證，提交後仍由引擎再次驗證；兩端規則來源相同。
- 人格／行為參數／座位覆寫必須用「預設 → 修正 → 最終值」呈現，不得只顯示最後數字。

### 6.2 統計優先

- 儀表板先顯示結論是否可判定，再顯示點估計；樣本、CI 與 estimator 不得藏在 tooltip 才能看到。
- 任何切片都保留目前桌型、策略版本、Bot 組合、時間範圍與有效樣本脈絡。
- 熱力圖同時提供色階圖例、數值、樣本門檻與色盲可辨識方案；不能只靠紅綠顏色。

### 6.3 動畫為輔助層

- 動畫只負責解釋發牌、下注、收池、showdown 與重播順序。
- 動畫可關閉或加速；報表、參數編輯及批次模擬不依賴動畫。
- 動畫資源與效果延後於參數面板、統計呈現及可用性驗收，不得反向壓縮核心工期。

---

## 七、效能驗收基準

### 7.1 驗收硬體上限

- 參考機：**Intel Core Ultra 7 258V 等級筆電 CPU、32GB RAM、內顯**。
- 正式效能驗收只能使用與上述相同或更低效能的 CPU，RAM 不得高於 32GB；不得以桌機高功耗模式或獨立顯卡結果代替。
- 每次報告記錄 Windows 版本、CPU、RAM、電源模式、engine build、資料庫大小與測試設定。

### 7.2 UX 硬門檻

| 情境 | 驗收門檻 |
|---|---|
| 參數輸入、切換、即時驗證 | p95 ≤ 100 ms |
| 面板切換、篩選已載入資料 | p95 ≤ 250 ms |
| 聚合圖表更新 | p95 ≤ 500 ms；超過時須顯示 loading／progress |
| 100 萬手 DB 載入指定手牌 | p95 ≤ 200 ms，冷啟動與 warm cache 分開記錄 |
| 互動模式 Bot 回應 | p95 ≤ 1 秒，單次不得超過 3 秒；超過 250 ms 顯示思考狀態 |
| 暫停／取消 | 暫停確認 ≤ 1 秒；取消確認 ≤ 2 秒 |
| 動畫 | 目標 60fps；驗收 p95 frame time ≤ 33 ms，且不得阻塞操作 |
| 記憶體 | 100 萬手批次與報表並行時 working set ≤ 12GB，且無持續成長 |

### 7.3 批次基準

在 M0 凍結的代表設定與預設 Equity sampling budget 下：

- 10 萬手須在 60 分鐘內完成；100 萬手須在 12 小時內完成。
- DB（含 index，不含可清除 WAL 暫存）100 萬手 ≤ 2GB。
- 批次運算期間仍須通過第 7.2 節的參數操作、瀏覽與取消門檻。
- 若未達門檻，優先調整演算法、sampling budget、寫入格式或可選手數；**不得改用高於參考機的硬體通過驗收**。

**每決策時間預算（由上述門檻反推，M0 必須據此驗證可行性）**：

12 小時 ÷ 100 萬手 = **43 ms／手**。9-max 一手平均約 20–40 個決策節點（含全部 Bot），扣除發牌、結算與 SQLite 寫入後：

| 項目 | 預算 |
|---|---|
| 單一決策（含策略查表 + Equity + 取樣） | **p50 ≤ 1 ms、p99 ≤ 5 ms** |
| 每手引擎開銷（發牌、結算、不變量檢查） | ≤ 5 ms |
| 每手持久化（事件序列化 + 批次寫入攤提） | ≤ 5 ms |

**互動模式與批次模式必須使用不同的 Equity sampling budget**。互動模式在 7.2 的 p95 ≤1 秒下每決策可用約 50 ms，比批次寬鬆兩個數量級；批次模式若沿用互動的 budget，100 萬手門檻必然無法達成。兩組 budget 均於 M0 凍結並分別寫入 `RunManifest`；**不同 budget 產生的 run 不得直接比較統計**。

若 M0 實測顯示多人 Equity 無法壓進 1 ms 預算，依下列順序處置，並在 M0 閘門記錄實際採用的方案：降低 sampling budget → 導入節點級 equity cache → 對 postflop 多人節點改用預算內的近似（誤差需量化揭露）→ 調降 100 萬手門檻或延長 12 小時上限（需業主同意）。**不得以提高硬體規格處置。**

---

## 八、最低驗收矩陣

- 桌型：6／7／8／9-max。
- 籌碼：統一深度與逐座不等深度；至少三人 all-in 的多層 side pot；桌次全生命週期的籌碼守恆。
- 破產離桌與補位：dead small blind、dead button、BB 不被跳過、補位關閉時人數不足 6 即結束桌次、使用者破產即結束桌次、自動補位沿用座位人格設定、dead button 手的位置標籤、每手開始人數 ≥6 的不變量（測試向量 R15–R25）。
- Forced bets：四種 ante 模式、無／單／double straddle、rake 開關、cap 與 no-flop-no-drop。
- 規則：fold、check、call、完整加注、短額 all-in、split pot、odd chip、showdown；[`德州撲克規則細則.md`](德州撲克規則細則.md) 第九章的測試向量 **R1–R25** 全數通過。
- 資訊隔離：每種街別驗證 DecisionView 不含未公開牌。
- 可重現：完成、暫停續跑、不同平行度三條路徑逐事件一致。
- 統計：用可人工計算的合成資料驗證每種 estimator、CI、比例分母、FDR 與 All-in EV。
- 儲存：schema migration、崩潰復原、2GB DB 搜尋、匯出與清理。
- UX：第七章全部門檻在不高於 258V／32GB 的筆電上通過。

