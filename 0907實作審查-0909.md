# 0907 實作審查 · 2026-09-09

審查目標：目前工作目錄 `c7afc1102cd91918c41179e6f1abcbd89535a51c`，branch `feat/report-panel-f-ipc`。本次未建立、切換或推送 branch，未修正產品程式。

## 結論

目前可見的 Claude 提交完成的是 **0907 Stage 0 的報表前置工作**，提交訊息也明確如此描述。不能據此判定整份 0907 計畫完成。

此工作目錄仍有舊 `HandStrength`、單欄 `board_texture`、IPC `textures` 與翻後「可用／不成立」表格；未找到 `hero_postflop_overrides`、`enumerate_postflop_nodes` 或 `classify_postflop_hand` 的實作。如果完整版本在另一個 checkout 或尚未同步的提交，本報告不涵蓋該版本。

## 發現

### 1. [P1] 比例區間未處理桌次群聚

位置：[apps/ipc/src/report.rs](apps/ipc/src/report.rs)，`frequencies()` 第 496–538 行、`position_rows()` 及整體比例聚合。

VPIP、PFR、獲勝比例等全部直接呼叫 `wilson()`；`HandFacts` 雖保存 instance，比例聚合只留下總分子／分母，沒有依桌次估計區間。核心規格 §5.3 與 UI 規格 F.3.1 要求有 cluster 時使用 cluster bootstrap。多人長 run 存在同桌連續手的相依性，固定當作獨立樣本會使正式區間缺少規格要求的群聚處理，可能低估不確定性。

建議保存各桌次分子／分母，以固定種子執行適用的 cluster bootstrap；DTO 附實際 estimator 與有效 cluster 數。桌次不足時明確標示近似及限制，不直接冒充已符合正式估計要求。補相同總比例但桌次聚集程度不同的測試。

驗證方式：原始碼與本地規格比對，尚未量測區間偏差程度。

### 2. [P2] C-bet 被加注後的棄牌仍計入 Fold to C-bet

位置：[apps/ipc/src/report.rs](apps/ipc/src/report.rs)，第 372–375 行。

條件只檢查翻牌第一個下注者是否為翻前攻擊者，未檢查英雄當下是否已面對其他人的加注。重現序列：玩家 A 翻前加注，英雄與 B 跟注；翻牌 A 下注 10，B 加注至 30，英雄棄牌。此時英雄面對的是 B 的加注，程式仍將 `faced_cbet` 與 `fold_to_cbet` 設為 true，污染直接面對 C-bet 的防守指標。

建議追蹤目前下注來源及本街加注次數，只在定義中的直接 C-bet 機會計數；另外定義面對加注的統計。補多人隔位加注、英雄先過牌、正常直接跟注／棄牌的固定序列測試。

驗證方式：臨時單元探針直接呼叫 `analyse()`，斷言上述序列不屬直接 C-bet 防守，實際失敗。探針使用最小合成紀錄隔離行動分類，不代表已完成整個引擎端到端案例；測後已移除，產品原始碼恢復原樣。

### 3. [P2] 一手負盈虧的最大回撤被回傳為零

位置：[apps/ipc/src/report.rs](apps/ipc/src/report.rs)，第 459–462 行。

`dispersion()` 在 `n < 2` 時同時將標準差與最大回撤歸零。最大回撤不需要兩筆樣本；從初始累計 0 到第一手 −10 bb，回撤應為 10 bb。短 run 或只完成一手的部分結果會顯示錯誤數字。

建議將標準差的樣本數檢查與累計回撤分開；空資料回撤為 0，單筆損失按初始峰值 0 正確計算。補空資料、單筆正負值及先贏後輸的測試。

驗證方式：臨時單元探針以 −20 最小單位、BB=2 呼叫分析與 `dispersion()`，預期 10.0，實際 0.0。探針測後移除。

## 驗證結果與限制

- `cargo test -p poker-ipc --test report`：11／11 通過。
- `pnpm --filter @taxes-leo/ui build`：TypeScript 與 Vite build 通過。
- 兩個補充邊界探針：2／2 失敗，對應上述第 2、3 項；測試失敗是發現缺陷，不能解讀成驗收通過。
- 已檢查 Tauri／devserver report 端點與型別匯出差異，未執行 Windows Tauri、全 workspace 測試、Clippy 或 10 萬手效能測試。
- codebase-memory-mcp 本次回傳 `Transport closed`，因此改讀實際檔案；結論以目前 checkout 為準。

既有測試以跟注策略為主，能驗證對帳、欄位、dead 手切片與空比例，但不能充分覆蓋翻後下注／加注序列與各種統計邊界。建議先補上述測試與修正，再繼續 0907 其餘階段。
